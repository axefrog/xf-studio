# Package UI bridge — isolated verification, 24 September 2026

Worktree: `D:/Dev/worktrees/package-ui-bridge` on `codex/package-ui-bridge`. Browser: Chrome at `http://127.0.0.1:49327/?verify=1`, using this worktree's independent SQLite and draft. The main checkout's draft and database were untouched. The preview head was unavailable in this checkout because ignored game-derived browser assets were absent; the collection controls loaded and remained usable.

The expanded **Collection files & library** group showed **Check game package** and **Build local package**. The preset was renamed to “UI draft package” without saving it to SQLite. A check of that current draft displayed “Ready to build 1 preset(s)” with the expected `xfs_c…` namespace. Build showed a live “Building and independently verifying” status, then displayed the verified candidate and manifest paths plus “Not installed or game-tested.” The saved collection list stayed at revision 3 throughout both actions, while the compiled build plan contained the unsaved preset name. The local server used the main checkout's ignored experiment-004 plate as a server environment input; the package and all intermediates remained in this worktree's ignored `dist` and `build` trees.

The resulting `xfs/local-package-1` manifest records collection `63922444-1f67-46e1-b17c-2a417787824e`, source SHA-256 `0e9b233e87178537f28ac519f19d71a98eb1047d915c70e249c9bf8cbdfd1ac0`, one preset, seven independently verified unpacked files, an 806,912-byte archive with SHA-256 `c9f376d7045aee4ae2de84e398c17884cb883c4d16f6309a52dca399536fc329`, and `installed: false` / `gameRenderingVerified: false`. The candidate path is `projects/xf-appearance-studio/dist/xfs_c639224441f6746e1b17c2a417787824e-1790202589941761800/` inside this worktree. This is offline package evidence only; no game session or deployment occurred.

Focused validation: six package CLI/server tests, four collection editor tests and two collection identity tests passed; TypeScript check and browser bundle passed. Boundary tests cover foreign origins, extra browser path fields, invalid or oversized collections, simultaneous build requests, real supported and unsupported finish preflight, and server response identity. A full game rendering check remains pending under the existing runtime test card.
