# Bun settings for this project. Run the tests from this folder (bun test tools) so the preload applies.
[test]
# Before any test file loads: switch key input off (XFB_NO_INPUT=1), so no test, whatever it imports,
# can ever press a key on this machine's desktop (RB-41).
preload = ["./tools/test/preload.ts"]
