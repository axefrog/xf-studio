# Four-point new-layer visual check — 24 September 2026

In an isolated `?verify=1` session of the worktree editor on the reference head, **Add layer** created an enabled, mirrored Matte layer. The Shape panel showed `Point 1 / 4`. At Front view, the colour sat as a short wash along the upper lid on both eyes; the eye openings stayed uncovered. At single-eye **Fit shape**, the four-knot outline formed a tapered band above the lash edge with no extra warp handle. This was inspected at face and close-eye scale after the 1K preview reached Ready.

Undo returned the preset to four layers and its historical six-point Petal wash. Adding the layer again and reloading retained the five-layer draft, selected Layer 5 and `Point 1 / 4`. The browser used the verification workspace and local reference assets; no saved V or user draft was opened. Screenshots include private game-derived pixels and are intentionally not committed. This is a browser-preview review, not evidence of game rendering.

The pure regression covers mirrored coverage, empty-preset Add, duplicate preservation, Reset identity, Undo, recipe parsing and flat compiler acceptance. The complete authoring suite, type check and browser build passed for this checkpoint.
