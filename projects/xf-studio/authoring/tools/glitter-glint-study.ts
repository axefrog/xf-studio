/** Preserve the isolated pilot import while the separately versioned browser
 * model shares its authored UV-cell shader implementation. */
export { installProceduralGlintStudy } from "../src/engines/layered-makeup/render/direct-glint";
