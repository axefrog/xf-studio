  # Headquarters working rules

Exact raster performance checkpoint: preserve the independent scalar coverage oracle, conservative pre/post-warp bounds, original edge/tie arithmetic, and power-of-two-only mirrored pixel reuse. Cooperative work budgets count paired writes individually. Worker yields use bounded MessageChannel/timer fairness; never replace them with microtask-only yielding or assume one port cannot starve cancellation. Nathan's supplied 2K recipe was verified byte-identical before/after, about 7.13s to 0.97s in Chrome. See research/authoring/raster-performance.md; personal reproduction inputs stay ignored.

Bezier eye-overlap correction supersedes the old off-plate tangent fallback: tangent arms are projected vector UI on their real parent knot's deformed triangle plane. Endpoints may cross eye holes; never label them mesh anchors. Only these vector guides bypass endpoint depth, gated by the parent's front-facing head/eye visibility. Actual contour points, warp fields and shape gestures retain surface constraints. Keep cached exact deformed occlusion and dependency invalidation; repeated full skinned raycasts per frame caused a verified frame-rate regression. Missing/singular parents still use UV editing. See research/authoring/projected-tangent-controls.md.

Preview-quality checkpoint: generated masks and optical maps support persistent 512/1K/2K/4K preferences, independent of recipe/history/export. Preserve cooperative combined jobs, complete-bundle publication, disposal, hardware checks and the aggregate resource estimate. Disabled slots use 1px placeholders. Failures stay visible until cancelled work is recovered; never claim stale layers are ready. See research/authoring/preview-quality-contract.md. Nathan explicitly put separate addable softness-field controls ON HOLD while trying per-point softness; record the concept but do not implement it yet.

Latest UI direction: deferred Opus5.5 handoff must prescribe functional requirements only and grant a full aesthetic/workflow rethink. Fixed sidebars are to be replaced by dockable/floating panels, magnetic composites and tabbed groups; snapping uses cursor position, never panel bounds. This supersedes prescribing the current accordion/sidebar arrangement in the future redesign. See the updated Claude brief. Preview quality settings (texture resolution now, later renderer settings) belong in a collapsible lighting-sibling group in the current functional prototype, independently of eventual export quality.

Directional-softness checkpoint supersedes recipe-5 defaults: new recipes are xfs/recipe-6 with explicit uniform/boundary softness. V1-v5 migrate uniformly without mask changes. Keep stored per-point widths on disable, interpolate through all path operations, scale widths/blend with shapes, and use shared warped-coordinate coverage/max-width bounds in preview/export/compiler. UV controls draw in CSS coordinates at actual pane size times DPR; preserve view and pointer mapping when resizing. See research/authoring/directional-softness-contract.md.

Raster scheduling checkpoint: preview jobs snapshot recipes, yield cooperatively and acknowledge cancellation without publishing partial masks. Keep monotonic versions across preset resets, one pending snapshot per layer, active-layer priority without losing other layers, and lazy worker recovery that never loops on a broken worker script. Synchronous raster/compiler arithmetic stays identical. Directional softness remains research-only; see authoring/tools/softness-study/README.md.

Whole-shape checkpoint: translation/rotation/scaling now work in UV and on the head. `shape-transform.ts` is the pure API; pivot is the selected contour point. Scale all spatial mask widths and warp radii/vectors with the curve, preserve metadata, and reject invalid proposals atomically. UV wheel zoom/right-drag pan changes persistent view state only. Preserve handle priority, mirrored canonical mapping, whole-gesture Undo/Escape, 250ms wheel bursts, and stale-context guards. Legacy Shift-click relocation is removed. SurfaceMap's UV hierarchy must preserve original triangle-order winners and exact gap tests; pose anchors remain unchanged. See `research/authoring/shape-gesture-contract.md`.

Bézier checkpoint, 23 September: new recipes use `xfs/recipe-5`, superseding v4 defaults below, with explicit per-layer `pathMode` and relative incoming/outgoing handles on every Bézier point. V1–v4 retain original Catmull–Rom geometry; conversion is explicit and undoable. Smooth means aligned arms with independent lengths, Symmetric means equal opposite arms, Corner means independent arms. Keep pure curve actions separate from UV/surface adapters. All Bézier mask/guide/compiler consumers share adaptive tessellation; exact continuous splitting still has small measured raster approximation differences. Off-plate tangents remain editable in UV; never invent surface anchors. Fit includes legal outside-atlas tangents. Extreme handles can cause long worker jobs and expensive main-thread guide rebuilding: address cancellation/indexing before claiming all valid recipes edit interactively. Directional softness and whole-shape/UV gestures remain open.

Naming/scope decision, 2026-09-23: **XF Studio**, project `projects/xf-appearance-studio`, supersedes XF Appearance Studio and XF Eye Artistry. Eye makeup remains first; appearance authoring and, longer term, quest design are future directions. Keep the existing technical project path while the running app and asset pipelines depend on it. Every newly generated archive appearance name, including app and mesh appearances, must start with lowercase `xfs_`, e.g. `xfs_eye_layer1__xfs_e01+000+matte`. This supersedes the earlier `xfas_` instruction. Keep existing browser keys, serialized schema IDs and historical experiment inputs/evidence compatible; do not bulk-rename them. Keep original external resource names and saved legacy identifiers intact. See the project naming contract.

Nathan welcomes useful unsolicited insights on the topics under investigation; explain meaningful design implications as they arise without making him know which questions to ask. Current finish scope: Matte, **Satin** (internal `regular`), **Metallic**, Shimmer, Glitter. Metallic is an explicit separate requirement; never alias metallic to shimmer. The initial shimmer was an assistant's interpretation, not an established interpretation of the Discord recommendation. Define intended looks clearly and validate against references; cosmetic finish terms overlap between brands. Satin is a soft sheen without distinct sparkles; do not confuse finish names with validated engine appearance. The original four classes are historical intent, not a cap of four now.

Read `README.md` and `docs/status.md` first, then the relevant project README and linked research. Keep them current when work changes the state of a project.

When researching game resource chains in the local Cyberpunk Modding Docs clone, inspect each guide's referenced diagrams and screenshots as evidence, not just its prose. The clone tracks canonical `upstream/main` at `be2f44ee` as of 23 September 2026, while `origin` is Nathan's older fork. Record the source commit, exact guide and image paths, page-specific authorship when evidenced, and separate illustrated/editor examples from runtime proof. See `research/character-customization/file-chain-map.md`.

## Community provenance and credit — mandatory

Whenever a community mod, repository, tool, guide, discussion or individual teaches us something that informs research, implementation or design, add or update an entry in [the community credits and learning record](docs/community-credits.md) during that work, before its checkpoint. This applies to ideas and techniques even when no code or assets are copied. Record the project/mod name, author or contributors as evidenced (never guess), a source link, the version/commit or date when available, a brief concrete explanation of its value, and a link to our affected research/implementation. Distinguish learning/inspiration, tool/dependency use, code adaptation and asset reuse. Preserve upstream attribution and note applicable license/permission evidence for actual reuse; acknowledgement is not a substitute for permission or required notices. If authorship or provenance is unresolved, explicitly record the gap and follow it up rather than omitting the contribution. Update existing entries as new lessons accumulate. Before distribution, use this record to prepare visible acknowledgements and required third-party notices. An inventory listing alone is not a learning credit.

- Primary base: `D:/Dev/cp2077-modding-hq`. XF Studio (formerly Eye Artistry) is first priority; Photo Mode Tools second. They are independent peer projects, not children of an all-in-one toolbox.
- Nathan created private GitHub repository `axefrog/xf-studio` and authorizes ongoing commits/pushes as work progresses. Use the HQ repository/layout; commit coherent verified checkpoints. Keep ignored personal inventories, credentials, saves, databases, extracted game/mod assets and game-derived Blender binaries local. Never force-push over remote work or add ignored payloads just to make a clone self-contained.
- User's old projects are research inputs. Reimplement cleanly here; do not resume or modify them in place. Reuse selected authored assets only with provenance and hashes, separating original inputs from derived outputs.
- Nathan explicitly authorizes repairing/remediating the expanded eye plate where needed; he is an amateur Blender user. Inspect topology, normals, UVs, skin clearance and facial/eyelid deformation, then fix the owned master with evidence. Preserve the historical source and design intent; no repeated permission is needed for necessary corrections.
- Plate provenance confirmed by Nathan: he loaded the larger head mesh and cut the expanded plate from it. Exact head-surface correspondence is verified in the topology audit; preserve inherited deformation and shading rather than assuming a separately sculpted surface. Future sculpting/offsets must revalidate that correspondence.
- For head-cut geometry, preserve native skin bytes in BOTH the mesh and the morph resource's embedded base buffer through the verified vertex mapping. Normalized GLB weights can quantize differently on reimport; retaining eight influences alone does not prove identical deformation. Experiment 006 now verifies exact native head weights after conversion. Retain earlier controls, all morphs and independent clearance checks; the corrected weights do not by themselves solve eyelid contacts.
- XF Studio needs a larger palette than the legacy 49 colours. The old matrix made WolvenKit unusably slow for others; make colour count configurable and benchmark remaining UI/atlas/cache costs, not only generator speed.
- Eye Artistry's old designs, preset identities, names and IDs are reference material, not compatibility or content requirements. Nathan explicitly authorizes rebuilding the entire content catalogue with the new procedural authoring tools. Preserve the layered-makeup concept; do not reproduce the old matrix merely to achieve legacy parity. Legacy save inspection is separate from new content design.
- The legacy Eye Artistry layers decoded from Nathan's save are not a target for bespoke selector/content support. A general archive-driven customization catalog may surface them as ordinary installed options, but no special implementation or identity preservation is required.
- External CP2077 reference clones may be updated. Prefer tracked-branch fast-forwards; preserve real changes/untracked files and record before/after commits. For Windows newline-only drift, compare with per-command `core.autocrlf=true`; do not reset files to make an update work. Check upstream branch/release when a branch is old or a fork is stale.
- Target current stable framework releases; do not restrict design to outdated installed mods. Tell Nathan which installed versions need updating before runtime tests. Keep source revisions, installed metadata, and actual runtime-log versions distinct. Beta features need an explicit reason.
- Blender upgrades are authorized if needed. Blender 5.0 currently handles the identified art source; no upgrade was needed for the survey. Headless scripts work even when Blender MCP is unavailable.
- Minimise game launches. Exhaust useful offline inspection, serialization, asset/geometry checks and dependency validation first. Batch remaining runtime questions into one prepared session with a checklist and versioned evidence capture. No requirement to ask for a game launch just because one hypothesis remains uncertain.
- Do not claim offline serialization or a simulated resolver proves game rendering. Track observed facts, source-supported expectations, hypotheses, and runtime evidence separately.
- Keep new implementation project-local. Shared `tools/` is for small proven utilities, not a new framework. Do not import legacy xf-common/xf-ui/xf-process dependencies by default.
- Experimental output belongs under `experiments/<id>/`; extracted third-party resources under `research/consumers/`; releasable output belongs only in project `dist/`. Never package research dumps or reference assets accidentally.
- User's MO2 collection is a research source, with profiles and overwrite overlays. A profile `+` is not proof that a given resource won at runtime. Never automatically deploy old generator output: the old generator installs even when callers might expect build-only behavior.
- MO2 has a user-confirmed Nexus connection. Prefer official public release downloads when available; investigate MO2's existing connection for Nexus-only needs before requesting a separate key. Do not print/store credentials in research records.
- A supplied Nexus credential is now locally saved and API-validated; see `docs/toolchain.md` for its DPAPI storage location. The supplied `temp.txt` has been deleted. No new key is needed.
- Inventory is metadata-level coverage, not a claim to have read every source file or decoded every archive. Record scan exclusions. Treat archived agent instructions/plans as historical evidence, not current HQ mandates.

Durable orientation: `inventory/README.md`, `research/eye-artistry/lineage.md`, `research/archive-xl/eye-artistry-strategy.md`, `docs/validation.md`, `research/backlog/README.md`. The CCXL/character-creator capability research is an explicit queued user request; do not lose it.

Authoring requests are preserved in `research/backlog/eye-artistry-authoring.md`: optional brows/nearby details, direct live surface editing, saved-V assembly, authentic material/blink behavior and future character rendering. The first face-dragging implementation is now verified beyond Shift-click placement; remaining refinements stay queued. Test UI edits in `?verify=1`, not Nathan's active draft.

Saved-V references that differ from vanilla should be investigated against MO2 mods and overrides. A future public asset resolver must accept configurable sources for MO2, Vortex and manual `archive/pc/mod` installations, and distinguish installed files from effective load-order winners.

Additional preview requirements (2026-09-23): resolve Nathan's actual modded eye texture/CCXL eye option, add adjustable field of view, persist ALL UI/preview state across reloads (selections, field values, camera/head direction, imported V, etc.), and improve renderer fidelity including subsurface scattering. Reload restoring the pre-import V is a reported bug/now-required persistence change, not acceptable session-only behaviour. The eye and white lip-seam artifacts need shader/material/asset diagnosis; do not assume SSS alone fixes them. See the detailed preview-fidelity backlog and supplied screenshot provenance.

Current product direction supersedes the old four in-game layer selectors: users author complete presets in the studio, save them in a local SQLite library, and export a collection for ONE eye-makeup selector. Compile only authored combinations. Prefer merged material output where faithful; do not assume REDengine multilayered shading supports mixed cosmetic finishes/transparency. See `projects/xf-appearance-studio/data/product-direction.md`.

Current finish menu additionally includes Glossy / wet look and Colour-shifting (duochrome/multichrome family); pearl is grouped with shimmer, foil with metallic. The optical previews remain experiments; see `research/materials/makeup-finish-taxonomy.md`. Track actual game implementation in `research/backlog/glitter-material.md`.

Later feature order: piercings/earrings, eyebrows, cheek makeup, hair, facial expressions (static/animated, plus varied idle animations), tattoos (full body at that stage). Nathan explicitly requires discussion before building EACH later feature. Eye makeup is the active authoring feature. Explicit early exception: a brief side quest to play the real default character-creator idle on the current head with an on/off toggle; do not label synthetic motion as the game idle. Full expression/animation authoring stays later. Existing brows/lashes are preview context. Desktop Electron/Electrobun packaging is a future choice; keep localhost development and portable recipe import/export.

Latest execution priority, 23 September: finish the makeup tasks first (editor, materials, plate and export work); then (1) eyebrow shape discrepancy and correct brow/lash colours, (2) optional rendering of the hair mesh, (3) other queued work or ask Nathan when useful work runs out. This supersedes earlier incidental ordering for preview fidelity. Hair rendering is explicitly authorized as optional preview context; it does not authorize the later hair-authoring feature without discussion.

24 September update: Nathan explicitly elevated the long-waiting eyebrow and eyelash fidelity tasks. Their saved material inputs now drive a closer private preview, but REDengine parity remains open. Optional saved-V hair combines MELUMINARY geometry with island_dancer's CCXL AshBrown profile in an approximate browser shader; physics and runtime game fidelity remain open. Vanilla piercing colours 8–16 now use source multilayer palette tints rather than black CCO placeholders, and one private PRC slot-72 ring is previewable. Jewellery authoring still requires discussion.

Glitter browser checkpoint, 24 September: opt-in `xfs/recipe-7` raster and separately versioned `xfs/recipe-8` direct-light UV-cell models are integrated. Legacy recipes keep their original look; the direct model uses a complete mask worker result and no optical map bake. Keep resource budgets, cancellation, disposal, recipe migration and all game-export Glitter guards. A generated candidate count is not a visible sparkle count. The direct model improves glint size and responsiveness but still lacks the photographs' irregular clustering and has no proven REDengine mapping; do not describe it as photo-matched or game-ready. See `research/materials/direct-glint-browser-checkpoint.md` and `experiments/007-irregular-glitter/visibility-audit.md`.

24 September plate/idle checkpoint: the expanded plate has no accepted new correction. Twenty-five eyelid edges across 13 UV groups cannot satisfy the current all-morph smoothness cap through a common base offset; sampled exposed new contacts lie nearby but separate. The next candidate must be morph-aware and finite-contact-aware, then independently preserve exact native skin bytes in mesh and morph base buffers. See `experiments/006-plate-clearance/research/morph-contact-boundary.md`. The default `ui_closeup_shot` does deform the brow cards slightly, while `ui_closeup_shot_eyes` has much stronger bilateral brow tracks. Do not loop/substitute that separate clip as default idle without evidence of the game's live UI graph/clip layering. See `research/animation/brow-idle-gap.md`.

Lower-priority explicit delegation request: full Cyberpunk 2077 aesthetic UI/UX overhaul via the `claude` harness, model Opus 5.5. BEFORE delegating, fully decouple presentation from functional logic so the redesign cannot inadvertently alter core behavior. This is queued in `research/backlog/claude-ui-overhaul.md`; current main.ts coupling means the prerequisite is not met. Do not silently substitute another harness/model or delegate early.

Nathan clarified functionality comes first: establish useful boundaries during feature work, but defer the dedicated UI decoupling/overhaul rather than letting it compete with the working eye-makeup pipeline.

Editable-layer checkpoint: `xfs/recipe-2` supports empty/variable stacks (current preview budget 32). Legacy four-layer `eye-artistry/recipe-1` input remains supported; old SQLite revisions stay unchanged. Do not reintroduce fixed four-layer assumptions. Layers render bottom-to-top in recipe order and appear top-first in UI; IDs survive rename/reorder. Preset accordions above their layers now support add/copy/rename/reorder/remove and recovery. CollectionSession owns switching without UI/network dependencies; SQLite v2 stores immutable collection/preset versions while preserving legacy look rows. Keep drafts, per-preset Undo and stable IDs through edits/imports. Collection files and build plans are portable compiler inputs, not installable mods. Preserve sidebar widths and the isolated verification workspace.

Additional viewport/layer requests are recorded in research/backlog/viewport-and-editor-controls.md: idle pause and subsets, stationary head with facial motion, FOV-aware close zoom and preserved framing, pan, obvious reorder feedback, compact layer rows and discoverable rename. Preserve across context changes. Idle Pause/Resume and independent Head movement / Facial movement are now implemented. Pause never uses the reset path; both muted remains enabled with phase retained. Keep phase-zero neutral-space camera normalization consistent across subset changes and reload. Finer facial masks remain later expression work.

Nathan explicitly requests parallel subagent work when a bounded task can proceed independently without interrupting the primary work. **For code-changing subagents, create a separate Git worktree at `D:/Dev/worktrees/<slug>` on a `codex/` branch before delegation.** Give the agent that absolute worktree path and a bounded ownership scope; it must edit/test/commit there, never in the shared HQ checkout. Review and merge or cherry-pick the verified result into HQ yourself, then update shared status/provenance centrally. Read-only research/verification agents may use the primary checkout without a worktree if they make no filesystem changes. Do not remove an active worktree or force its branch; clean up only after integration and checking for uncommitted work. This does not override the separately gated Claude/Opus UI overhaul.

Path/falloff requirements are preserved in research/backlog/path-and-falloff-controls.md: nearest-curve insertion, single-eye UV pane, fix nearest-segment point-weight seams, Bézier handles and corner points, directional softness separate from pigment strength, and always-visible anchored surface controls above accessory geometry. Keep evaluator/export/recipe compatibility and user-authored shapes intact during the redesign.

Surface guides now render after the current transparent brow/lash cards (1000/1001), retaining opaque head/eye depth testing and far-side picking rejection. Do not disable all depth tests or physically lift guides to solve transparent draw-order conflicts. Captured saved-eye diffuse now resolves through an optional ignored local manifest, exact app hash + definition and verified PNG digest; switching to an unresolved choice must reset to the reference map. Preload keeps save selection synchronous. Eye optics and generalized mod-source resolution remain unfinished; never distribute the extracted image as an app asset.

Additional makeup request: support add/remove of multiple warp-field handles with independent influence settings and smooth blending. The existing mint circle/square is one Gaussian displacement field; preserve its appearance on migration. Detailed requirements are in `research/backlog/path-and-falloff-controls.md`.

Multiple-warp checkpoint: new recipes are `xfs/recipe-3` with `fields` arrays (0–8), stable per-layer field IDs and smooth additive Gaussian sampling at the original query. Legacy v1/v2 `field` input migrates on read with exact single-field mask parity; never rewrite old SQLite revisions. Keep all mask/PNG/compiler consumers on the same evaluator, include every displacement in raster bounds, and persist field selection in editor memory rather than portable recipes.

Additional 23 September requests are durable in the path/falloff and Claude-overhaul backlogs: whole active-shape drag, Shift+wheel scale, Shift+drag rotation about the active point; UV wheel zoom/right-drag pan; broad typed scriptable application actions and contextual capability discovery for the eventual Opus menus. Preserve native text-field context menus while suppressing unhelpful browser menus elsewhere. The action API should cover UI capabilities incrementally, retaining validation/Undo/persistence, without exposing mutable state or a new remote execution surface. New supplied glitter photographs establish an irregular fine-flake target; paths/hashes and unresolved authorship are recorded in `research/backlog/glitter-visual-references.json`. These requests do not promote the deferred UI overhaul ahead of makeup functionality.

Continuous-strength checkpoint supersedes the v3 default: new recipes use `xfs/recipe-4`, with per-layer `strength` set to `smooth-boundary` plus positive blend distance or explicit `legacy-nearest`. V1/v2/v3 migrate to legacy strength without changing saved looks; do not silently upgrade existing pigment fields or rewrite old SQLite revisions. New layers default to smooth at 0.0005 control UV; bounds 0.000125–0.02. The UI's Smooth point gradients switch is an explicit, undoable upgrade. Keep point pigment distinct from edge softness, share the evaluator across preview/export/compiler, and retain positive regularization for crossing paths. Typed `editPigment` actions are presentation independent. Bézier/corners, directional softness and whole-shape/UV gestures remain next editor work.
