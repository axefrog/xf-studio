# Saved V hair preview — 23 September 2026

The captured AutoSave-12 has `lm097_hair`, appearance resource hash `14407260537193084196`, definition `38_ash_brown` in its `hairs` group. The selected MO2 profile `2025 (again)` enables **LONG LENGTH PAK VOL 3**. Its installed metadata identifies MELUMINARY package 6.0.0.0 (Nexus mod 27125), and the `#011.xl` customization points to `base\mel_ccxl_hair\appearances\lm097_hair.app`.

The app's two compiled skinned-mesh component hashes are `5445057220471966502` and `2949750575149566069`. WolvenKit's path-hash command maps them to `lm127_hair_pt1.mesh` and `lm097_hair_pt2.mesh` in that package. The app's `resolvedDependencies` still names other style meshes; the component references and local archive entries agree on the actual pair. Part 1 exports 798 vertices and 71 skin joints. Part 2 exports three meshes totaling 151,084 vertices and 121 joints, including dynamic hair bones. Both retain `JOINTS_1` and `WEIGHTS_1`; the preview restores original first-set weight bytes before using the existing eight-influence skin adapter. The mod's `lm_hair__long.mi` points its `Strand_Alpha` input to `hair_lm60_a.xbm`, which supplies the locally converted PNG.

The [local intake script](../../projects/xf-appearance-studio/authoring/tools/intake_hair.ts) copies already extracted/converter-produced GLBs and alpha PNG into ignored `public/assets/hair/`, generates an ignored manifest keyed by the saved app hash and definition, and records [SHA-256 evidence](../../projects/xf-appearance-studio/authoring/evidence/hair-intake-manifest.json). The browser consumes that narrow manifest and validates every asset digest; it contains no MO2 path. Another source adapter can provide the same manifest after resolving effective MO2, Vortex or manual installation resources. No extracted game/mod asset is tracked or packaged.

The Face details toggle renders this hair only when an imported save matches both the hash and definition. It persists in the isolated and regular browser workspaces and leaves makeup recipes unchanged. The two hair rigs participate in the existing head/idle bone binding, but their physics chains are held at rest. The saved `38_ash_brown` binding is **not** reconstructed: this extracted app contains a base `01_blonde_platinum` definition and the mesh/material setup uses dynamic profiles. The browser uses a labelled dark-brown colour and approximate card/cap shading. Installed profile enablement and archive membership do not establish the effective runtime winner or exact rendered colour.

In `?verify=1`, the captured save produced a matched-hair message and visible long hair. Toggling it off exposed the unchanged head, and the off choice survived reload with the imported V intact. Toggling it on during the extracted character-creator idle kept the hair attached through head motion. Three focused matcher/digest/workspace tests, the full authoring suite, typecheck and build are the offline gates; game rendering and hair physics were not claimed.
