# Code health ledger

The code and architecture must stay clean enough that the long roadmap (V customisation, then body, then world tools) keeps moving forward instead of looping on bug fixes. This ledger drives threshold-triggered deep reviews. Routine merge gates (tests, typechecks, [architecture boundary tests](architecture-contract.md), link check, coordinator diff read) run on every merge regardless.

## Review triggers

Run `python tools/review_due.py` after each merge into `main`. A deep review is due when **any** of these holds since the last reviewed commit below:

- **Merges:** 5 or more merged feature branches.
- **Changed lines:** 4,000 or more lines of TypeScript/Python source under `projects/` (tests and generated files excluded).
- **New subsystem:** a new top-level module family or host capability landed. Record it by hand in the table below.
- **Boundary exception:** a new exception was added to the [UI architecture boundary](ui-architecture-boundary.md).
- **Release:** a release tag is about to be created.

## How a deep review runs

- **Reviewers:** independent, read-only reviewers work in parallel by area, typically (1) domain and application core, (2) pipeline, resolver and host adapters, and (3) presentation and desktop.
- **Checks:** conformance to the architecture contract, duplication and dead code, coupling and layering, complexity hotspots, test gaps, error and cancellation handling, and whether new code extends cleanly to later domains.
- **Findings:** each is recorded below with a severity.
  - **High:** a correctness bug, a broken or bypassed boundary, a security/data-loss risk, or a design that would force rework of later features.
  - **Medium:** maintainability debt that will slow the next features.
  - **Low:** polish.

## Debt budget

Reviews never block feature work directly. Fixes run as a parallel cleanup track. **If more than 3 High findings are open, feature merges pause** until the count is back to 3 or fewer; cleanup and critical-path fixes may still merge.

## Last reviewed

| Commit (newest first) | Date | Scope | Result |
|---|---|---|---|
| `6a1ff15` | 2026-09-26 | CC controls slice 2: Character panel, context actions, catalogue endpoint, request-4 | 1 High (PIPE-78), 7 Medium, 11 Low (PIPE-78..83, PREV-86..87, UI-67..72, CORE-70..74; renumbered from the reviewer's IDs, which collided with the blink review). Open Highs 2. All fixed in claude/cleanup-ccpanel |
| `e1219fd` | 2026-09-26 | Game blink (solved clip, per-eye-shape seats, lash and brow binding) | 0 High, 1 Medium, 7 Low (PREV-80..85, UI-61..62, CORE-65). No drift over 5,000 scrubs; no GPL code copied |
| `368f70b` | 2026-09-26 | Diagnostic Glitter route and verifier; `volume-info.ts` | 0 High, 2 Medium, 8 Low (PIPE-68..77). Production guard holds; builds deterministic; verifier misses flake contents. Fixes in claude/cleanup-glitter |
| `88b5379` | 2026-09-26 | Prepare speed (installation registry, route fingerprint, one-launch WolvenKit, prefetch) and the layered cleanup's fixes | 0 High, 8 Medium, 22 Low (PIPE-52..67, PREV-73..79, CORE-63..64). PIPE-41/42/43, PREV-63/65/66/67, UI-48/49/51 confirmed fixed; PIPE-40 and PREV-62 partly. Fixes in claude/cleanup-hosts3 and claude/cc-panel |
| `2ce9987`+ | 2026-09-26 | Creator catalogue and character context (CC controls slice 1) | 0 High, 7 Medium, 14 Low (CORE-50..62, PIPE-46..51, UI-59..60). Fixes fold into CC controls slice 2 |
| `fc36eae` | 2026-09-26 | Studio light rig (rendering, presentation) | 0 High, 0 Medium, 7 Low (PREV-69..72, UI-56..58). Default reproduces the old look exactly; persistence and render-on-demand pass |
| `4afea26` | 2026-09-26 | Platform step 5: facades, live features, `app.transaction`, view contributions, renames, locked looks, spec limits | 0 High, 2 Medium, 9 Low (CORE-45..49, PIPE-44..45, UI-52..55). No performance regression. Fixes in claude/cleanup-platform3 |
| `90b8602` | 2026-09-26 | Layered material and resolver-fed piercings (rendering, resolver, presentation) | 1 High (PIPE-40), 6 Medium, 8 Low (PIPE-40..43, PREV-62..68, UI-48..51). UI-02 and PIPE-11's piercing part confirmed fixed. All fixed in claude/cleanup-layered (PREV-64 as a documented limit), which also made host/page version skew a worded notice and a tried style a sub-second, in-place swap |
| `4bf688a` | 2026-09-26 | Authored plate blend and skin light (rendering) | 0 High, 1 Medium, 4 Low (PREV-57..61). Matches the export plan and the game model otherwise; idle draws nothing. Fixes in claude/cleanup-render3 |
| `4bf688a` | 2026-09-26 | Platform steps 3–4 and the platform cleanup (core); plate blend and skin light reviewed separately | Core: 0 High, 2 Medium, 5 Low (CORE-38..44). All fixed in claude/cleanup-platform2 |
| `bb2cc22` | 2026-09-26 | Deep review since `84cb39d`: platform steps 1-2 (core) and eye rendering + face decals (rendering) | 0 High, 4 Medium, 14 Low. Fixes in claude/cleanup-platform and claude/cleanup-render2 |
| `84cb39d` | 2026-09-26 | Deep review since `f1b732f`: guidance v1, host cleanup, render-on-demand cleanup, UV-window cleanup (four reviewers) | 0 High, 5 Medium, 12 Low. Fixes in claude/cleanup-hosts2 and claude/cleanup-polish |
| `f1b732f` | 2026-09-25 | Deep review since `ac251d8`: plate-local UV window and verifier, store coverage, grading-LUT host, skin chain (pipeline/hosts); skin adapter, creator lighting, scene growth (rendering) | 1 High (PREV-29), 6 Medium, 11 Low. Open Highs 3 (at budget). Fixes in claude/cleanup-hosts, claude/cleanup-window, claude/cleanup-render |
| `ac251d8` | 2026-09-25 | Deep review since `ecb4b33`: plate lift and diagnostics, head camera input, release prep, package gate, site knowledge generator, private-path check, P0 character details; plus a separate security review of the runtime bridge | 2 High (PIPE-28, RB-01), 8 Medium, 15 Low. Open Highs now 4, over budget: feature merges pause until PIPE-28 and RB-01 are fixed |
| `ecb4b33` | 2026-09-25 | Release-trigger review before `v0.1.0-alpha.1`: new 3D preview setup service, and release readiness (workflow, versions, changelog, notices, site, packaging, CI) | 0 High, 1 Medium (PREV-20), 8 Low. Release blockers: Desktop release workflow never run; changelog claims features not in the release. Fixed in claude/release-prep (PREV-20..24, UI-34/35, REL-01; PREV-25 partly; changelog and site corrected); the workflow's first run failed on a test timeout, fixed there, rerun pending |
| `524a575` | 2026-09-25 | Pre-alpha review of presentation, startup, preview card and desktop host at `7e02636` (completes the `19bf84c` deep review after the legacy-shell removal); the core and pipeline cleanups merged since fix reviewed findings | 0 High, 7 Medium, 6 Low (UI-21..33); UI-05 fixed, UI-06 mostly fixed. Alpha blockers UI-19/20/21/22/23/24/25/27 assigned to claude/alpha-polish |
| `19bf84c` | 2026-09-25 | Deep review (10 merges, ~7,000 lines): domain core, and pipeline/verifier/hosts incl. WolvenKit download (two parallel reviewers). Presentation deferred to after the legacy-shell removal merges | 1 High (CORE-16), 8 Medium, 13 Low. PREV-01/02/04/05/06, PIPE-02/16 and UI-07 confirmed fixed. Fixes run in claude/cleanup-pipeline2 and claude/cleanup-core2 |
| `f3f7147` | 2026-09-25 | Focused review: game-asset export and derived 3D preview core | 1 High, 7 Medium, 8 Low (PREV-*). PREV-01/02/04/05/06 assigned to claude/wolvenkit-fetch. |
| `b9597bd` | 2026-09-25 | First deep review: core, pipeline/resolver/adapters, presentation/desktop (three parallel reviewers) | 7 High, 30 Medium, 18 Low. Over the High budget, so feature merges are paused except critical-path work. |

## Open findings

| ID | Severity | Area | Finding | Status |
|---|---|---|---|---|
| PIPE-01 | High | Pipeline | Built-in plate always cut from the vanilla head, not the head the game actually loads (head mods/patches) | **Fixed** (claude/cleanup-pipeline, 25 Sep) |
| PIPE-02 | High | Pipeline | Verifier trusts builder-produced roundtrip/export files; not data-independent; `.xl` only substring-checked | **Fixed** (claude/cleanup-pipeline, 25 Sep) |
| UI-01 | High | Desktop | Damaged/incompatible `workspace.json` bricks the desktop app; window can't close | **Fixed** (claude/alpha-readiness, 25 Sep) |
| UI-02 | High | Rendering | Renderer hard-codes brow/lash identities and per-mod manifests; resolver output not connected to rendering | **Fixed** (claude/multilayer-piercings, 26 Sep): piercings were the last part on manifests; they now render from the resolver's character record through the layered adapter, and the vanilla and PRC manifests, intake tools and aggregation are removed. Core head/plate/eyes load through one typed render record; every V detail renders from the character record with no per-mod identities (boundary test covers the piercing and layered modules) |
| PIPE-78 | High | Resolver | The creator catalogue's `state()` calls `ensure()`, which rebuilds whenever the entry has an error, so a repeating build failure (e.g. unreadable TweakDB after a patch) loops a full rebuild on every 800 ms poll, even with the panel closed, and the panel says "Reading…" forever (`cc-catalogue-service.ts:69,92-99`) | **Fixed** (claude/cleanup-ccpanel, 26 Sep; see below) |
| PIPE-40 | High | Resolver | The host writes untrusted creator and mod names into the character record without the browser parser's bounds (choice names `^[A-Za-z0-9_.@:+ -]{1,96}$`, CNames < 128, ≤ 96 components), and the browser refuses the whole record on one bad entry; `choices` lists every piercing option installed (`render-detail.ts:260-261,356,384`, `character-detail-plan.ts:335`). One piercing pack named `piercings_(ccxl)` blanks skin, eyes, hair and piercings for every V | **Fixed** (claude/cleanup-layered, 26 Sep; see below) |
| CORE-01 | High | Core | Autosave loop: save status re-triggers persist every ~180 ms with no edits | **Fixed** (claude/cleanup-core, 25 Sep) |
| CORE-02 | High | Core | Workspace exceeds browser storage (~5 MB) with realistic histories; autosave silently stops | **Fixed** (claude/cleanup-core, 25 Sep); completed in claude/platform-step4 (look history, `fitWorkspace`) |
| CORE-03 | High | Core (design) | Presets/Undo/routing only understand eye-makeup recipes; needs domain registry + general preset model before CC controls | Partly fixed: [feature-module platform](feature-module-platform.md) step 1 (claude/platform-step1, 26 Sep) routes every action through one registry of system families and the eye-makeup module, with no fallback; step 2 (claude/platform-step2, 26 Sep) puts presets, the library and the workspace on looks with per-feature parts and editor memory, and eye makeup's actions are pure `capability`/`apply` in the registry; step 3 (claude/platform-step3) versions each layer model. Step 4 (claude/platform-step4) gives each look one Undo history. Step 5 (claude/platform-step5, 26 Sep, without its file moves) adds the presentation's feature facades, a live document per feature with generic feature handlers and `app.transaction`, view contributions for panels, layouts and activity sources, and per-look locking of newer data. Open: the step-5 file moves (`features/eye-makeup`, `engines/layered-makeup`, the view list into `compose/`) and steps 7–9 |
| CORE-16 | High | Core | `selectGlitterModel` doesn't know `xfs/recipe-11`: choosing Fine/Clustered/Direct Glitter beside a game-matched Glossy/Shimmer layer is offered then refused, or silently downgrades the schema (`glitter-model.ts:62-65`, `recipe-actions.ts:178-180`). CORE-09 made worse | **Fixed** (claude/cleanup-core2, 25 Sep) |
| PREV-01 | High | Preview export | Incomplete WolvenKit exports cached as complete; preview permanently stuck until the game changes | **Fixed** (claude/wolvenkit-fetch, 25 Sep) |
| PREV-02 | Med | Preview export | Export/preview cache keys ignore WolvenKit identity and GLB/material hashes | **Fixed** (claude/wolvenkit-fetch, 25 Sep) |
| PREV-03 | Med | Preview export (design) | Material chains resolved by WolvenKit's view of the game folder, not the resolver's winning archives. Worse since PIPE-01: Build cuts the plate from the head the launch route loads, while the preview still reads only `archive/pc/content` (`preview-core-recipe.ts:32` comment claims they share one head) | Mostly fixed. Brows, lashes and hair follow the launch route's winning archives (claude/render-resolver). The head's geometry and skin chain do too (claude/skin-material): the character record carries the resolved head and its `skin.mt` chunk, and the renderer draws the skin on the core head when the two are the same surface, otherwise the resolved head itself. The eyes do too (claude/eye-render): the record's `eyes` slot carries the resolved eye component, and the base-game eye remains only as the fallback for layered designs. Open: the eye plate is still cut from the base-game head in the preview (Build cuts it from the launch route's) |
| PREV-04 | Med | Preview export | Catch-all blames WolvenKit for cache/disk/JSON errors | **Fixed** (claude/wolvenkit-fetch, 25 Sep) |
| PREV-05 | Med | Preview export | 'Head missing' inferred from missing outputs; tool failure misreported as blocked | **Fixed** (claude/wolvenkit-fetch, 25 Sep) |
| PREV-06 | Med | Preview export | Duplicated WolvenKit runner (sixth invocation path) with preview-specific errors | **Fixed** (claude/wolvenkit-fetch, 25 Sep) |
| PIPE-23 | Med | Pipeline | Colour-shift "one pigment" rule compares against all active layers, not exportable ones (`finish-export.ts:104`): a Colour-shifting layer plus a Glitter layer omits the whole preset | **Fixed** (claude/cleanup-pipeline2, 25 Sep) |
| PIPE-24 | Med | Verifier | Verifier takes each preset's route from the builder's `build.json` (missing counts as flat); a Shimmer preset compiled flat would pass every gate. Route isn't in the manifest | **Fixed** (claude/cleanup-pipeline2, 25 Sep) |
| PIPE-25 | Med | Pipeline/hosts | Every Build runs the full head-source resolver (source discovery, archive indexes, `.xl` files, head-archive hash) synchronously on the host, even when the plate is cached; extends PIPE-20 | Partly fixed (claude/cleanup-pipeline2): unchanged plate inputs skip extraction and hashing and archive hashes are memoised (about 7 s to 2 s on the reference route); route resolution itself still runs synchronously on the host each Build |
| RES-01 | Med | Resolver | When a creator switcher picks a non-default choice, the default choice's target stays active too (skin types 01 and 03 both active in test states); see [head CC render evidence](../character-customization/head-cc-render-evidence.md) | **Fixed** (claude/resolver-choices, 25 Sep): switcher targets take activation only from switchers, checked against all six vanilla UI presets |
| RES-02 | Med | Resolver | A "None" choice (e.g. no scar) yields an empty entry and a missing-appearance warning instead of nothing | **Fixed** (claude/resolver-choices, 25 Sep): a definition named `None` emits no descriptor |
| PREV-20 | Med | Presentation/startup | A head-load failure after the scene loads leaves head-bound wiring attached (theme binding, app attach, preview device, surface editor, controls listener) and `createScene` late errors leave an extra canvas; Try again then doubles them | **Fixed** (claude/release-prep, 25 Sep) |
| RB-01 | High | Runtime bridge | Unbounded JSON nesting: a ~20 KB tokenless message overflows the game thread's stack (reproduced at depth 5,000 on the self-test host) (`Dispatcher.cpp:261-297`) | **Fixed** (claude/bridge-hardening, 25 Sep) |
| RB-02 | Med | Runtime bridge | `FlushFileBuffers` waits for a client that never reads, so kill and game exit hang (`PipeServer.cpp:194`) | **Fixed** (claude/bridge-hardening, 25 Sep) |
| RB-03 | Med | Runtime bridge | Exceptions can escape thread entries, exported functions and natives (`dump()` on invalid UTF-8; byte-cut `Sanitize`) | **Fixed** (claude/bridge-hardening, 25 Sep) |
| RB-04 | Med | Runtime bridge | A request reported cancelled after a timeout can still run on the game thread (`GameThreadQueue.cpp`) | **Fixed** (claude/bridge-hardening, 25 Sep) |
| PIPE-28 | High | Verifier | Plate verification no longer compares the whole mesh render blob and morph blob against the input: tampered `bonePositions`, `renderLODs`, `version`, `numDiffs`, texture-diff data, morph bounding box, trailing mapping bytes and chunk `materialId` all still verify (`resource-checks.ts:251-253`, `plate-geometry.ts`) | **Fixed** (claude/cleanup-verifier, 25 Sep) |
| PIPE-29 | Med | Verifier | Lift tolerances derive from the builder-chosen quantisation with no bound; the re-quantisation branch is untested (0 of the fixture's targets re-quantise; 24 of 105 on the real plate) | **Fixed** (claude/cleanup-verifier, 25 Sep) |
| PREV-26 | Med | Character details | Detail request key ignores launch route, profile, mod set and WolvenKit identity; `ready` is kept for the host's lifetime, so a new hair mod or profile switch isn't picked up until restart (`character-detail-host.ts:173,199-201`) | **Fixed** (claude/cleanup-verifier, 25 Sep); in-place updates inside an existing MO2 mod folder are not detected until restart |
| REL-02 | Med | Packaging | Package content scan misses lower-case, WSL, percent-encoded, escaped and POSIX user paths; false positives on `git@` remotes (`package-content-scan.ts:18,22`) | **Fixed** (claude/cleanup-hygiene, 25 Sep) |
| REL-03 | Med | Repo hygiene | `check_private_paths.py` misses an address before a full stop and digit-led local parts; placeholder names match as prefixes; exemptions are by file name anywhere and switch off every pattern; some file types unscanned | **Fixed** (claude/cleanup-hygiene, 25 Sep) |
| PREV-40 | Med | Lighting | Creator LUT requested once per scene and never re-requested: a profile/LUT-mod/route change or a first activation before WolvenKit leaves the old or neutral grade until restart (`lighting-preset-stage.ts:49-50`) | **Fixed** (claude/cleanup-hosts, 25 Sep): the stage asks the host on every creator activation and every 20 s while shown; the host answers per installation fingerprint; the grade changes only when the host's cube does |
| UI-38 | Med | Rendering | `scene.ts` grew to 838 lines with skin-placement policy and user wording; still renders every frame and allocates per frame; Creator mode adds a 4x MSAA half-float pass every idle frame (extends UI-11) | **Fixed** (claude/cleanup-render, 25 Sep) |
| PREV-41 | Med | Rendering | Brow underlay always projects onto the core head's UVs, even when the resolved head is drawn (`scene.ts:225-243`) | **Fixed** (claude/cleanup-render, 25 Sep) |
| PREV-29 | High | Hosts/cache | Grading-LUT host and character-detail host share the resolver cache and both name batch folders `tmp/batch-<pid>-<n>` from 1: concurrent preparations delete each other's output and can write a lasting `.failed` marker (`resolver-host.ts:132,184,239`) | **Fixed** (claude/cleanup-hosts, 25 Sep): unique `mkdtemp` batch folders; one extraction lane per cache folder (batches serialised, single-flight per resource); `.failed` only after a clean `convert` with the extracted file intact; versioned markers clear stale ones |
| PIPE-32 | Med | Verifier | UV-window mapping gate isn't independent: the head-UV coverage reference comes from the same `rasterWindow` it checks (a mirrored raster passes) (`package-bake.ts:79`, `verify-build.ts:185-192`) | **Fixed** (claude/cleanup-window, 25 Sep) |
| PIPE-33 | Med | Pipeline | Check doesn't know the UV window: a preset entirely off the plate passes Check, then fails Build with a raw verifier error for the whole collection | **Fixed** (claude/cleanup-window, 25 Sep) |
| PREV-30 | Med | Hosts | Grading-LUT host runs WolvenKit directly: no timeout, exit code ignored, no cancel, no .NET-missing guidance (extends PIPE-04/07) | **Fixed** (claude/cleanup-hosts, 25 Sep): LUT extraction runs through `wolvenkit-cli.ts` with a per-step deadline, strict exit/log rules, cancellation of a superseded preparation and a plain missing-.NET note |
| PREV-45 | Med | Hosts | Any WolvenKit error is transient, so the LUT host retries every ~40 s forever; each retry re-runs discovery synchronously (1.7-2.4 s host stall) (`grading-lut-host.ts:114-126,145,198`) | **Fixed** (claude/cleanup-hosts2, 26 Sep): the host keeps the opened installation per fingerprint; retries re-extract only what failed, with doubling backoff (30 s to 8 min) and at most 5 retries; a WolvenKit exit code or unreadable output on the same archive, entry and WolvenKit is lasting |
| PREV-46 | Med | Hosts | `.failed` markers aren't keyed by WolvenKit identity, so a resource stays missing after a WolvenKit upgrade (`resolver-host.ts:175,180-182,305`) | **Fixed** (claude/cleanup-hosts2, 26 Sep): markers (version 3) record `wolvenKitIdentityKey` and count only for it; the JSON cache key includes the identity |
| PREV-47 | Med | Hosts | Archive-index `.u64` cache written non-atomically and trusted on read: a killed first scan leaves an empty index that hides a mod's overrides until the archive changes (`resolver-host.ts:103,114`) | **Fixed** (claude/cleanup-hosts2, 26 Sep): written with `writeFileAtomic`; a cached index is trusted only at the entry count the archive's index declares, else deleted and read again |
| PIPE-36 | Med | Pipeline | Check can plan without a plate (fresh install) or on a stale one (profile/head/game change): it reports N ready while Build packages fewer, and never says reach wasn't checked (`eye-plate-service.ts:198-200`) | **Fixed** (claude/cleanup-hosts2, 26 Sep): the cache status records the route and head choice (`routeKey`); Check needs it, the content fingerprint and the current window rule to match, and otherwise says reach is checked at Build. A committed vanilla footprint was rejected (verbatim game mesh data) |
| PIPE-37 | Med | Pipeline | Cached plate footprint isn't checked against the current window rule; a future rule change makes every Build fail until the cache is deleted by hand (`eye-plate-service.ts:134-135`) | **Fixed** (claude/cleanup-hosts2, 26 Sep): cached and Check-planned footprints must follow the window rule recomputed from their bounds; the builder's mismatch is `package_plate_stale`, and both hosts discard that entry and build once more |
| CORE-27 | Med | Core (data) | A newer part schema is refused only in the current draft; in recovery drafts, removed presets and Undo history it is dropped as damage and the next save makes the loss permanent (desktop instead refuses the whole workspace). History is written in the current schema | **Fixed** (claude/cleanup-platform, 26 Sep): typed `NewerDataError` anywhere; the workspace opens read-only with its current draft and is never written back; desktop never sets a readable draft aside; histories and workspace looks written in the oldest part schema (step-2 code reads them) |
| CORE-28 | Med | Core | `lastPackageIsCurrent()` snapshots (and stashes) the whole workspace per repaint once a package result exists: ~68 ms per frame on the large fixture (CORE-05 not fully fixed) | **Fixed** (claude/cleanup-platform, 26 Sep): a version-key comparison, no snapshot or stash (68–75 ms → 0.011 ms per repaint); benchmark test |
| CORE-29 | Med | Core (design) | Nine non-root modules import `compose/studio-registry` as a service locator; the boundary test only checks direct `features/` imports | **Fixed** (claude/cleanup-platform, 26 Sep): registry, part registry and live feature injected from the startup and server roots; only `compose/` and roots import `compose/`; the feature-import test is transitive |
| CORE-38 | Med | Core | Export and Build plan save to the library first, which the CORE-30 guard refuses for any collection needing `xfs/collection-2`, so the export fails too and its message says to export to a file (`collection-service.ts:324-325`). Every multi-feature collection will hit this | **Fixed** (claude/cleanup-platform2, 26 Sep): the draft is saved first only when the library takes it; otherwise its snapshot is exported unsaved, with a plain message; test with the real library |
| CORE-39 | Med | Core (data) | A one-feature history is always stored as whole parts (12–17× the `xfs/look-history-1` form), so `fitWorkspace` drops steps instead of switching form: a heavy 32-layer look keeps 4 of 80 steps, below the floor of 10 (`document.ts:380`, `workspace-budget.ts:98-144`) | **Fixed** (claude/cleanup-platform2, 26 Sep): `fitWorkspace` stores every history look-level before dropping steps (whole parts kept whenever they fit); the heavy look keeps all 80; the desktop host keeps that form |
| CORE-45 | Med | Core (data) | Reading with `keep` sends any preset with a truthy `locked` key straight to `keepLook`, skipping part validation, and import reads untrusted files that way (`platform/core/document.ts:200`, `collection-service.ts:374`): an imported file with `"locked": "x"` and a garbage body is accepted, and after autosave the workspace no longer restores | **Fixed** (claude/cleanup-platform3) |
| CORE-46 | Med | Core | `redoValid()` trusts the eye-makeup geometry revision alone, so a feature change with Undo policy `none` leaves Redo offered and Redo then silently overwrites it (`authoring-history.ts:165`, `studio-application.ts:479`) | **Fixed** (claude/cleanup-platform3) |
| CORE-50 | Med | Core | Resetting a link controller clears only the controller; its followers keep the copied position, so the request still draws the old skin tone, neck and body colour (`character-context.ts:219,238-248`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| CORE-51 | Med | Core (data) | Link propagation writes explicit values into every family member: one hair-colour change yields 522 user-set values and a 104 KB preset naming 118 mods, each reported missing elsewhere (`character-context.ts:244-265,357-369`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| CORE-52 | Med | Core | `loadSave` doesn't validate the save's shape and clears the state before iterating: a malformed group throws a raw TypeError after the context was wiped (`character-context.ts:277-283`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| CORE-58 | Med | Core (design) | Two owners for the shown V's creator choice: the character context (slot + definition) and `character.tryChoice` (part/option/choice key), with different persistence (`character-context.ts:409-423`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| PIPE-46 | Med | Resolver | The base creator resource's provenance is dropped, so a mod replacing the base `.inkcharcustomization` shows its options as vanilla (`cc-catalogue.ts:216-219`, `cc-catalogue-host.ts:183-186`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| PIPE-48 | Med | Resolver | The ArchiveXL text-precedence plan and `gameLanguage` live only in the host and are tested only by the opt-in oracle (`cc-catalogue-host.ts:140-160`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| UI-59 | Med | Presentation (design) | The catalogue has no compact projection: 131,856 choices each carry provenance, label and swatch objects (55 MB compact JSON) before reaching a browser (`cc-catalogue.ts:96-109,260-272`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| PREV-50 | Med | Rendering | Decal colour is solved for the game's square-root blend in linear light, but the Studio stage draws straight to the sRGB canvas, so the curve is applied twice (dark liner 115 → 73; lipstick 119 → 97 green); only the Creator preset is linear (`face-decal-material.ts:227-236`, brow path) | Fixed in claude/cleanup-render2 (see below) |
| PREV-57 | Med | Rendering | The plate composite stores the facet moment per layer, not per merged texel, so a faceted layer over another partial layer widens roughness even at mip 0, which the export never does; lower mips weight by coverage where the export box-averages (`plate-blend.ts:189-190, 305, 312-314`). Shimmer 50 % over Glossy 50 %: export roughness 0.253, preview 0.33–0.37 | **Fixed** (claude/cleanup-render3, 26 Sep) |
| PREV-80 | Med | Rendering | `setShape` re-applies the blink even at Closure 0, writing the blink's neutral pose onto ~442 bones the idle owns: with the idle paused, an eye-shape change snaps the face back to the editing pose while neck and body stay posed (`game-blink.ts:183-206,242-243`, `scene.ts:366-367,428`) | Open (claude/cleanup-blink) |
| PIPE-79 | Med | Resolver | Five name/limit rules along preset → context → request → host → page disagree (512 vs 127 vs 128 characters, `/` and `"` allowed in one and refused in the next, 8,192 vs 2,048 entries): one long or quoted name in a preset fails the whole V (`cc-preset.ts:62`, `character-context.ts:350-351,388`, `character-detail-request.ts:56-58`) | **Fixed** (claude/cleanup-ccpanel, 26 Sep; see below) |
| PIPE-80 | Med | Resolver | Deriving a V with choices needs the full catalogue build (texts, TweakDB): once any choice is stored, every launch delays the V by 2.5–3.1 s warm or ~93 s after a mod change, and a catalogue failure fails every such V (`character-detail-host.ts:160`) | **Fixed** (claude/cleanup-ccpanel, 26 Sep; see below) |
| PREV-86 | Med | Rendering | `follow()` calls `setCharacter` on every context publish; after a failed change each publish silently retries (replacing the error with "Updating…") and a failed V is cleared and re-prepared each time (`browser-head-attachment.ts:117-124`) | **Fixed** (claude/cleanup-ccpanel, 26 Sep; see below) |
| PREV-87 | Med | Rendering | Face morphs have two writers (`savedV.clear`/`applySavedV` and `follow()`); a matching key skips the update after the other writer zeroed them, so the head keeps the base face (`browser-head-attachment.ts:109,116-121`) | **Fixed** (claude/cleanup-ccpanel, 26 Sep; see below) |
| UI-67 | Med | Presentation | `drawChoices` rebuilds every choice button when its key changes: keyboard focus drops to `<body>` after a choice, and "Show more" rebuilds thousands of buttons (`panels/character.ts:176-195`) | **Fixed** (claude/cleanup-ccpanel, 26 Sep; see below) |
| CORE-70 | Med | Core | Switcher choices are keyed by `localizedName` and a click sends only the key: two mods' same-named hairstyles both show checked and the second can't be chosen (`panels/character.ts:185-191`, `character-context.ts:130-137`) | **Fixed** (claude/cleanup-ccpanel, 26 Sep; see below) |
| CORE-71 | Med | Core (design) | "Hide my V's own makeup"'s domain rule (Makeup category, active option, its Off key) lives in the panel, against the contract (`panels/character.ts:24,36-42`) | **Fixed** (claude/cleanup-ccpanel, 26 Sep; see below) |
| PIPE-68 | Med | Pipeline (verifier) | The glitter checks never look at the flakes' contents: all-flat normals, flake roughness/metalness reset to the base, negated normal Y and a grey pigment all verify (`mod-verifier/glitter-checks.ts:111-242`, `verify-build.ts:299-381`) | Open (claude/cleanup-glitter) |
| PIPE-69 | Med | Pipeline | Flake counts are unclipped and uncapped: a knob inside `GLITTER_RANGES` yields ~5 M flakes (303 MB) over the window or ~71 M (4.3 GB) over the head, and `Math.min(...)` throws past ~500 k items (`glitter-route.ts:80-85,246,269`) | Open (claude/cleanup-glitter) |
| PIPE-52 | Med | Resolver | Evicting a route's installation doesn't bump its generation, so after a mod change inside an MO2 mod folder the old ready record is served under an unchanged key (`installation-registry.ts:176-180,99-101`, `character-detail-host.ts:123`) | Fixed in claude/cleanup-hosts3 (see below) |
| PIPE-53 | Med | Resolver | A transient failure sticks: the host reuses a ready state however it was built and the try cache keeps nulls and texture-less components from a WolvenKit timeout (`character-detail-host.ts:123`, `character-detail-service.ts:321-680`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| PIPE-54 | Med | Resolver | A resource WolvenKit writes nothing for counts as transient every time, with no marker: each prepare relaunches WolvenKit and the graph (and its merged-CCO memo) is thrown away (`resolver-host.ts:377-387`, `installation-registry.ts:166-170`) | Fixed in claude/cleanup-hosts3 (see below) |
| PIPE-55 | Med | Resolver | The kept graph is bounded by resource count (6,000, above anything the reference setup reads) per view and route, not bytes: the heap grows with every V and style (`installation-registry.ts:35,168`) | Fixed in claude/cleanup-hosts3 (see below) |
| PIPE-56 | Med | Resolver | Slot labels (≥200) and messages (≥512) built from mod names still make the whole record fail to parse (the PIPE-40 symptom) (`render-detail.ts:541-542`, `character-detail-plan.ts:272,302-303`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| PREV-73 | Med | Rendering | A bake during WebGL context loss is marked failed permanently; restore re-arms only baked handles (`layered-material.ts:567-577`, `scene.ts:517-520`) | Fixed in claude/cleanup-hosts3 (see below) |
| CORE-63 | Med | Core | A failed try's message is overwritten when it reverts to the V's own, and hidden for saves: the style snaps back unexplained (`character-detail-actions.ts:168-174,212`, `panels/preview.ts:123`) | **Fixed** (claude/cc-panel, 26 Sep; see below) |
| PIPE-41 | Med | Resolver | `LAYERED_SLOTS` (only piercings and eyes draw layered chunks) guesses where each chunk's `renderMask` answers: shadow-only chunks carry `MCF_RenderInShadows` alone; a visible layered chunk on CCXL hair or an accessory is dropped with no limit code (`character-detail-plan.ts:166,178`) | **Fixed** (claude/cleanup-layered, 26 Sep; see below) |
| PIPE-42 | Med | Resolver | `applyChoiceOverride` re-implements creator selection outside the shared R5 rules: a switcher choice naming several options, or a linked colour follower, loses its other targets (`character-detail-plan.ts:355-366`) | **Fixed** (claude/cleanup-layered, 26 Sep; see below) |
| PREV-62 | Med | Rendering | A WebGL context restore never re-bakes layered materials: handles stay `baked`, so piercings and layered eye designs draw black after a GPU reset (`scene.ts:231`, `layered-material.ts:394`) | **Fixed** (claude/cleanup-layered, 26 Sep; see below) |
| PREV-63 | Med | Rendering | Layered bakes are at least 1024² per chunk with 6 RGBA32F transient and 3 RGBA16F kept targets, unshared, and hidden slots still bake: style 09 keeps ~160 MiB, a 20–30 item framework 0.6–1 GiB (`layered-material.ts:342-347,420,461`) | **Fixed** (claude/cleanup-layered, 26 Sep; see below) |
| PREV-64 | Med | Rendering | Rigid (unskinned) chunks are bound to a static bone at the component root; the component's `parentTransform` binding is never read, so a rigid nose ring stays put while the idle moves the head (`character-detail-loader.ts:92-110,214`) | **Resolved as a limit** (claude/cleanup-layered, 26 Sep): the data binds the part to the entity's `root`, not a head bone, so the preview keeps it in place with the visible limit `rigid-part` (see below) |
| UI-48 | Med | Presentation | The tried piercing style has two sources of truth (`PreviewActions.state.piercingStyle`, `CharacterDetailActions.tried`), and the device port calls back into an application service (`browser-scene-preview-ports.ts:41`, `preview-actions.ts:165`) | **Fixed** (claude/cleanup-layered, 26 Sep; see below) |
| PREV-07 | Med | Preview export | Exporter not a shared host service; no single-flight or cross-process guard | Partly fixed: the resolver fetcher now has one lane per cache folder with single-flight and unique batch folders (claude/cleanup-hosts); the game-asset exporter is still per-caller, and there is no cross-process single-flight (unique folders and atomic cache writes keep processes from corrupting each other) |
| PREV-08 | Med | Rendering (design) | Render record is a closed core-head shape; no cancellation/release; material templates unused | Mostly fixed: record version 2 carries per-component chunks with template, scalars, colours, textures, hair and skin profiles; character details load with cancellation, supersede and dispose (claude/render-resolver); the head's skin moved onto it with a `skin` slot and the `skin.mt` adapter (claude/skin-material). Open: the core head record (plate, eyes and the fixed default skin shown until the character record arrives) is still the closed v1 shape |
| PIPE-03 | Med | Pipeline | Localhost and desktop Build host services drifted (cancellation, deadlines, error codes, result gate) | Open |
| PIPE-04 | Med | Resolver | Resolver WolvenKit runner: no timeout/exit check, poisoned promise chain, non-atomic cache, cache not keyed by WolvenKit version | **Fixed**: the fetcher runs WolvenKit through the shared runner (time limit, missing .NET), its lane never rejects, and cache files are written atomically (claude/cleanup-hosts); the JSON cache key includes the WolvenKit identity (claude/cleanup-hosts2, 26 Sep) |
| PIPE-05 | Med | Pipeline | Readiness and diagnostic tools hard-code MO2 mods/profiles dirs | Open |
| PIPE-06 | Med | Resolver | Five implementations of virtual-file precedence with different semantics | Open |
| PIPE-07 | Med | Pipeline | Five WolvenKit invocation paths; version not recorded in manifest | Partly fixed: Build, eye plate and preview share `wolvenkit-cli.ts`, and the preview records the WolvenKit version; the verifier now runs through it too (claude/cleanup-pipeline2); the resolver fetcher and grading-LUT host too (claude/cleanup-hosts); the package manifest does not record the version |
| PIPE-08 | Med | Pipeline | Eight inconsistent path-containment helpers | Open |
| PIPE-09 | Med | Pipeline | No single typed package manifest schema/parser | Open |
| PIPE-10 | Med | Pipeline | Two MO2 journal/rollback engines and four modlist parsers | Open |
| PIPE-11 | Med | Resolver | Per-mod intake paths/manifests bypass the resolver (PRC, lash, hair, brow, eyes) | Partly fixed: the brow, lash and hair intake tools and manifests are removed (claude/render-resolver); the PRC and vanilla piercing intakes and manifests are removed (claude/multilayer-piercings), piercings resolve like every other slot. Open: `tools/intake_native_eye.ts` (the base-game eye intake study) |
| PIPE-12 | Med | Pipeline (design) | Build, inventory and verifier shaped for one product; need a mod-product descriptor before a second exporter | Open |
| PIPE-13 | Med | CI | Authoring suite not run in CI on pushes to main; oracle/integration tests skip silently | **Fixed** (claude/cleanup-pipeline, 25 Sep) |
| PIPE-14 | Med | Pipeline | process-tree and WolvenKit error paths untested | Partly fixed: the shared WolvenKit runner's success policy, runtime detection and identity are tested (`tests/wolvenkit-cli.test.ts`); process-tree itself is still untested |
| UI-03 | Med | Presentation | Two setup forms with separate settings state (stale Build availability, conflicting revisions) | Open (settings v2 track) |
| UI-04 | Med | Desktop | Desktop bootstrap is a second untyped, untested UI | Open |
| UI-05 | Med | Presentation | Startup wiring duplicated in studio-main, port-smoke and main | **Fixed** (claude/retire-legacy, 25 Sep) |
| UI-06 | Med | Presentation | Desktop decisions leak into shared startup via data attributes; raw error text shown | Mostly fixed (claude/retire-legacy): typed host object; remaining raw error text is UI-22 |
| UI-07 | Med | Desktop | Build readiness probes block the server synchronously | **Fixed** (claude/alpha-readiness, 25 Sep) |
| UI-08 | Med | Desktop | Build failure details only in console | **Fixed** (claude/alpha-readiness, 25 Sep) |
| UI-09 | Med | Desktop | Desktop autosaves queue instead of replacing | **Fixed** (claude/alpha-readiness, 25 Sep) |
| UI-10 | Med | Presentation | UI re-implements domain rules (satin alias, glitter model IDs, eye-shape list, limits) | Partly fixed (claude/platform-step5, 26 Sep): input limits and units are action-spec fields read through `limitsFor` and the descriptors (`action-limits.ts` retired), the eye-shape choices come from the preview snapshot, and panel titles, layouts and activity sources come from view contributions. Further fixed (claude/cleanup-platform3, UI-54): the Satin alias comes from the finish catalogue (`stored`), the shift strength, size variation and orientation spread ranges and the flake density scale from `rt.range`, and the density/size refusal from the domain. Open: the Glitter model-ID → model-name mapping (`panels/inspector.ts`) should come from eye makeup's Glitter-model catalogue |
| UI-11 | Med | Rendering | scene.ts 893-line monolith, no dispose, renders every frame, leaks on load failure | Partly fixed: dispose and load-failure release (PREV-20); render on demand, skin placement and evidence moved out (claude/cleanup-render). Open: `scene.ts` is still about 770 lines (piercing loading, idle rig and eye gaze setup, blink pose); the scene-host split of the [feature-module platform §5](feature-module-platform.md#5-rendering-ui-02-ui-11) |
| UI-12 | Med | Tests | UI/desktop test gaps (bootstrap, panels, dock DOM, startup) | Partly fixed (claude/alpha-polish): the 3D preview first run and startup failure paths are tested through the DOM-free setup service (`tests/preview-setup.test.ts`); the bootstrap, panels and dock DOM still have no behavioural tests |
| UI-21 | Med | Presentation | Preview preparation and WolvenKit setup reachable only through the standalone card; after "Not now" there is no way back until restart (not on the port; no boundary exception) | **Fixed** (claude/alpha-polish, 25 Sep) |
| UI-22 | Med | Presentation | Head-load failure after ready is latched: no retry, raw loader text shown (e.g. "geometry nodes are missing") | **Fixed** (claude/alpha-polish, 25 Sep) |
| UI-23 | Med | Presentation | Preparation/WolvenKit polling stops for good after one failed poll; the card freezes mid-step | **Fixed** (claude/alpha-polish, 25 Sep) |
| UI-24 | Med | Presentation | Every not-ready head state uses the error phase: normal preparation shows a red danger icon | **Fixed** (claude/alpha-polish, 25 Sep) |
| UI-25 | Med | Presentation | Game & tools and desktop Build setup show developer wording (revision/overrides, Bun executable, WolvenKit not marked optional); extends UI-03 | **Fixed** (claude/alpha-polish, 25 Sep) |
| UI-26 | Med | Presentation | `studio-startup.ts` owns settings policy (partial-field merge, queued refresh, revision watch) instead of `LocalSetupActions` | **Fixed** (claude/alpha-polish, 25 Sep): `setup.update` and `requestRefresh()`; the revision watch moved into the preview setup service |
| UI-27 | Med | Accessibility | Card and consent links use yellow `--accent` text: about 1.4:1 contrast in light theme | **Fixed** (claude/alpha-polish, 25 Sep) |
| CORE-04 | Med | Core | Undo at the history limit mislabels entries and creates no-op entries | **Fixed** (claude/cleanup-core, 25 Sep) |
| CORE-05 | Med | Core | Queries deep-copy/reparse the collection (context menu 0.1–0.4 s) and some stash as a side effect | **Fixed** (claude/platform-step2, 26 Sep): target checks, context binding, request capability and the Glitter-model preset lookup read the draft's identity; views get primitive `summary()` facts from part codecs. Reopened as partly fixed by the steps 1–3 review (package freshness still snapshotted and stashed per repaint, CORE-28); completed with CORE-28 in claude/cleanup-platform (reads never stash) |
| CORE-06 | Med | Core | Control edits skip validation and throw raw errors, leaving a transaction open | **Fixed** (claude/cleanup-core, 25 Sep) |
| CORE-07 | Med | Core | Optical-bake and range rules duplicated across core, workers and descriptors | Open |
| CORE-08 | Med | Core | Duplicate action catalogues and Undo policies; finish choices not from the catalogue | **Fixed** (claude/platform-step1, 26 Sep): kind sets, descriptors and Undo policy come from the registry, and refusals carry the code their check chose. Earlier: descriptor-based Undo policy (claude/cleanup-core2), finish enum from the catalogue (claude/cleanup-pipeline2) |
| CORE-09 | Med | Core (design) | One layer's glitter model bumps the whole recipe schema; nested schema conditionals | **Fixed** (claude/platform-step3, 26 Sep): `xfs/eye-makeup-part-2` has no recipe schema; a per-layer model registry validates each layer's models and gives the oldest recipe schema writers use |
| CORE-17 | Med | Core | `layer.setFinish` always replaces optics: re-selecting Colour-shift resets shift colour/strength and adds an Undo step; re-selecting Glossy silently upgrades an earlier-model layer; shift settings aren't remembered like Glitter's | **Fixed** (claude/cleanup-core2, 25 Sep) |
| CORE-18 | Med | Core | `layerExport(layerId)` judges layers alone, but Colour-shift eligibility is preset-wide: the Inspector says Experimental while Check omits the layer | **Fixed** (claude/cleanup-core2, 25 Sep) |
| CORE-19 | Med | Core | Same defect as PIPE-23 (Fresnel rule counts unexportable layers) | **Fixed** with PIPE-23 (claude/cleanup-pipeline2, 25 Sep) |
| CORE-20 | Med | Core | Finish/route rules in five places (`finish-export.ts` two tables, `recipe.ts` `GAME_OPTICS_FINISHES`, verifier `resource-checks.ts:61`, `makeup-stack.ts:184-187`); extends CORE-07/UI-10 | **Fixed** (claude/cleanup-pipeline2, 25 Sep) |
| CORE-10 | Med | Core | One damaged recovery draft blocks the whole workspace restore | **Fixed** (claude/cleanup-core, 25 Sep) |
| CORE-11 | Med | Tests | Core test gaps (startup wiring, history limit, workspace size, routing) | **Fixed**: startup wiring, history limit and workspace size in claude/cleanup-core; the routing tests in claude/platform-step1 (`tests/studio-registry.test.ts`: exhaustiveness, unknown kinds, derived kind sets, golden registry) |

**Low findings** (picked up opportunistically). Paths are relative to `projects/xf-studio/authoring`.

- **PREV-09..16** (preview export, 25 Sep focused review): silent fallback to unhashed prepared files; host precedence drift; non-atomic cache write; no cache eviction or work-folder sweep; duplicate fingerprints; synchronous hashing on the request path; readiness passed via data attributes; test gaps (loader, error mapping, cache reuse).

- **PIPE-15:** a candidate that fails the second result gate stays in `package-candidates/` (`desktop/build.ts:206-212`).
- **PIPE-16:** Fixed (plate inputs hashed at start and end; morph count comes from the plate recipe).
- **PIPE-17:** `reg.exe` output is decoded as UTF-8, and process output is decoded per chunk (multi-byte splits).
- **PIPE-18:** error codes are mapped by matching message prefixes; needs a typed `PackageFilterError`.
- **PIPE-19:** stale comments in `mod-branding.ts` and `eye-plate-cache.ts` (the publish race comment).
- **PIPE-20:** plate derivation parses large JSON on the host event loop; move it to a worker.
- **PIPE-21:** selector indices shift when presets are omitted; save behaviour unproven. Add to the in-game test card.
- **PIPE-22:** the game-folder ArchiveXL bundle is listed non-recursively and outside source discovery.
- **UI-13:** Fixed in alpha readiness (plain library chip and autosave wording).
- **UI-14:** the command palette recomputes every command's availability per keystroke.
- **UI-15:** theme and lighting actions ignore failures and bypass `rt.dispatch`.
- **UI-16:** Fixed in alpha readiness (CSP, navigation guard, 415, identifiers renamed).
- **UI-17:** `desktop/update-electrobun.ts` is dead and unchecked; two TypeScript versions (5.9.3 vs 7.0.2).
- **UI-18:** `about.css` and boot-watchdog styles aren't tokenised; duplicate font-size tokens; palette input has `outline:none`.
- **CORE-12:** a fresh workspace opens the oldest collection and adds an "Unsaved preset".
- **CORE-13:** dead history helpers; glitter measurements never pruned and keyed by layer IDs that repeat across presets; wrong import consequence text.
- **CORE-14:** confusing names (`selectedCollection` returns a preset ID); `ReadonlyDeep` defined three times; shared mutable `glitterChoices`; stale test counts in the boundary doc.
- **CORE-15:** preview reports ready for one frame after an edit; geometry cache recopies on double updates. The reason-code part (codes matched from message text) is fixed in claude/platform-step1.
- **UI-19:** Fixed in claude/alpha-polish (the category is a plain label while Eye makeup is the only one).
- **UI-20:** Fixed in claude/alpha-polish (see below).
- **PIPE-26:** Fixed in claude/cleanup-pipeline2 (plain `plate_source_incomplete` stop).
- **PIPE-27:** Fixed in claude/cleanup-pipeline2 (typed `eyePlateHead` setting in Game & tools).
- **PIPE-07/PIPE-17 (extended):** the verifier's WolvenKit calls and the desktop Bun probe were moved onto the shared runners in claude/cleanup-pipeline2; `dotnet-runtime.ts` still adds another UTF-8-decoded `reg.exe` reader duplicating `install-detection-host.ts`.
- **PREV-17:** the WolvenKit post-install probe ignores the cancel signal (`wolvenkit-setup-host.ts:314`); Cancel does nothing for up to 30 s.
- **PREV-18:** WolvenKit unpacking runs synchronously on the server, and the .NET registry check re-runs every 3 s while the card polls every 500 ms.
- **PREV-19:** test gaps: `tool-download.ts` stall/redirect/length/oversize paths, `createInstalledHeadSource` end to end (its completeness gate is now unit-tested) and `process-tree`. The PIPE-23 and PIPE-24 cases are covered.
- **CORE-21:** Fixed (claude/cleanup-core2): shift edits are labelled "Shift colour" or "Shift strength".
- **CORE-22:** Fixed in claude/cleanup-pipeline2 (dead eligibility exports removed).
- **CORE-23:** Fixed (claude/cleanup-core2): one `historyTimeline()` mapper in `authoring-history.ts` serves both.
- **CORE-24:** Fixed (claude/cleanup-core2): the first collection keeps every editor-memory field except the recipe.
- **CORE-08/CORE-15 (extended):** new actions were added to both the descriptor table and the hand-kept `recipeKinds`/`undoPolicy()` sets (fixed in claude/cleanup-core2, see CORE-08); new refusals came back as `invalid_value` instead of `incompatible_mode` (fixed in claude/platform-step1: a refusal states its code).
- **CORE-26:** codes that step 1 kept as the old text matching produced them, although another code fits better: Blink study during the game idle is `asset_unavailable` (`incompatible_mode`), "no change to undo/redo" and "already the current step" are `invalid_value` (`unavailable`), and "Saved collection is no longer in this list" is `invalid_value` (`missing_target`). The first changes the toast's severity, so decide with the presentation.
- **UI-20 (extended):** Fixed in claude/alpha-polish.
- **UI-28, UI-29, UI-30, UI-31, UI-32:** Fixed in claude/alpha-polish (see below).
- **UI-33:** Partly fixed in claude/alpha-polish: one `PolledHostState` serves both polling ports. Open: `uv-editor` calls `getComputedStyle` on every draw.
- **UI-04/UI-10/UI-11/UI-12 (extended):** `bootstrap.js` grew (Start fresh); Satin alias and glitter ID mapping remain in the UI and the shift slider hard-codes 0–1; `scene.ts` 925 lines, renders every frame, allocates per frame; no behavioural tests for the preview card or startup failure paths.
- **PREV-21, PREV-22, PREV-23, PREV-24, UI-34, UI-35, REL-01:** Fixed in claude/release-prep (see below).
- **PREV-25:** Partly fixed in claude/release-prep: startup head wiring, out-of-order and shared replies, dispose, start gating, failed-head reset and show requests are tested. Open: tests of the rendered card and consent dialog (the suite has no DOM).
- **RB-05..11** (runtime bridge security review at `ac251d8`): Fixed in claude/bridge-hardening (see below).
- **PIPE-81..83, UI-68..72, CORE-72..74** (Character panel review at `6a1ff15`): Fixed in claude/cleanup-ccpanel (see below).
- **PREV-81..85, UI-61..62, CORE-65** (game blink review at `e1219fd`), Open (claude/cleanup-blink):
  - **PREV-81:** during Play blink `animating` stays true for the whole 2.45 s cycle though the clip lasts 0.5 s, so ~80 % of frames redraw a held pose.
  - **PREV-82 (latent):** bones are bound by name only; nothing checks a target's bind against the driver's rest, so a male or modded skeleton with the same names would turn lids about the wrong centres.
  - **PREV-83 (latent):** `setFaceMorphs` re-seats the blink only through `applyFaceMorph` when an eye shape is shown.
  - **PREV-84:** `bake_game_blink.py --setup` without `--output`/`--evidence` overwrites the Studio's blink and tracked evidence, and the evidence names the female setup regardless.
  - **PREV-85:** the knowledge page and CHANGELOG state the per-eye-shape seat and an unmeasured `h111` figure as fact, and omit the `h011` regression.
  - **UI-61:** raw parser errors from a damaged or non-GLB blink asset reach the UI; "not prepared" is shown when no bones matched.
  - **UI-62:** the missing-asset note points to a developer-only guide with no link, and the hard-coded "2.45 s" isn't labelled as a Studio choice.
  - **CORE-65:** idle/blink composition is tested by source-text matches; no tests for damaged assets, drift or restoring a saved Closure.
- **PIPE-70..77** (Glitter route review at `368f70b`), Open (claude/cleanup-glitter):
  - **PIPE-70:** the guard holds only because both hosts' `parseCollection` drops `diagnostics`; no test pins it, and the bundled CLI accepts the knob from any file.
  - **PIPE-71:** the symmetry refusal runs at bake time only, so Check reports ready and Build throws.
  - **PIPE-72:** a region is the box of the layer's control points, missing handles, overshoot, softness and warp coverage (the verifier shares the box).
  - **PIPE-73:** the verifier's copy of the knob rules drops "no `uvSpace: head`" and `tiltMaxDeg ≥ σ/4`.
  - **PIPE-74:** the head-UV accent mask gets no stored-row-order check and no check at the plate's UVs.
  - **PIPE-75:** ~1.05 GB peak per preset build; the verifier builds a message string per texel check.
  - **PIPE-76:** test gaps: determinism covers one chain at 1024×256; ordinary-build identity rests on the 021 README; no negative checks for tilt, density, sheen, accent placement or flake contents; no Check/Build test for a symmetric region.
  - **PIPE-77:** `volume-info.ts` has no tests; `GetVolumePathNameW` runs on every call.
- **PIPE-57..64, PIPE-66..67, PREV-74..79** (prepare speed review at `88b5379`): Fixed in claude/cleanup-hosts3 (see below).
- **PIPE-65, CORE-64** (prepare speed review at `88b5379`): Fixed in claude/cc-panel (see below).
- **CORE-53..57, CORE-59..62, PIPE-47, PIPE-49, PIPE-51, UI-60** (creator catalogue review): Fixed in claude/cc-panel (see below).
- **PIPE-50** (creator catalogue review), Open (unassigned; `resolver-host.ts` JSON-resource mode): the catalogue host re-implements the fetcher's batching for `.json` resources (24 WolvenKit launches cold). A JSON-resource mode needs the fetcher's output walk to keep a raw `.json` resource apart from its serialized `.json.json` companion; left to the resolver owner so two tracks don't edit the fetcher at once.
- **PREV-69, UI-56** (studio light rig review at `fc36eae`): Fixed in claude/cleanup-layered (see below).
- **PREV-70..72, UI-57..58** (studio light rig review at `fc36eae`), Open:
  - **PREV-70:** test gaps: the head-attachment fake scene answers any key, so nothing proves the real path offers the rig; the creator-preset test hides a hand-made light list, not the rig's own; the skin light's image-based term isn't covered by the room-light GPU check.
  - **PREV-71:** the rim is a live directional light even at zero strength, so every lit program (hair's per-light loop included) evaluates a third light that adds nothing by default.
  - **PREV-72:** `studioLighting()` in `scene.ts` has no caller, and the key's reference geometry is written twice (`KEY_DISTANCE`, `DEFAULT_KEY_ELEVATION`).
  - **UI-57:** refusal wording and units differ from the controls ("Environment strength … 0 and 3" for the "Room light" percent slider; exposure refused in raw units but shown in EV, with the default reading +0.3 EV).
  - **UI-58:** no panel test for the EV slider mapping, the setup chips' state or hiding under the creator preset.
- **CORE-47..49, PIPE-44..45, UI-53..55** (platform step 5 review at `4afea26`): Fixed in claude/cleanup-platform3 (see below).
- **UI-52** (platform step 5 review at `4afea26`): Partly fixed in claude/cleanup-platform3 (`featureStateOf` reads the editor document's feature). Open: views still dispatch through `port.authoring`, so facade isolation is neither used nor enforced; moving each feature view onto its own facade, with a boundary test that its dispatches go only through that facade, is deferred to the step-5 file moves (recorded in the [boundary assessment](ui-architecture-boundary.md#open-work)).
- **PREV-65..68, PIPE-43, UI-49..51** (layered material review at `90b8602`): Fixed in claude/cleanup-layered (see below).
- **PREV-58..61** (plate blend review at `4bf688a`): Fixed in claude/cleanup-render3 (see below).
- **UI-11 (extended):** `scene.ts` is 808 lines.
- **CORE-40..44** (platform steps 3–4 review at `4bf688a`): Fixed in claude/cleanup-platform2 (see below).
- **CORE-30..37** (platform steps 1–3 review): Fixed in claude/cleanup-platform (see below); CORE-36 was recorded there as a design gap and is fixed in claude/platform-step4.

## New subsystems since last review

- None.

## Fixed in claude/cleanup-ccpanel

Findings PIPE-78 to PIPE-83, PREV-86, PREV-87, UI-67 to UI-72 and CORE-70 to CORE-74 from the Character panel review at `6a1ff15`, with the reviewer's probes turned into tests (`tests/cc-panel-review.test.ts`, `tests/character-panel-dom.test.ts` over a new light DOM harness, `tests/light-dom.ts`). The registry and spec-fields goldens were recaptured: the diff is `character.hideOwnMakeup`, `character.retry`, `setOption`'s optional `activates` and the shared 255-character name bound. The look-history parity golden and the 0.1.0-alpha.1 reader tests are unchanged. The character record is now `xfs/render-detail-7`, so every V is prepared once more after the update. Checked in headless Chrome (`?verify=1`, own port, this worktree's copy of the caches, reference MO2 profile) with the reference save, save B, the default V and a preset at the limits; a failed preparation was simulated in the page (the host's answer replaced), since the real host prepared every change. Timings (warm): Hide my V's own makeup (2 rows on this save) 0.23–1.07 s to settled; a keyboard-only Eye Color change 0.23 s with focus kept on the same swatch, and Ctrl+Z restored the old choice with focus kept; Default V 1.4 s and Undo back to the save 1.3 s; save B 1.7 s (32 s the first time, WolvenKit exports); a 3,001-entry preset 26 ms to the view and 0.6–1.2 s to the prepared V. The status line (24 px), the quick-action section and the search field kept their positions in every frame of the run. The reference installation has no same-named switcher choices (ArchiveXL merges them by name), so CORE-70 is covered by the fixture tests only.

- **PIPE-78 (High):** a failed catalogue build is kept per fingerprint with its plain line; `state()` starts only the first build for an installation and never builds again; a question that needs the catalogue builds again only after a backoff (10 s, doubling, at most 10 minutes), and `retry` (`POST {kind:"retry"}`, the panel's **Try again**, `character.retry`) builds at once. The context stops at a failure (no polling) and offers Try again; while the build runs it polls every 0.8 s only while the panel is being looked at, else every 4 s. Tests: a build that always fails is built once over 20 state reads, then once more after the backoff; the endpoint answers `failed` (503) with a plain line; Try again follows the catalogue again.
- **PIPE-79:** one name and limit rule (`src/creator-names.ts`): a creator name is at most 255 characters without control characters or a backslash (`/`, quotes and angle brackets are legal CNames); mod names 255 and never a path; 2,048 choices per V; one request limit (8 MiB) for both endpoints. The preset reader, `characterChoiceOf`, `savedDescriptorsOf`, the request parser, the page's option IDs and the action descriptors all use it. A preset entry whose name is only too long is kept verbatim (`unknownEntries` with `unusable`) and written back; entries past 2,048 are reported (`carryPreset`, reason `not-carried`) and never sent, and stay in the kept entries for Save preset. Tests: preset → request → host → stored round-trips names at 255 characters with quotes, slashes and angle brackets; a 3,001-entry preset carries 2,048 and reports the rest.
- **PIPE-80:** the preparation derives a V with choices from a structural catalogue of the merged creator resource it already loads (`structuralInput`: options, choices, links and activation, no texts or TweakDB, memoised per merged resource), so a stored choice no longer waits for the panel's catalogue. Choices that can't be interpreted show the V without them, with one plain line in the ready state's message, and the answer is degraded (prepared again next time). Test: the structural input equals the full catalogue's.
- **PIPE-81:** every page and search answer carries the catalogue's identity; an answer from another catalogue starts the panel again.
- **PIPE-82:** `slotChoices`, `RenderOverride`, the record's `choices` and `character.override` and their readers are gone; the record schema is `xfs/render-detail-7` (v6 is refused as retired, so a page and a host of different versions show the version-skew notice). The README describes the Piercings row instead of tried styles.
- **PIPE-83:** both endpoints refuse a declared `Content-Length` over the limit before reading, and count the body in bytes as it streams (`src/request-body.ts`); `deriveCharacter` keeps one choice per option (the last, in its place); switcher choices are indexed by their activated options (`CatalogueIndex.activating`), so matching and link following are no longer quadratic.
- **PREV-86:** the follower asks for the V's details only when the context's request changes (`src/character-follow.ts`), and `CharacterDetailActions.setCharacter` never prepares the same request again, even after a failure; `retry()` (through `character.retry`) is the one way to try again. Tests: five publishes after a failure send nothing; Try again sends exactly one request.
- **PREV-87:** the context's view is the one last writer of the face: its shape is applied only once the view answers the current state, and written again whenever the saved-V service changes the scene (a save applied or cleared). Test: the same shape is written again after the saved-V service wrote the face.
- **UI-67:** a row's choices are a `ChoiceList` (`src/studio-ui/panels/character-choices.ts`): selection and roving focus change attributes in place, a later page is appended (Off joins the front), another option or search rebuilds and returns focus to the same choice by position. Measured: Show more appended 14 choices to 240 in 62 ms with the first element and focus unchanged.
- **CORE-70:** a switcher choice's identity (the options it activates) travels with `character.setOption`; the context checks it, the host matches it (indexed), R5 takes the chosen position (`SwitcherPicks` in `cco-model.ts`), the view reports each value's `position`, and the list marks the chosen item by position. Tests: two same-named hairstyles, each turning on its own hair, from the page to R5 and in the list.
- **CORE-71:** `character.hideOwnMakeup` is an action of `CharacterContextActions` with a capability (loading, the view still on its way, every makeup row already Off); the host's projection marks the makeup section (`makeup`, `xfs/cc-panel-2`); the panel only dispatches, and the command palette has the command (with Reset every creator change).
- **CORE-72:** every catalogue follow is a session with a generation: a new one aborts the previous pages, searches and views, and an answer that arrives anyway is dropped. Test: a page in flight across a body change never lands under the new body's option.
- **CORE-73:** every action that changes choices (set, reset, reset all, keep changes, hide makeup) needs the catalogue of the shown V's body; a V change and Undo/Redo don't, so a V is never stuck behind a failed catalogue. `setOptions` changes are validated whole by the shared rule; the action catalogue says so.
- **CORE-74:** an earlier build's tried piercing style (the retired preview fields) becomes the matching Piercings choices once the catalogue is ready, if the context is untouched: the host finds the offered head switcher choice with that name activating an option that offers the definition (`legacyChoices`, `POST {kind:"legacy"}`), from the catalogue's data alone. Once the host has matched a loaded preset's choices, only the entries it couldn't match are kept.
- **UI-68:** one status line of fixed height (clamped to one line, full text in its tooltip) with Try again, Keep my changes and Details inline, keeping their place when not offered; the plain lines about what couldn't be used sit behind Details; a row reserves its detail line only when one of its options has a static detail, and "not shown in the 3D view" for a conditional option is a fixed-size marker in the row head.
- **UI-69:** "your save stays loaded" and "uses the creator default" are replaced by what happens ("Undo shows your V again"; a save's choice "can't be changed here"; a preset's choice "keeps the V's own look"); the device words every host refusal by its code and a network failure in one plain line, never the host's own text.
- **UI-70:** choices are a listbox: arrow keys, Home and End move focus without choosing (each choice prepares the V anew), Enter or Space chooses; provenance is each item's and each row's accessible description; **Reset all** dispatches `character.resetAll`. Documented in the style guide's Creator options pattern.
- **UI-71:** tests for the follower, `makeupOff`/`rowOption`, the panel DOM (light DOM harness), a failing build and names at the limits. The boundary scan reads the static parts of template literals and covers the composition root (`browser-head-attachment.ts`) and the new modules.
- **UI-72:** search runs on the host over every choice (`GET ?search=`, `searchChoices`); an open row whose name doesn't match lists only its matching choices (`choicePage` with a query).

## Fixed in claude/cc-panel

CC controls slice 2: the generated Character panel with live preview, plus findings CORE-50 to CORE-62, PIPE-46 to PIPE-49, PIPE-51 and UI-59 to UI-60 from the creator catalogue review, and PIPE-53, PIPE-56, PIPE-65, CORE-63 and CORE-64 from the prepare speed review. PIPE-50 is left to claude/cleanup-hosts3 (above). The look-history parity golden and the 0.1.0-alpha.1 reader tests are unchanged; the registry and spec-fields goldens were recaptured for the new family (the diff is the `characterContext` kinds, `savedV.clear` and the two preset file workflows, less `character.tryChoice`). Checked in the browser (`?verify=1`, reference MO2 profile, own port, disposable data) with the default V, the reference save (three legacy XF Eye Artistry layers, lipstick, blush) and save B; timings are in the [CC controls backlog](../backlog/cc-controls-and-presets.md#measured-in-the-browser-verify1-reference-mo2-profile-26-september).

- **CORE-58:** the character context owns every creator choice for the shown V: `CharacterContextActions` (family `characterContext`, registered in `STUDIO_OWNERS` with a `StudioApplication` handler) holds the V's source and the choices set; the host interprets them (`deriveCharacter`) for the view and the preparation (`xfs/character-request-4` carries the save's descriptors and the choices). `character.tryChoice`, `applyChoiceOverride` and `descriptorsFromSwitcher` are gone; the piercing try is the Piercings row. Tests: `tests/character-context.test.ts`, `tests/piercing-resolver.test.ts` (a piercing chosen in the context, several targets and a linked follower through R5), `tests/studio-application.test.ts`, `tests/studio-registry.test.ts`.
- **CORE-50, CORE-51:** only the choices a person set are stored, one per link family (the latest wins); followers and other controllers are derived at request time, Reset removes the family's choice so it recomputes from the base, and a preset writes only the choices set. Tests: reset of a skin tone on a save gives neck, skin type and body back their saved tone; one skin tone is one preset entry.
- **CORE-52:** `savedDescriptorsOf` validates a save whole before anything changes (the capability refuses it with a plain reason; `recoverSave` is a pure function over validated input).
- **CORE-53:** CNames with `/` and names up to 512 characters round-trip; the writer leaves out (and counts) an entry the reader would refuse instead of writing a file the Studio refuses whole (`presetEntryWritable`).
- **CORE-54:** unknown preset data is copied into prototype-free objects; a `__proto__` key is kept as data and written back.
- **CORE-55:** the 1 MB limit counts UTF-8 bytes; unknown data is bounded (depth 16, 20,000 nodes) with plain reasons; a file's JSON is read once (`readPresetJson`) and the context parses the preset once (the capability's parse is reused by dispatch).
- **CORE-56:** the host checks a preset before writing it with the repository's personal-data patterns (`src/private-data.ts`, which imports only the pattern keys of `tools/private-data.json`, so no bundle carries its test vectors): a name, kept entry or kept field naming a user folder or an address is left out and counted, and a text that still holds one is refused. Test: every vector of the shared file is flagged or passed as the repository scan does.
- **CORE-57:** R5's activation is exported from `cco-model` (`activeOptionNames`, `r5Roots`) and used by both R5 and the context's view.
- **CORE-59:** creator changes have their own Undo history (in memory, 100 steps): every change, the quick action, a save or preset load (one step each, with Keep my changes), and Undo across a V change shows the earlier V (`savedV.restore`, the new `savedV.clear`). `character.reset` requires its part and option; `character.resetAll` resets everything.
- **CORE-60:** the catalogue carries no coverage; the preview side projects it (`catalogueCoverage`) when the panel projection is built, and a `conditional` option settles from the parts the shown record draws (`status.assets.characterDetails.drawn`).
- **CORE-61 (hypothesis):** linked switchers take the choice activating the same options (`linkedChoice`); documented with in-game test ask 11 on the knowledge page.
- **CORE-62:** the catalogue copies every list it takes from the merged resource. Test: changing the catalogue's lists leaves the resource untouched.
- **CORE-63:** a change that can't be prepared keeps the V as it was shown and says why (`updateError`, the panel's status line) until the next change; asking again retries. Test: `tests/character-switch.test.ts`.
- **CORE-64:** the context family needs no scene: before the preview is ready, and while the catalogue loads, a change is refused as `not_ready`.
- **PIPE-46:** a base creator resource won by a mod archive names that mod on its options and choices.
- **PIPE-47:** the text table keys entries by primary key alone, as ArchiveXL does: a same-ID mod entry replaces the whole entry, a secondary-keyed mod entry is added under FNV-1a 32 and 64 of its key, the fallback language only fills in. The secondary-key lookup's tie-break is recorded as a hypothesis on the knowledge page.
- **PIPE-48:** `textPlan` and `gameLanguageOf` are pure and unit-tested.
- **PIPE-49:** every TweakDB count is bounded by the bytes that could hold it before anything is allocated or read.
- **PIPE-51:** a language whose game texts aren't installed falls back to English with the gap `texts-language-missing`; an unset or relative `LOCALAPPDATA` reads no settings file.
- **PIPE-53:** a preparation during which the fetcher's transient count grew, or WolvenKit failed on an archive, is degraded: everything it added to the preparation cache is forgotten (`CharacterPreparationCache.mark`/`forget`), and the host serves the answer once and prepares it again when asked again. Tests: `tests/cc-catalogue-service.test.ts`.
- **PIPE-56:** the planner builds slot labels and messages within the record's limits (`clampedList`: whole names, then "and N more"), and the reader cuts one that is still too long with a note instead of refusing the V. Test: `tests/render-detail-record.test.ts`.
- **PIPE-65:** one rule picks a switcher choice by name, preferring the one that turns an option on (`switcherChoiceNamed`), for R5 and the catalogue alike.
- **UI-59:** the panel gets a compact projection (`xfs/cc-panel-1`, 300 KB for 943 options and 131,856 choices on the reference installation) with choices paged per option, built once per installation in the long-lived host. Test: a 1,000-option, 130,000-choice creator stays under 600 KB.
- **UI-60:** a head option without a category is filed beside the rows around its position.

## Fixed in claude/cleanup-platform3

Findings CORE-45 to CORE-49, PIPE-44, PIPE-45 and UI-53 to UI-55 from the platform step 5 review at `4afea26`, and UI-52 in part. Stored workspace and library bytes of the committed fixtures, the look-history parity golden, the registry golden and the spec-fields golden are unchanged; the 0.1.0-alpha.1 reader tests pass. The reviewer's probes are now tests.

- **CORE-45:** `PartRegistry.readCollection` never trusts a `locked` key: a look is locked only when reading its parts (or, in a stored workspace, its memory) meets a newer build's data. In-memory drafts are validated again with `rereadCollection` (rename and preset edits), which keeps a look this session locked. Tests (`tests/locked-looks.test.ts`): an imported file carrying the key with a garbage body is refused with exactly the message it gets without the key, through the reader and the import request; a readable preset carrying the key opens editable, in a file and in a stored workspace; a look locked only by its newer Undo history stays locked through rename and collection rename, is stored without the key and locks again on restore.
- **CORE-46:** Redo validity also compares the other live features' revision (`AuthoringDocument.partRevision`); when it changed, the look's content key decides, so a change that records no step hides Redo and a selection does not. Test (`tests/look-transaction.test.ts`, from the probe): a `none`-policy content action after Undo removes Redo and keeps its value; a hair selection after Undo keeps Redo.
- **CORE-47:** `fitWorkspace` has two new stages: before dropping recovery drafts or removed presets it writes locked looks in them without their kept memory (`plan.kept: "outside"`), and as the last resort the current presets' locked looks too (`"everywhere"`). Their parts are always verbatim; a look locked by its parts is locked again on restore. Test: the probe's over-budget workspace (a 200-step newer history) now fits at half its size with the locked look's parts unchanged and writable on reload; a recovery draft's kept memory goes first, the draft and the current draft's memory stay.
- **CORE-48:** `app.transaction` refuses a thenable result (reverting what ran synchronously, no step, no Redo) with a plain message, and its type (`NotThenable<T>`) rejects an async body at compile time (`@ts-expect-error` in the test).
- **CORE-49:** the draft summary marks an oldest recoverable draft that holds a locked look, the consequence carries its ID and the lock, and the import/open confirmation then says the draft may be that look's only copy and offers **Export collection** for it. That exports the earlier draft unsaved (`exportCollection` takes an optional `draft`; a draft no longer queued is refused with `missing_target`). Tests: the summary, the consequence and the export of the earlier draft in `tests/locked-looks.test.ts`.
- **PIPE-44:** a locked look never counts as packageable; when nothing else is, Export plan, Check and Build are refused with a plain reason ("Every look in this collection was made with a newer version of XF Studio…", or, beside looks without eye makeup, that the looks with eye makeup are newer). Export collection stays available.
- **PIPE-45:** the Export plan's message names the looks it leaves out instead of saying they are exported as they came, and the plan file carries the view's `omitted` list. The schema stays `xfas/export-plan-1`: the field is optional and present only when something is left out, the package pipeline plans its packaged copy (which never carries it), and no reader checks for unknown fields, so every existing reader and every normal plan is unchanged.
- **UI-52 (part):** `featureStateOf` reads the editor document's feature instead of `"eye-makeup"`. The facade-only dispatch boundary test is deferred to the file moves (see the open findings).
- **UI-53:** facades expose `locked()` (the plain reason, or undefined), backed by `StudioApplication.featureLocked`; the Layers notice and the viewport hint read `Frame.locked`, and the unused `Frame.editable` is gone.
- **UI-54:** the shift strength, size variation and orientation spread sliders take their ranges from `rt.range`; field density is a percentage of the registered count range; the density/size refusal comes from the domain (`IRREGULAR_RANGE_MESSAGE`, built from the flake limits) and the panel shows it as it is; the finish catalogue's `stored` names (Satin's `satin`) replace the alias in `inspector.ts`, `layers.ts` and `target-menus.ts` (`StudioRuntime.finishOf`).
- **UI-55:** "Undo the last change (outside text fields)" and "There is no change to undo."; the style guide's Undo scope text; `feature-module-platform.md` and `ui-architecture-boundary.md` now describe the step-5 names as current. The parity golden, which records the old refusal text, is compared through a one-entry map of that intentional rename in its observer.
- **Browser (`?verify=1`, private port and data):** an imported file whose newer look carries a `locked` key and a damaged body was refused with the same message as without the key; a newer look imported locked showed the Layers notice with "Get the latest version" and the viewport hint; with only that look left, Export plan and Check were refused with the new reason while Export collection stayed available; after four more imports the import confirmation named the discarded draft's newer look and offered Export collection. No console errors.

## Fixed in claude/platform-step5

Step 5 of the [feature-module platform](feature-module-platform.md#step-5-status), without its file moves. Details, decisions, tests and measurements are in the design's step 5 status. Every golden except the registry snapshot is unchanged; that one differs by exactly the Undo-port rename (`history.undo`/`history.redo`, policy `part`).

- **CORE-03 (routing, presentation):** `port.features()`/`port.feature(id)` facades (the port's `editor` became eye makeup's `view()`); a live document per registered feature, generic feature handlers and `app.transaction(label, features, fn)`, tested with a synthetic second feature; view contributions for the panel catalogue, factory layouts, closed-panel homes, lazily repainted panels and activity sources (saved layouts restore unchanged; `<feature>.<panel>` IDs now accepted by the dock).
- **UI-10 (partly):** `units`, `limits` and `consequence` are action-spec fields (`action-limits.ts` retired), with a golden captured before the move.
- **CORE-27 (extended):** newer data in a collection draft or an imported collection file locks only its look, which is kept verbatim and written back exactly by every writer; the rest of the workspace stays editable. The loose editor, workspace-1 drafts and a newer workspace schema keep the read-only protection.

## Fixed in claude/cleanup-hosts3

Findings PIPE-52, PIPE-54, PIPE-55, PIPE-57 to PIPE-64, PIPE-66, PIPE-67 and PREV-73 to PREV-79 from the `88b5379` prepare speed and layered cleanup review, with the reviewer's probes turned into tests. Measured on the reference MO2 route with WolvenKit 9.0.1 in one process through the character-detail host (default V, reference save, save B, the default V again, styles 09, 12 and 14, then both saves again; warm caches, the second of two runs each; offline measurements, not game evidence): the first V in the process 3.06 → 2.99 s (it opens the route), the reference save 1.07 → 0.85 s, save B 0.45 → 0.38 s, the tried styles 0.12/0.42/0.13 → 0.08/0.37/0.09 s; stamp checks for the ten requests 13 → 8; heap after the three Vs 274 → 133 MB and after everything 318 → 136 MB (growth over the first V: +105 → +29 MB), with 44 MiB of JSON kept by the one graph. The resolver integration test (`XFS_RESOLVER_REFERENCE=1`, reference save) and the creator catalogue oracle pass on the real install. In the browser (`?verify=1`, own port, disposable data, reference profile): the default V, then styles 12 gold, 14 gold and 09 black tried in turn, then a forced WebGL context loss and restore; every layered chunk baked again after the restore with the same sizes, the resolved eye stayed shown, and the only console errors were the private idle assets this worktree doesn't stage.

- **PIPE-52:** a route dropped to make room for another keeps its watch list; `revalidate` compares it for a route that isn't open, and an open compares it after opening (a change made even during the open counts), so a change inside an MO2 mod folder while the route was closed moves the generation on. A route whose kept list is let go (more than eight) has its generation moved on. An unchanged route keeps its key. Tests: `tests/installation-lifetime.test.ts` (the reviewer's probe, with and without a check first).
- **PIPE-54:** a resource the archive lists but WolvenKit wrote nothing for, in launches that all finished cleanly (the `unbundle` step's exit is now judged too), gets a `not-written` marker counting batches; from the second (`NOT_WRITTEN_RUNS`) it is lasting, keyed like every marker by the archive's fingerprint and the WolvenKit identity, and removed once the resource is written. The fetch port says whether a null may not repeat (`transient`, per cache folder's lane, whichever fetcher answered); the graph counts only consumer reads (`retryableFailures`), so a prefetched resource nobody asked for no longer replaces the graph or forgets the merged creator resource. Tests: `tests/resolver-fetch-markers.test.ts` (the reviewer's probe: transient, then lasting, then no launch; an unclean launch doesn't count; another WolvenKit tries again), `tests/resource-graph-retention.test.ts`, `tests/installation-registry.test.ts`.
- **PIPE-55:** the graph keeps a mesh, morph target or `.app` as the shape its model reads (appearances, material entries and instances, render-chunk masks, targets, definitions) with only its type as the loaded root, and counts what it keeps (`retainedBytes`: the cache file's JSON length, or the shape's). Over the reference cache the new shapes build identical models to the old whole documents for 269 meshes, 334 morph targets and 349 `.app`s, keeping 21.9 MB where the documents are about 485 MB. The registry keeps the views' graphs of every route within `MAX_GRAPH_BYTES` (256 MiB) in total, giving the least recently used views fresh graphs first and the view being handed out last, and drops a route whose WolvenKit changed in place. On the reference route the graph keeps 44 MiB after the three Vs and three styles, so nothing is evicted in that use; the heap after them fell from 318 to 136 MB. Tests: `tests/resource-graph-retention.test.ts`, `tests/installation-lifetime.test.ts`.
- **PIPE-57:** the installation summary carries `readErrors` (unread archive indexes, unreadable folders, entries, mod list or settings file, unreadable `.xl`); an installation with any is opened again once it is `PROBLEM_TTL_MS` (60 s) old, and its generation moves on only if the errors changed. Test: `tests/installation-lifetime.test.ts`.
- **PIPE-58:** `volume-info.ts` reads the volume's file system (`GetVolumePathNameW`, `GetDriveTypeW`, `GetVolumeInformationW`, no process); folder times are trusted on NTFS and ReFS only, and elsewhere (FAT32, exFAT, network shares, an unidentified volume) source discovery stamps each scanned folder by a hash of the listing it read (`list|…`), which the registry's check reads again. POSIX volumes keep folder times. Test: a folder's time put back after adding an archive is missed by time stamps and caught by listing stamps (`tests/installation-lifetime.test.ts`). The development machine's volumes are all NTFS, so the listing route runs only in the test.
- **PIPE-59:** `GradingLutHost.refresh` revalidates its registry (the shared one, or its private one over the test seam), the LUT endpoint calls it before answering on both hosts, and the LUT key passes that registry to `installationFingerprint`. Test: `tests/installation-lifetime.test.ts`.
- **PIPE-60:** both extraction routes store and serve JSON without `Header.ArchiveFileName` (`withoutArchiveFileName`, also applied to older cache files when read). The opt-in oracle `tests/wolvenkit-uncook-oracle.test.ts` compares `uncook -u -s` with `unbundle` + `convert s` on seven vanilla resources (a material instance, a mesh, an `.app`, an `.ent`, a morph target, a layer setup and a texture) per accepted WolvenKit version; with the export time set aside it passed for 9.0.1 and 8.17.4 on the reference install.
- **PIPE-61:** `uncookPatterns` splits a launch's paths so the escaped pattern (counted as `depotPathRegex` writes it) stays within `MAX_PATTERN_CHARS` and the whole command line (the CLI, every archive, the output folder, the flags, Windows quoting) within 32,767 characters; a batch's archives are capped at `MAX_ARCHIVE_CHARS`. Test: 60 long archive paths and 900 escaped paths (`tests/resolver-fetch-markers.test.ts`).
- **PIPE-62:** a clean `revalidate` vouches for the next acquire of that route within `CHECK_FRESH_MS` (250 ms), provided nothing reused the installation since that check began; an acquire after an acquire always checks. A POST now reads each stamp once. Test: `tests/installation-lifetime.test.ts`. The synchronous reopen (about 1 s) still blocks the event loop; moving discovery off the loop was not in scope.
- **PIPE-63:** precedence ambiguities are recorded for consumer reads (`load`, `provenance`, and each memoised model's resource and patch sources on every ask), never for prefetched reads; `ResourceGraph.collect` gathers one resolution's, the merged creator resource carries its own, and `resolveCharacter` reports those instead of everything the long-lived graph ever met. Test: a cold cache (prefetching a template) and a warm one report the same ambiguities, and a later V on the graph reports its own (`tests/resource-graph-retention.test.ts`).
- **PIPE-64:** an `.app`'s part `.ent`s are no longer prefetched; the resolver reads the requested appearance's parts together (one batch); patch sources are read only once the target is provided. Tests: only appearance `b`'s two parts are read, two at once; a patch of an absent target reads nothing (`tests/resource-graph-retention.test.ts`); the prefetch chain test reads parts together as the resolver does.
- **PIPE-66:** a registered custom creator file an archive provides but that could not be read is the gap `custom-cco-unreadable`, worded from the graph's `loadErrors`; one no archive provides stays `custom-cco-missing`. Test: `tests/resource-graph-retention.test.ts`.
- **PIPE-67:** `chunkInScene` counts only a missing mask as drawn; an empty flag list, `0` or a value of another type is not. The reference cache has no empty mask. Test: `tests/resource-graph-retention.test.ts`.
- **PREV-73:** a bake while the renderer's context is lost stays pending (baked after the restore), and a bake that failed because the context was lost (`isContextLost`, or the draws' `CONTEXT_LOST_WEBGL`) is re-armed by `contextRestored`; any other failure stands. Test: `tests/layered-material.test.ts`.
- **PREV-74:** the scene publishes the placed V's limits whenever they change after placement (`onBakeLimits`: a hidden slot shown and baked, a re-bake after a context restore), the device words them per shown slot as `show` does (`onLimits`), and the character-detail service replaces the shown slots' codes (`withSlotLimits`). After a restore the core eye's visibility is recomputed from what baked again. Hook in the claude/cc-panel file `character-detail-actions.ts`: the optional port member `onLimits` and one constructor line. Tests: `tests/detail-limit-updates.test.ts`, the restore and visibility wiring in `tests/render-scheduler.test.ts`.
- **PREV-75:** the bake asks for the framebuffer status once per target (the two accumulation targets and the kept maps) and for WebGL errors once per pass (after the clear, after all layers, after the resolve), not after every layer: a 12-layer stack makes 3 status and 4 error queries instead of 13 and 14. An error from any layer is still caught (the flag stays set until read). Test: `tests/layered-material.test.ts`.
- **PREV-76:** the mesh-wide normal's intensity, UV scale and bias are clamped to the layers' ranges. A bottom layer whose template could not be read is drawn as a neutral stand-in (mid grey, fully rough, not metallic, no maps) instead of being left out (which baked black at zero roughness), with the limit `layered-base` ("The base finish of some of your V's piercings or layered parts couldn't be read, so a plain grey stands in for it."). Tests: `tests/layered-material.test.ts`, `tests/character-material-adapters.test.ts`.
- **PREV-77:** bakes are sized per side (`layeredBakeExtent`): a domain wider than tall gets a wider target (an eyeball spanning three tiles no longer gets a square one), and the material and evidence carry the height. Style 09 black's ring strip (a UV domain 0.99 wide and 0.15 tall) now bakes at 1024 × 256 instead of 256², the density along U the square bake fell short of by about 2.6 times; the style's kept maps go from 2.0 to 4.2 MiB. The other half of the hypothesis was checked and kept as it was: the texel density divides the part's world area by its summed UV area, which is right for mirrored or overlapping UVs (each copy of a texel lies on the surface at that density); dividing by the covered area would size a mirrored part up by √2 for no visible gain. Test: a mirrored part gets its unmirrored half's bake (`tests/layered-material.test.ts`).
- **PREV-78:** the previous V is released after the new one has baked (`setCharacterDetails`), so a try can share a bake the previous V held; shared maps are credited to their longest-standing user still holding them, so the evidence sums to what the GPU keeps as users come and go. Tests: `tests/layered-material.test.ts`, the order in `tests/render-scheduler.test.ts`.
- **PREV-79:** the loader's texture ledger key includes the resource's `isGamma` flag (`textureLedgerKey`). Test: `tests/character-material-adapters.test.ts`.

## Fixed in claude/cleanup-layered

Findings PIPE-40 to PIPE-43, PREV-62 to PREV-68 and UI-48 to UI-51 from the `90b8602` layered material review, PREV-69 and UI-56 from the `fc36eae` studio light rig review, plus two additions asked for during the work: host/page version skew (seen live after a rebuild) and fast piercing tries (the reference installation took 7.6–10.1 s a try with everything cached, over 20 s the first time). Checked in the browser (`?verify=1`, reference MO2 profile, own port, disposable data) with the default V, the reference save, save B, style 09 black, style 12 gold and silver, style 14, a forced heart eye design, and switching reference ↔ B; measured against a build of `4036d07` on another port with the same data.

- **PIPE-40:** one creator-name rule, `isChoiceName` (1–127 characters, no control characters or `\ / < > "`), used by the record reader, the request parser, the host's choice list and the workspace. The record reader (`render-detail-6`) keeps its frame strict but leaves out a part, a choice, a colour or the applied override that breaks a rule, with one "Left out of the prepared details" note, and a slot left with nothing becomes unavailable with a plain line; parts beyond 96 are capped the same way. The host drops unusable creator names with a note (`slotChoices`), cuts layer CNames to the record's length, and writes the reader's own output (`parseCharacterDetail` before the identity), so what it writes always parses. Tests: `tests/render-detail-record.test.ts` (`piercings_(ccxl)`, a 200-character definition, a 130-character CName, 196 parts, a bad override; parse idempotence), `tests/piercing-resolver.test.ts` (host-side filtering with notes), `tests/character-detail-service.test.ts` (the written record equals its re-parse).
- **Version skew (added):** a record of a retired or newer schema throws `RenderDetailVersionError`; the host names the record schema it writes in every state and answers an unknown request version with `409 unsupported_version`; a host refusing a request this page's reader accepts counts as skew too. The device turns each into `DetailVersionSkewError`, the service publishes the typed notice `version-skew`, and the presentation words it ("XF Studio was updated while it was running. Restart it to see your V's …"). Tests: `tests/character-switch.test.ts` (older host, newer host, newer record, refused request, plain failure without a notice, the host's 409), `tests/detail-limits.test.ts` (wording from the code).
- **PIPE-41:** the resolver reads each render chunk's `renderMask` (`chunkInScene`) and the planner keeps only `MCF_RenderInScene` chunks; `LAYERED_SLOTS` is gone, so a layered chunk draws on any slot that brings it. An explicit `lodMask` of 0 now means no level of detail (it was read as the top level); the reference save's CCXL hair brings a one-triangle proxy with in-scene chunks at `lodMask` 0, which stays out, and the hair stays shown. A part of a slot that can't be served no longer blanks the slot when others drew ("Some of your V's hair couldn't be read…"). Tests: `tests/character-detail-plan.test.ts` (shadow-only proxy out, in-scene layered chunk drawn on the hair slot, `lodMask` 0 out), `tests/character-detail-service.test.ts` (partial slot). `knowledge/head-cc-rendering.md` §4 and Q3 updated.
- **PIPE-42:** a tried choice is a switcher choice (its `localizedName`, the creator's choice identity) plus a definition of the option it drives (its link controller). `applyChoiceOverride` drops everything the slot's switchers can reach and derives the replacement with `descriptorsFromSwitcher` (the shared R5 rules rooted at the switcher, so every target and each linked follower's colour index resolve like the creator); `detailSlotOf` puts a switcher target on another creator slot into the switcher's detail slot. Request `xfs/character-request-3`. Test: several targets, a linked follower on its own slot, a replaced follower, in `tests/piercing-resolver.test.ts`.
- **PIPE-43:** `readSetup`/`readTemplate` clamp every number to `LAYER_RANGES` and cap override tables at 4096 entries (the largest vanilla table, a paint template's colours, holds 906; a first cap of 256 turned style 09's black plastic white in the browser check and was raised); `layerBakeParameters` clamps again. Each record has a decoded-texel budget (256 Mi texels; the host leaves textures beyond it out with a note, the loader refuses a part over it). Tests: `tests/layered-material.test.ts` (1e38 tiles, NaN, 50 layers, 5000-entry tables, a vanilla-sized table read whole).
- **PREV-62:** on `webglcontextrestored` the scene forgets the renderer's shared bakes, resets every layered handle (`contextRestored`: hidden, pending) and bakes again. Tests: a real loss and restore beside the plate's probe (`tests/webgl-plate-composite.test.ts`: without the restore path the part draws differently, with it within 0.2 %), the handle in `tests/layered-material.test.ts`, the wiring in `tests/render-scheduler.test.ts`.
- **PREV-63:** bakes are sized by texel density on the part's own surface (20 texels a millimetre from its triangles' world and UV areas; 256–2048), accumulate in RGBA16F, keep two packed 8-bit maps (sRGB colour with roughness in alpha, normal with metalness in alpha; the lit material reads the alphas), share one bake between chunks of identical stack, domain and size, and skip hidden slots until shown. Save B, style 09 black (5 layered chunks): kept maps 160 MiB (5 × 1024², three RGBA16F maps with mips) → 2.0 MiB (5 × 256², 3 distinct bakes, 0.67 MiB each); transient accumulation per bake 96 MiB → 3 MiB. With the heart eye design added: 288 MiB → 44.7 MiB (the eyeball stays 2048², as its UVs span several tiles: 128 → 42.7 MiB). `renderer.info.memory` for that V: 102 → 88 textures, 25 → 21 geometries. Parity: within one byte of the CPU reference for the constant stack and for a non-constant stack (tile 2, an offset, a mask differing by row and column, an sRGB colour map) at all 256 texels; a flat baked part lit beside a plain standard material of the same surface agrees within 6 %, and the same maps read the default way (roughness G, metalness B) differ by more than 25 % (this caught the packed-channel shader injection replacing text that is still an `#include` at compile time). Tests: `tests/webgl-display.test.ts`, `tests/layered-material.test.ts`, `tests/character-material-adapters.test.ts` (a 4 mm part bakes at 256²).
- **PREV-64:** researched in the resolver's component JSON: every vanilla and framework piercing component binds `parentTransform` (`entHardTransformBinding`) and `skinning` (`entSkinningBinding`) to the entity's `root` animated component, with no slot, so the data names no head bone to follow [resource]. The rigid part stays bound in place and now says so: limit `rigid-part` ("A piercing part stays in place while your V's head moves in the idle…"); how the engine moves an unskinned mesh in a skinned component is a hypothesis for an in-game check. Test: `tests/character-detail-geometry.test.ts`.
- **PREV-65:** after each bake draw the handle checks the program diagnostics, the framebuffer status and `gl.getError`; any failure reports `failed` with its reason and the slot's limit code. Test: a renderer stand-in in `tests/layered-material.test.ts` (unlinkable program, incomplete framebuffer, WebGL error, no half-float target).
- **PREV-66:** the clear, layer and resolve programs and the quad are made once per renderer (`bakeKit`). Test: three bakes draw with the same three materials.
- **PREV-67:** `stackProblems`: the `layered-mask` limit only for a mask the host could not read (no layer, or a layer inside the mask's count without its image); a layer whose `.mltemplate` could not be read is recorded as `templateUnreadable`, left out of the bake and noted. Tests: `tests/layered-material.test.ts`, `tests/character-material-adapters.test.ts`.
- **PREV-68 (widened):** the host keeps a `CharacterPreparationCache` per installation fingerprint (which includes the installation registry's generation). It is layered on the registry's long-lived `Installation` from `claude/prepare-speed` (the opened archives, the graph and the memoised merged creator resource stay the registry's), and holds only what preparations derive from it: each resolved descriptor, interpreted templates, setups, profiles and gradients, exports, and each served component by its plan; a reopened installation (new archives, `depot`) resets it, while the registry's per-acquire view objects and graph replacements do not. Going back to the V's own is also shown as on its way ("Going back to your V's own piercings…"). so a try re-plans from the resolved V and resolves and exports only the tried slot; it works with a long-lived `Installation` passed in. Exported files are hashed once per path, size and time. The browser keeps the V visible and interactive during a try (status stays `ready` with `trying`; the character panel says "Trying Style 14…"; the full "Preparing" line is for a V's first preparation), and the loader takes over every unchanged component (objects, materials, bakes, textures through a shared ledger) so only the changed slot loads. Measured on the reference profile: a cached try 0.27 s on the host and 0.3–0.7 s in the browser end to end (was 7.6–10.1 s); the first try of the framework's style 12, which needs six WolvenKit reads, 38.9 s (6.1 s resolving the framework's 128-slot appearance cold, 32.7 s exporting); a new V on the same installation reused 9 of 10 parts. Tests: `tests/piercing-resolver.test.ts` (one open, only the tried part exported, back to the V's own exports nothing and gives the same record), `tests/character-detail-geometry.test.ts` (reuse and ownership), `tests/character-switch.test.ts` (no overlay phase during a try).
- **UI-48:** `CharacterDetailActions` owns the tried choice with the typed `character.tryChoice` action (family `characterDetails`, separate from the CC work's `characterContext` family that shares the `character.*` namespace, as the action catalogue documents; its own capability check against the V's choices); the presentation dispatches it and reads `previewState().character`; `preview.setPiercingPreview`, the preview port's piercing callbacks and `PreviewConfig.piercingStyle` are gone, and the workspace stores the service's value. Tests: `tests/studio-application.test.ts`, `tests/studio-registry.test.ts` (golden updated), `tests/character-switch.test.ts`.
- **UI-49:** the host gives one label per style and per colour (`styleLabel`, shared with the slot label); the `1024` sentinel and the device's wording are gone.
- **UI-50:** `projects/xf-studio/README.md` and the public site describe piercings, brows, lashes and hair as rendered from the player's own files; the localhost host refuses `/assets/prc/` and `/assets/piercings/` (the desktop host serves only the derived core files), and the boundary test checks it. The ignored payloads in the main checkout were left in place.
- **UI-51:** the workspace keeps the tried style only when both names pass `isChoiceName`; the service reads it with the host's rules (`validOverride`), clears it once the V's choices show it is not offered (never re-sent), and clears it when another V is loaded: a tried style belongs to the V it was tried on (decision recorded in the service's header). Tests: `tests/character-switch.test.ts`.
- **PREV-69:** the studio rig's `setStudioLights` is part of `createBrowserScenePreviewPorts`; the head attachment no longer spreads it in. Test: `tests/trusted-preview-services.test.ts` (the port carries it, the attachment has no spread).
- **UI-56:** studio controls under the creator preset are refused as `incompatible_mode`. Test: `tests/studio-lighting.test.ts`.
- **Test gaps:** the GPU parity test with non-constant maps, the real exporter's `masks()` path (`tests/character-detail-service.test.ts`: layer folder, cache, by hash), and piercing identifiers in the boundary regex (`piercings_\d\d`, `earring__\d\d`, `i0_000_pwa`, `i1_000_pwa`).

## Fixed in claude/cleanup-render3

Findings PREV-57 to PREV-61 from the `4bf688a` plate blend review. Measurements: headless Chrome (ANGLE, D3D11, RTX 4070); in-app captures in `?verify=1` with disposable data, 732×788 viewport, fixed face camera, idle off, experiment 016's Board 1 and a Shimmer-over-Glossy look (Glossy 50 % with Shimmer fine 50 % over it on one lid, Shimmer fine 50 % alone on the other), both lighting presets, before and after. The default V's details were still loading in that workspace, so the plate lit with the standard light over the core head. Private captures stay in the ignored evidence tree.

- **PREV-57:** the composite pass moved to `src/plate-composite.ts` and now follows the export's faceted chain. The layers accumulate premultiplied into an accumulation target; a resolve pass writes the merged decal as `encodeSurface` stores it: the merged normal per texel, rounded to the export's bytes (flat where nothing covers), and that normal's squared length. The GPU builds that target's mips once (box means, which are `facetedMipChain`'s), and one draw per level writes the export's roughness chain (level 0 unwidened, lower levels (r⁴ + E[|n|²] − |E[n]|²)^¼, zero outside coverage) into a single-channel target the plate samples as the game samples the exported map. The normals are two-channel. Test: `tests/webgl-plate-composite.test.ts` (Shimmer at 30–100 % over Glossy at 50 % on a 16-texel grid through the stack's own layer materials, against `facetedMipChain` of the compiler's bytes for the same texels): half-float within 0.56 of a byte step for roughness and 0.51–0.57 for coverage at levels 0 and 2, 1.12 for the normal at level 0 (half-float sums rounding a tie one byte away) and 0.55 at level 2; at the Shimmer 50 % over Glossy 50 % texel the export's roughness is 0.353 and the composite's 0.354, where the earlier composite gave 0.444 at level 0. In the app, Board 1 (flat, no facets) is unchanged (max 3 levels, no pixel over 2); in the Shimmer-over-Glossy look 0.07 % (studio) and 0.08 % (creator) of the frame changes, by up to 10 and 22 levels, all in the Shimmer sparkle over Glossy, whose facets are no longer dulled at level 0; the Shimmer alone changes only faintly (bilinear filtering no longer widens between texel centres). GPU memory for the 469 × 153 half-float composite: 2.30 → 3.54 MB (the accumulation target and the roughness chain).
- **PREV-58:** `makeup.contextRestored()` marks the blend dirty. The rest of the frame changed because the studio environment's prefiltered target came back empty (every surface lost its image-based light): `src/studio-environment.ts` owns it and `restore()` prefilters again and copies into the same texture, so the creator preset's saved studio state keeps working. `scene.ts` calls both on `webglcontextrestored` (already a render trigger). Tests: a real context loss and restore in `tests/webgl-plate-composite.test.ts` (without the hooks the plate and the environment-lit skin read black; with them both return to their pixels exactly), `tests/plate-blend.test.ts` (the next frame redraws the composite), and the wiring in `tests/render-scheduler.test.ts`.
- **PREV-59:** either `EXT_color_buffer_float` or `EXT_color_buffer_half_float` makes half float renderable (Three.js enables both; Chrome offers the latter alone on some GPUs), so the linear path, the composite and the prefiltered environment use either. With neither, the creator preset renders into an 8-bit sRGB target (the GPU encodes on write and blends in linear light; values clip at one before the grade) and the studio environment becomes a light probe of `RoomEnvironment`'s spherical harmonics (`ROOM_ENVIRONMENT_SH`, recomputed on the GPU by the test; its diffuse light is 7.5 % below the prefiltered room's on white matte), so neither preset goes dark and no framebuffer is incomplete. The darkened stage was the prefilter's half-float target. The preset stays offered: it works. Tests: `tests/webgl-display.test.ts` reruns its page with both extensions hidden (no WebGL or shader errors; creator decal blends within one level of the half-float target's), `tests/lighting-preset.test.ts` (target type and colour space per extension set). In the app with both hidden, neither preset is dark: the creator frames differ from the half-float build's by 2–9 % mean luminance, within the run-to-run spread of when the colour grade arrives in a fresh workspace (two runs of the old build differed by 8 % on the same look), so the probe's measurement is the controlled one; the studio stage is 11 % lower in mean luminance than the linear path with the prefiltered room (drawn straight to the canvas, no sharp reflections).
- **PREV-60:** a later skin of the same plate is copied into the existing underlay attributes (`copyArray`, one upload each); a different vertex count frees the geometry's GPU buffers before replacing them; no skin leaves them in place, unread. Test: `tests/plate-blend.test.ts`.
- **PREV-61:** only the merged target asks for mips, so the renderer generates them once, after the resolve; the accumulation target has none and the roughness chain is drawn level by level. Mip generations per drawn frame during a one-layer opacity edit: Board 1 (6 layers) 19 → 4, the Shimmer-over-Glossy look (3 layers) 10 → 4 (3 for the composite, 1 for the edited mask's upload); 12 layers in the GPU test: 3 per update (was 36).
- **Tests the review asked for:** the merged slots equal `planPresetExport`'s included layers across seven presets mixing flat finishes, game-matched and earlier Glossy and Shimmer, Glitter, one- and two-pigment and earlier Colour-shifting, disabled and opacity-0 layers (`tests/plate-blend.test.ts`); the 8-bit composite path against the export (within 2 byte steps at levels 0 and 2). A V switch re-reading the skin under the plate stays covered by the stack's own test (a new underlay source is read once on the next frame, with its skin light) and the source check in `tests/render-scheduler.test.ts`; a behavioural test through `setCharacterDetails` needs the scene split (UI-11).

## Fixed in claude/cleanup-platform2

Findings CORE-38 to CORE-44 from the platform steps 3–4 review at `4bf688a`. Details, decisions and measurements are in the [design's cleanup after step 4](feature-module-platform.md#cleanup-after-step-4). The standard stored form of every fixture, the look-history parity golden, the registry golden and library bytes are unchanged; the 0.1.0-alpha.1 reader tests pass.

- **CORE-38:** Export collection and Export compiler plan save first only when the library takes the collection (`writeMinimal` gives `xfas/collection-1`); otherwise the draft snapshot is exported unsaved, and each path's message says plainly whether it was saved and why not. Test: `tests/alpha-library.test.ts` with the real `CollectionLibrary` (Save refused; both exports succeed, the collection file is `xfs/collection-2` with the hair part, the library and the draft's revision are unchanged; a storable collection is saved as revision 1 and exported as collection-1). It failed before the fix with the old message.
- **CORE-39:** `PartRegistry.writeMemory(memory, { lookLevel })`, threaded through `serializeWorkspace` and `writeCollectionWorkspace`; `WorkspacePlan.lookLevel`; `fitWorkspace` tries the look-level standard plan after the whole-part one and trims in that form (unless it is not smaller). `storesLookLevelHistory` lets the desktop store write a look-level workspace back in that form. Reviewer's heavy look at 2,000,000 units: 4 → 80 steps kept. Tests: `tests/look-history.test.ts` (standard → look-level → trimming order with both forms restoring the same histories; a 32-layer look whose whole-part floor does not fit keeps all 80 steps, and the pre-step-4 reader refuses that form as newer data; the reload gate now derives its budget from the look-level floor and checks that whole parts could not hold it), `tests/workspace-persistence.test.ts` (16 presets store look-level with nothing trimmed and status `saved`; a tighter budget trims and says so), `desktop/tests/workspace-store.test.ts` (a look-level post is written back look-level, less than half the whole-part size, with the same histories).
- **CORE-40:** `LookHistory.fromData` maps each stored chunk to its local address once and retains it for later references. Heavy look: `fromData` 24 → 1.8 ms, restore 24 → 2.8 ms, preset switch 35 → 13 ms.
- **CORE-41:** `fitWorkspace(state, model, budget, from)` gallops each stage from the previous fit's value; `WorkspacePersistence` passes its last plan. The desktop store parses the previous file once per save (not at all when it is the text it last wrote or checked) and the new text once, and reads the previous schema from that parse. Heavy look: over-budget autosave 57 → 37 ms (the remaining cost is mostly the whole-part standard encoding, kept so the older-build form is used whenever it fits); 32-layer large fixture: autosave 22.7 → 6.1 ms, desktop save 164 → 67 ms. Tests: a hinted search equals the cold one for every pairing of seven budgets; the desktop test counts `JSON.parse` calls per text.
- **CORE-42:** a labelled checkpoint equal to the top step relabels that step; a transaction without its own step names the top step at commit when its change is kept, and `cancel()` restores the start through `TransactionHost.revert(undefined)` without taking the step off (`AuthoringHistory.revertTransaction`). Tests: the platform transaction test (de-duplicated gesture cancelled, control committed and returned to start) and an app-level session: a gesture moved and back, a second gesture cancelled with Escape (the step stays), an opacity edit (undone as "Opacity"), Undo, and an Escape-cancelled gesture that leaves Redo available.
- **CORE-43:** the scan covers `export … from`, bare `import "…"`, dynamic `import("…")` and inline `import("…").T`, with its own test; it revealed no violation, so no exception was recorded.
- **CORE-44:** `RecipeActions.dispatch` and `capability` removed (their tests use `app.dispatch` or the registered apply); `applyRecipeAction` `field.add` and `editLayers` add/duplicate refuse a missing ID; `editPresets` takes `newId` in add and copy commands, filled by `CollectionSession` from its host's ID source, and capability refuses an ID in use. Tests: `tests/application-actions.test.ts` (deterministic `editPresets`, the session's ID source, the refusals), `tests/layer-stack.test.ts`.

## Fixed in claude/multilayer-piercings

- **UI-02 (completed):** piercings render from the resolver: the `piercings` slot (creator slot `piercings_color`, face groups), each drawing component with its chunk mask, `multilayered.mt` chunks through the new layered adapter, and any framework that replaces a style's `.app` by archive precedence alone (the reference profile's jewellery framework shows its filled slots). Removed: `src/piercing-preview.ts`, `src/piercing-palette.ts`, `tools/intake_prc.ts`, `tools/intake_piercings.ts`, their tests, the scene's manifest loading, `prcError`/`prcAvailable`/`piercingError` in the status and the PRC note in the preview panel, and `matchedPiercing`. The piercing style and colour controls now list the creator's options from the record and ask the host for the V with the tried choice. Tests: `tests/piercing-resolver.test.ts` (vanilla, framework replacement, Off, override, choices, requests), `tests/layered-material.test.ts` (bake maths), `tests/render-detail-record.test.ts` (v5 schema; the boundary test now covers piercings with no exception), `tests/character-switch.test.ts` (tried style keeps the V on screen), `tests/webgl-display.test.ts` (the bake on a real GPU against its CPU reference).
- **PIPE-11 (piercing part):** see the table.

## Fixed in claude/platform-step4

Step 4 of the [feature-module platform](feature-module-platform.md#step-4-status). Details, decisions and measurements are in the design's step 4 status.

- **CORE-02 (completed):** one Undo history per look (`LookHistory`: part and look steps, a content-addressed chunk store, Redo), in memory in every look's memory, so preset switches, draft copies and autosave captures no longer repeat each step's whole recipe (large fixture: in-memory draft 2,250,819 → 551,846 code units, draft snapshot 32 → 2.5 ms, preset switch 3.6 → 1.0 ms). `fitWorkspace` keeps the standard policy and trims in the design's order, keeping at least 10 steps of the selected look unless the looks alone leave no room (the old smallest level kept none); a quota refusal fits a smaller budget. The stored form is unchanged for every history this build makes (byte-identical on every fixture; the previous build's reader, vendored in `tests/fixtures/pre-step4/`, reads it), and a history only the look-level form holds opens older builds read-only. Tests: `tests/look-history.test.ts` (chunk store and collisions, part and look steps, limit, trimming, stored forms and the previous reader, damaged steps, the platform transaction, `fitWorkspace` order, the reload gate, the size benchmark), `tests/look-history-parity.test.ts` (golden from `0885ba6`: every fixture's histories, stored form and a scripted session).
- **CORE-11 (part):** gesture and form-control transactions share the platform's `HistoryTransaction` with their rules as policies; spec `label` and the gestures' `label` name Undo steps from the registry.
- **CORE-36:** library requests and file workflows are registered asynchronous families (`LIBRARY_FAMILY`, `FILES_FAMILY`; `AsyncSystemFamily`, `Registry.routeAsync`), bound by `StudioApplication` (`StudioOwnerRequests`) and routed through the registry by `requestCapability`/`execute` and `fileCapability`/`executeFile`, which the collection application and the presentation's `files` port now use; the request, gesture and file catalogues derive from the registry (the golden registry snapshot is unchanged). Gesture proposals are eye makeup's `gestures.descriptors`.
- **Not done (recorded):** the per-look "not editable in this version" state moves to step 5 with the presentation; the Undo policy keeps the name `recipe` until the port changes.

## Fixed in claude/cleanup-render2

Findings PREV-50 to PREV-56 from the `bb2cc22` rendering review. Measurements: headless Chrome `?verify=1` (ANGLE, D3D11, RTX 4070), 510×888 viewport, face camera, idle off; the default V, the reference save, save B and the reference save again, both lighting presets, dark and light themes.

- **PREV-50:** `src/linear-display.ts` replaces `creator-display.ts` and serves both presets: the scene renders into one half-float 4× MSAA target. For the Studio stage, one coverage pass sets alpha to one wherever a surface wrote depth, Three draws the stage backdrop on the canvas as before, and one output pass applies the renderer's own ACES tone mapping at the stage exposure and sRGB encoding, laid over the backdrop by coverage. The canvas has no MSAA or depth; without `EXT_color_buffer_float` the Studio stage draws straight to a multisampled canvas as before (`frameTiming().display.path` says which). Look parity against the previous build on the default V: forehead and cheek patches max 1 level (mean 0.05 to 0.09), the backdrop byte-identical, the creator preset identical for all four Vs (one pixel off by one level). The differences are the intended ones: blended passes (face decals, brows, eye shell, makeup-plate edges) now match the linear solve, and sub-pixel coverage (hair, lashes) resolves in linear light, so thin dark strands and the reference save's blush and lipstick read lighter. Idle: no frame drawn in 4 s with nothing changing. Test: `tests/webgl-display.test.ts` (dark liner over skin 98,70,57 against the solve's 97,70,57, where the old path showed 41,32,28; lipstick 189,127,115 exact, old 172,94,94; opaque surface within 1 level; backdrop identical) and `tests/lighting-preset.test.ts` (pass order and restored renderer state).
- **PREV-51:** the face-decal adapter returns how the skin under it was read (`decalSurface`), the loader keeps it on the decal entry, and the scene evidence reads it there; the scene's `WeakMap` is gone. Tests: the adapter keeps the evidence, and the evidence reader takes it from the entry.
- **PREV-52:** the underlay carries metalness (`xfsUnderMetalness`: the resolved skin's Roughness map G, zero on the core head and the flat fallback), and the shader mixes metalness from it by the surface share, as `forwardSurface` does. Tests: placement sampling, shader text.
- **PREV-53:** the host serves only the drawn chunks (`glb.ts` `keepGlbMeshes`: nodes kept, sparse POSITION/NORMAL morph deltas, no TANGENT deltas, target names unchanged), keyed in the store by the export's hash, the chunk list and a version (`storeChunkGeometry`); a file it can't read is served whole with a note. The loader parses each file once and clones objects and skeletons for a second component. Budget on the same Vs: default 78.6 → 58.2 MiB, reference 138.1 → 99.9, save B 86.8 → 63.4, and the scar and tattoo test V 151.2 → 72.9 MiB, its face slot 71.3 → 13.1 MiB (scar mesh 43.5 → 0.6 MiB). With the new files the creator-preset renders were identical to before (one pixel differed by one level). Tests: `tests/character-detail-geometry.test.ts`.
- **PREV-54:** `render-templates.ts` lists each template's `required` inputs (`decalRequired` for the face family); a chunk is dropped only when one of those is unreadable, and an optional or recorded-for-later input earns a note. Test: an eye without its `Normal` still draws; without `Albedo` the eyeball chunk is left out.
- **PREV-55:** the by-hash fallback runs only when the archive index lists the texture, and up to `BY_HASH_CONCURRENCY` (4) launches at a time, each into its own folder; the first failure stops new launches and surfaces after the others finish. Test with a fake WolvenKit: six launches peak at four and take two rounds; an unreadable index launches nothing.
- **PREV-56:** `tests/webgl-display.test.ts` bundles `tests/webgl-probe-page.ts` and runs it in headless Chrome through `tools/cdp.ts` (about 4 s; skipped without Chrome, failing under `XFS_REQUIRE_ORACLES=1`): every face-decal variant (plain, double diffuse, gradient recolour; with and without underlay and skin light), the brows, skin, eyeball, shell and display passes compile and draw with no shader error (51 programs).
- **Design note (not a finding):** the Studio's own makeup plate blended in linear light, so its soft edges read lighter than the game's square-root blend. Done in claude/plate-blend: `src/plate-blend.ts` gives every exportable layer the face decals' per-vertex skin underlay and forward solve, plus a UV-space target of the blended layers below it, so the preview lands on the export's merged decal over the skin (see [head CC rendering §3](../../knowledge/head-cc-rendering.md#3-the-head-decal-family)).

## Fixed in claude/cleanup-platform

Findings of the platform steps 1–3 review. Details, decisions and measurements are in the [design's cleanup section](feature-module-platform.md#cleanup-after-steps-13).

- **CORE-27:** `NewerDataError` (`platform/api/document.ts`) is thrown for a part schema or layer model this build does not know, wherever it sits (current draft, recovery drafts, removed presets, loose look, Undo histories), and is never treated as damage. `loadWorkspace` opens such a workspace read-only (`NewerPolicy` `omit`: its current draft without the newer entries, `writable: false`, a plain protected message), so nothing is written back. The desktop store loads a workspace whose current draft is readable, refuses to replace one holding newer data and never sets that draft aside; only an unreadable current draft is set aside by Start fresh. `writeMemory` writes each Undo history in the oldest part schema that holds every entry, and workspace looks use `minimalLook`; the step-2 code (`2dced39`) read the small, loose and large fixtures written this way as writable with the same content. Tests: `tests/newer-data.test.ts` (five placements, a newer current draft, damage still dropped, desktop load/save/Start fresh, minimal histories, a part-1-only registry as the step-2 read path).
- **CORE-28:** `lastPackageIsCurrent()` compares `[content, collection ID, saved revision, selected look, live revision]` with the key recorded at the request; `CollectionSession.snapshot()` no longer stashes into the draft. Test: `tests/package-freshness.test.ts` (large fixture: 75.2 ms before, 0.011 ms after, per repaint; stale after an edit, a preset switch and a rename; current after a new Check; the draft is unchanged by reads).
- **CORE-29:** `DocumentModel` (`collection-workspace.ts`) and `StudioComposition` (`trusted-authoring-core.ts`) are passed in by `studio-startup`, `server.ts` and `desktop/server.ts`; nothing else imports `compose/`. Eye makeup's types moved to `src/eye-makeup-model.ts`, the history and collection family types to their services, `StudioOwnerActions` to `StudioApplication`. The package pipeline reads eye-makeup parts with `parseEyeMakeupPart`. Boundary tests: only `compose/` and roots import `compose/`, and no module outside them reaches `features/` or `compose/` transitively.
- **CORE-30:** the design doc states the alpha's exact limit; the library refuses a save that would write `xfs/collection-2` (`COLLECTION_2_LIBRARY_MESSAGE`), and the desktop `.bak` is refreshed whenever a version-1 file is replaced. Tests: `tests/alpha-library.test.ts` with the release's `list()` vendored from `60a60e9` (`tests/fixtures/alpha-0.1.0/collection-list.ts`): it lists everything this build saves, and one collection-2 row makes it fail; the desktop `.bak` refresh test.
- **CORE-31:** form controls apply through the registered `apply` and the eye-makeup port; gesture frames through the module's registered `gestures` provider (`GestureProvider`, `applyRecipeGesture`) and the port. `AuthoringLayerActions` is deleted and its tests dispatch through `app.dispatch`. Test: `tests/registered-editing.test.ts` counts registered apply and gesture calls from dispatch, controls and gestures.
- **CORE-32:** eye-makeup dispatch records Undo by Undo policy; context-menu input options use `undoPolicyOf` for their variant. Tests: a content action registered with policy `none` records nothing; every option's policy equals its variant's.
- **CORE-33:** `field.add` carries `fieldId` and layer `add`/`duplicate` carry `newId`; the spec's `assignIds` fills them from the host's ID source (the port's `newId`), the registered apply refuses an action without them, and capability refuses an ID in use. The purity test covers all 29 kinds, deep-frozen, applied twice with identical results.
- **CORE-34:** package and build-plan capability need a look with eye makeup; the eye-makeup view lists looks without eye makeup and other features' parts in `omitted`, reported by Check, Build and the manifest (a new `part` omission kind; looks count in the original preset count; the packaged copy and hash never carry the list). Pipeline guide and its authoring diagram updated (visual review recorded). Test: `tests/package-omitted-looks.test.ts`.
- **CORE-35:** each stored part is parsed once on restore (`part()`/`canonicalParts` use in-memory bodies), sessions copy instead of re-parsing (a JSON copy), preset edits validate only the looks, and SQLite `save()` reads the previous revision number only. Large fixture: `CollectionService` construction 70 → 27 ms (604 parses → 0), draft snapshot 150 → 59 ms, stored restore 41 → 38 ms (169 → 137 parses), unchanged `save()` 11.6 → 8.1 ms.
- **CORE-36:** recorded as a design gap in [§4](feature-module-platform.md#4-routing-and-the-registry-derived-catalogue-core-03-core-08): library requests and file workflows are asynchronous and gesture proposals run inside a gesture session, so they join as families with the look history (step 4); gesture frames already apply through eye makeup's registered provider.
- **CORE-37:** `withPreferences` gives the workspace-1 fixtures schema'd UI preferences with a dock layout and tour progress (`tests/workspace-preferences.test.ts`: restore, every budget level and a second restore keep them); the CORE-27/30 tests above; the purity test covers 29 kinds; the browser-globals check catches `globalThis`, `sessionStorage`, `navigator` and `fetch(`, with its own test.
- **CORE-11:** closed (the routing tests landed in step 1). **CORE-05:** completed with CORE-28.

## Fixed in claude/cleanup-polish

Low findings from the `84cb39d` review, plus two render-review nits.

- **PREV-48:** `lighting-preset-stage.ts` notifies whenever the cube changes (`file` differs), not only when the source description does, so the scene's lighting subscription asks for a frame. Test in `tests/lighting-preset.test.ts`: an updated LUT file with an identical source notifies once; the same cube again stays quiet.
- **UI-45:** `render-scheduler.ts` keeps an in-frame flag: a request made while a frame runs (the controls' `change` inside `controls.update()`) only marks the scheduler dirty, and no longer resets `previous`, so the next dt covers the whole interval. Test: a frame that requests another 5 ms in has each dt equal to the time between frame starts (it failed before). Measured in headless Chrome with the idle playing and three orbit drags per window: the idle advanced at 0.966-0.976 × wall time before, 1.0015-1.0021 × after.
- **UI-46:** `src/device-pixel-ratio.ts` (`viewportPixelRatio`, capped at 2; `watchDevicePixelRatio`) re-arms a `(resolution: Ndppx)` query after each change. `scene.ts` applies the new ratio, resizes and invalidates, and releases the watch with the scene. Tests in `tests/device-pixel-ratio.test.ts`. Not confirmed in a browser: headless Chrome's device-metrics emulation fires no media-query `change` events (a plain listener stayed silent too).
- **UI-47:** `surface-editor.ts`'s `rehover` ends with `viewer.requestRender?.()`: the release is stopped before the viewport's own pointer trigger sees it. Test: `tests/surface-rehover.test.ts` (after a shape drag released over the interior, the hover highlight is gone without another move).
- **PREV-51, PREV-52, PREV-53, PREV-54, PREV-55, PREV-56:** Fixed in claude/cleanup-render2 (see below).
- **UI-41:** F6 and Shift+F6 include the visible `.guidance-callout` regions (tour card, onboarding offer), after the status bar; focus lands on the callout's primary action, never its close button. Checked in headless Chrome `?verify=1`: F6 from the status bar reached the tour card's Next, and the offer's "Show me around".
- **UI-42:** the controller reads the step index before awaiting `runCommand`, so `then: "next"` cannot advance a second time after a paint moved the tour on.
- **UI-43:** `guidance.startTour` ends a running tour through `end(…, "skipped")` first, so the outcome is recorded and readers see it end. The controller keys the shown step by tour and index, so a new tour starting at step 1 is announced. Test in `tests/guidance.test.ts`. In `?verify=1`, starting What's new on onboarding's second step recorded onboarding as skipped and announced What's new step 1 of 4.
- **UI-44:** the Help topic and the onboarding finish step carry tokens (`{{finishes.export}}`, `{{finishes.mod}}`) that `guidance/finish-text.ts` fills from the catalogue the port publishes (`rt.finishes`, from `authoring.finishCatalogue()`), through `helpTopicsFor` and `toursFor`; the presentation's value-import allowlist did not grow. Test: the data names no finish, the filled text has no token left, each finish appears once in the sentence of its export status, and moving Glitter to experimental changes the words.
- **Render-review nits:** `detail-limits.ts`'s comment no longer claims no device writes user sentences: it says the codes cover partial drawing only, and that the character-detail loader still words its load failures (the wording itself stays in the loader for now). The name-based "every scene mutator is wrapped" source test documents its limits (name prefixes, layout-dependent key reading, wrapping versus effect).

## Fixed in claude/platform-step3

- **CORE-09:** step 3 of the [feature-module platform](feature-module-platform.md#step-3-status). Eye makeup's part is `xfs/eye-makeup-part-2`, the in-memory recipe with no recipe-level schema; each layer's Glitter and game-matched optics models are validated by their own IDs in the model registry, which also carries each model's oldest recipe schema. `selectGlitterModel`, `applyRecipeAction`, gestures and shape transforms no longer touch a schema, and `requiredRecipeSchema` (with its "never go below" pinning) is gone: writers derive the oldest schema that holds the layers, for "Export recipe", library rows, collection-1 files and part-1 bodies, so 0.1.0-alpha.1 reads more files than before. Recipe files keep their per-schema gates (checked exhaustively against the step-2 rule). A newer part schema or unknown layer model stays refused on read (decision recorded in the design). Parity with the step-2 code: masks, preview keys, export plans and compiled maps of every recipe schema and 27 Glitter edits beside Glossy; collection and workspace-1 fixtures; no new revisions for unchanged presets; the four-preset Build's 47 compared files byte-identical from the original, minimal and part-2 inputs.

## Fixed in claude/cleanup-hosts2

- **PREV-45:** `GradingLutHost` (`src/grading-lut-host.ts`) keeps the installation it opened for a fingerprint, so a retry runs no second source discovery; the creator environment is read again through a fresh `ResourceGraph` over the same indexes only when it failed. A transient failure (time limit, missing .NET, a process that could not start, a disk error) is retried on a request after `GRADING_LUT_RETRY_MS`, doubling each time, at most `GRADING_LUT_MAX_RETRIES` (5) times; meanwhile the previous answer stands and the note says when XF Studio tries again. A WolvenKit exit code, an unhandled exception in its log, or output that cannot be read or decoded is lasting for that decoded-LUT key (archive fingerprint, entry, decoder, WolvenKit identity) for the host session. A damaged cached cube is a miss; the cube and its key are written atomically. Tests in `tests/grading-lut.test.ts` (retry timing, cap, one discovery, lasting exit code, WolvenKit update, failed cache write).
- **PREV-46, PIPE-04 (rest):** `WolvenKitFetcher` computes `wolvenKitIdentityKey` once; cache files are `<hash>-<archive fingerprint>-<identity tag>.json`, and `.failed` markers (version 3) record the identity and count only for it. Existing JSON cache entries are extracted again once. Tests in `tests/resolver-fetcher-concurrency.test.ts`.
- **PREV-47:** `readArchiveIndex` (`src/resolver-host.ts`) writes the `.u64` cache through `writeFileAtomic` (now shared from `derived-cache.ts`) and reads it back only when its length is 8 × the file count the archive's index declares (`readRdarIndexCount` reads the header and the first 20 bytes of the index); otherwise it deletes the file and reads the archive again. Test in `tests/rdar-index-fs.test.ts`.
- **PREV-49:** a fetcher cache write that fails still answers with the extracted document (recorded as "not cached"), and a failed marker write is advisory. The LUT host's comments now say that a superseded preparation's WolvenKit run stops at once while discovery and a resource load already under way finish first.
- **PIPE-36:** the eye plate cache status records `routeKey` (`eyePlateRouteKey`: route settings, head choice, and size and time of the game's mod folder, its `modlist.txt`, the MO2 profile's `modlist.txt`, MO2's overwrite folder and the manual mod folder). `cachedPlateReach` requires the same route key, game folder, content fingerprint, recipe and the current window rule; both hosts pass the key. Without such a plate Check returns `plateUv: null` with `notes: ["Whether each look reaches the eye area is checked when you Build."]`, shown in the result card and the status message. A committed vanilla footprint was considered and rejected: it is every plate vertex UV and triangle of the CDPR head's eye area, verbatim game mesh data rather than asset-free metadata, and it would not describe a modded head. Tests in `tests/eye-plate.test.ts` and `tests/package-server.test.ts`.
- **PIPE-37:** `loadCached` and `cachedPlateReach` recompute the window from the recorded bounds and treat a mismatch as a miss (derived again once); a change to how the footprint is read from the mesh changes its schema. The resource builder's mismatch throws `PlateFootprintChangedError`, which the CLI reports as `package_plate_stale`; localhost `runLocalPackage` and desktop `runDesktopBuild` read the CLI's `XFS_PACKAGE_ERROR` code (`packageErrorCode`), `discardCachedPlate` that entry and build once more (once only). Tests in `tests/eye-plate.test.ts`, `tests/package-build-service.test.ts`, `tests/package-server.test.ts` and desktop `tests/build.test.ts`.
- **PIPE-38:** `mappingStats` counts `drawn` (map ≥ 1/255) and `authored` (reference ≥ 2/255) samples, and `checkMapping` refuses a map that is empty at the plate where the authored makeup reaches it, however faint. Test in `tests/plate-reach.test.ts` (at 2 % opacity an empty map's mean error is under the limit; it is now refused).
- **PIPE-39:** `mappingOffset` returns null below 24 authored-content samples or an edge strength (spread of the total error over the coarse shifts) below 8 coverage units. On lossless lid bakes a preset reaching 13 samples, and 0.5-1 % opacity, gave spurious 0.2-0.5 texel estimates; presets with at least 100 content samples and edge 24 estimate exactly 0. Tests in `tests/plate-reach.test.ts`.

## Fixed in claude/platform-step2

- **CORE-03 (data), CORE-05:** step 2 of the [feature-module platform](feature-module-platform.md#step-2-status). New `platform/api/document.ts` (part, editor and memory codecs, looks, `FeatureState`/`FeatureResult`), `platform/core/document.ts` (`PartRegistry`: collection-1/-2 reading, minimal writing, canonical comparison, per-feature memory), the eye-makeup part-1 codec and pure core in `features/eye-makeup/`, and `STUDIO_PARTS` in `compose/`. `CollectionSession`, `CollectionActions`, `CollectionService` and the SQLite store work on looks; `save()` and the service baselines compare canonically, which also fixes the step-1 store giving every migrated preset a new revision on its next save. The workspace is `xfs/workspace-2` (per-feature memory; workspace-1 read losslessly; desktop `.bak`). Library rows and exported files use `xfas/collection-1` whenever it holds the looks exactly, so 0.1.0-alpha.1 still reads them (checked by running that release's readers). CORE-05: `targetCapability`, `contextFor`/`contextQuery`, `requestCapability` and the Glitter-model preset lookup no longer clone or stash the draft (`targetCapability(preset)` 240 ms → 1 µs, `contextQuery(preset)` 1.56 s → 6 µs on a 6 × 16-layer draft with 80-step histories). Tests: `tests/look-model.test.ts` (golden workspace-1 restores from the step-1 code, recipe and collection parity at 512/1K/2K and compiled maps, a step-1 SQLite library, unregistered features, downgrade, pure apply, CORE-05), `desktop/tests/workspace-store.test.ts`; the four-preset Build's archive members are byte-identical before and after.
- **CORE-14 (partly):** the Glitter-model preset lookup no longer calls `workspaceSnapshot()` (which stashed and copied the whole draft on every Glitter-model or finish change). The shared mutable `glitterChoices` object remains, now as eye makeup's feature memory.

## Fixed in claude/platform-step1

- **CORE-08, CORE-15 (reason codes), part of CORE-03:** step 1 of the [feature-module platform](feature-module-platform.md#8-migration-plan). `STUDIO_REGISTRY` holds six system families (history, collection, preview, motion, quality, saved V) and the eye-makeup module. Each registers its existing descriptors as-is through a table keyed by its action union, so a missing or doubly owned kind fails to compile or to construct. `StudioApplication` reads kinds, descriptors, Undo policy, scene gating (`needsScene`) and the code for a failure after the gate (`thrown`) from the registry, and binds one handler per owner through a mapped type plus a constructor check. The saved-V fallbacks in `capability()` and `dispatch()` are gone; an unknown kind is refused with `invalid_value` ("Unknown command.") instead of throwing. `reasonCode()` and the request-code text matching are gone: every refusal in the recipe, layer, collection, preview, motion, quality and saved-V checks states its code, or its validation issue implies one (`coded()`). A before-and-after matrix of 68 capability and dispatch cases (62 refusals, quality refusals through the real assessment) gave the same result for all but two, both now `asset_unavailable`: an idle-load error whose text lacks "unavailable" (was `invalid_value`) and a test port's "Unavailable" hair message (no production port says it). `CollectionActions` and `PreviewActions` keep their uncoded `capability()` shape for existing callers and add a coded `check()`. Tests: the golden registry equals the catalogue captured before the change, byte for byte and in order; the derived kind sets equal the sets kept by hand before; exhaustiveness and unknown-kind refusal; no message matching in the facade; platform, feature and composition import boundaries. Measured cost on the same machine (six presets × 16 layers × 80 steps; median): `snapshot()` 193 → 191 ms, `requestCapability(package)` 102 → 104 ms, `actionsFor(layer)` 22.0 → 22.7 ms, `contextQuery(shape)` 306 → 311 ms (all within run-to-run noise), and on a fresh workspace `dispatch(layer.select)` 10.1 → 10.1 µs and six mixed `capability()` calls 0.67 → 0.57 µs.

## Fixed in claude/cleanup-hosts

- **PREV-29:** `WolvenKitFetcher` (`src/resolver-host.ts`) names each batch folder with `mkdtemp` (`tmp/batch-<pid>-XXXXXX`). Every fetcher on one cache folder in the process shares a lane: batches run one at a time, and a resource another fetcher is already extracting is awaited instead of extracted again. A batch re-reads the cache before running (another process may have filled it), and cache files and markers are written through a temporary sibling and renamed. `unbundle` accepts any exit (outputs are judged per file) but a crash, time limit or missing .NET fails the batch without markers. A `.failed` marker is written only when `convert` exited 0 without `Unhandled exception` and the extracted file is still there at its unbundled size; markers carry `markerVersion: 2`, and an unversioned marker is removed and the resource retried. Test: `tests/resolver-fetcher-concurrency.test.ts` (two fetchers on one cache folder: on the old code all four resources came back null; now all four convert with no marker and an empty `tmp/`; single-flight; marker only after a clean run, none after exit 1 or a time limit, and a retry; a pre-fix marker is cleared). A run on the reference MO2 route with copied caches prepared the Nova LUT and a resolver fetch concurrently with no leftovers; the one existing marker (`l0_000_base__full.app` from a UV-framework mod) was cleared and written again as a genuine failure, because WolvenKit 9.0.1 logs an RTTI type error for it and writes an empty JSON with exit 0 (see [tooling](../../knowledge/tooling.md#success-and-failure)).
- **PREV-30, PREV-44:** `extractUntrimmedJson` (`src/grading-lut-host.ts`) runs `unbundle` and `convert` through `runWolvenKit` with a 2-minute step deadline, the default exit and log rules and the preparation's abort signal, in an `mkdtemp` folder. `GradingLutHost` aborts a superseded preparation and starts the next once it has stopped; a WolvenKit failure that leaves the neutral grade gets a plain note (the shared missing-.NET message, a time limit, or a failed read). The boundary test requires both hosts to import `./wolvenkit-cli` and never spawn. Tests in `tests/grading-lut.test.ts`.
- **PREV-31:** a WolvenKit or unexpected failure is answered with the neutral grade and prepared again on a request after 30 s (`GRADING_LUT_RETRY_MS`); an undecodable LUT stays neutral until the installation changes. The decoded-LUT key covers `GRADING_LUT_DECODER_VERSION` and `wolvenKitIdentityKey`. Test: missing .NET, no retry within the interval, success after it, and a changed WolvenKit build decoding again.
- **PREV-40:** `lighting-preset-stage.ts` asks the host on every creator activation and every `LUT_RECHECK_MS` (20 s) while the preset shows; the host's answer is keyed by `installationFingerprint`, the character details' fingerprint. The stage re-applies the grade only when the host's cube name changes, and a re-check that never reached the host keeps the current grade. Test in `tests/lighting-preset.test.ts`.
- **PREV-32:** the Windows detection host lists drives with `GetLogicalDrives` and `GetDriveTypeW` (Bun FFI, no process) and keeps only fixed and removable volumes (`localDriveRoots`; A:/B: removable skipped); the `MountedDevices` parser is gone. On the development machine it returned the four fixed drives `Win32_LogicalDisk` reports, where the registry list named two more absent letters. [Source discovery](source-discovery-foundation.md) updated.
- **PREV-33:** `gameDetectionNote` (`src/install-detection-actions.ts`) gives the setup card one note when no folder was found: an unfinished Epic install first, then the Xbox app message, then an unreadable launcher record (reworded without "registry"). Test in `tests/preview-setup.test.ts`.

## Fixed in claude/cleanup-render

- **UI-38 (and UI-11 in part): render on demand.** `src/render-scheduler.ts` draws a frame only when something visible changed (a dirty flag) or while the idle or the blink study plays; otherwise the loop stops. Triggers: every scene mutator (wrapped by `invalidating`, including `onFrame` registration and removal, resize and front), the makeup stack's canvas and layer updates, the lighting device's notices (preset, body, LUT arrival) and its creator options, the idle's `onChange` (enable, pause, seek, contributions, bones joining or leaving), the controls' `change` event (so each damped orbit step asks for the next), canvas pointer, wheel and context-restore events, the ResizeObserver, `drawUV` (recipe and selection changes move the surface handles) and the surface editor's show/hide. The per-frame `Vector3` and the unbounded timing arrays are gone (fixed rings). Skin placement moved to `src/head-skin-placement.ts` (it returns the `head-shape` limit code), the evidence projections to `src/scene-evidence.ts`, and the limit wording to the presentation (`DETAIL_LIMIT_TEXT` in `studio-ui/panels/preview.ts`; codes in `src/detail-limits.ts`, the skin adapter's glow limit included). Tests: `tests/render-scheduler.test.ts` (one frame per request, coalescing, damping, animation, wake-up dt, dispose, every trigger source, the idle's change notices, and a source check that every scene mutator is wrapped and no reader is) and `tests/detail-limits.test.ts`.
  - **Measured** in headless Chrome (RTX 4070 through ANGLE D3D11, 1600×900 window, default V with resolved skin, brows, lashes and hair, `?verify=1`). Animation-frame callbacks and head draw calls were counted, and GPU time taken with `EXT_disjoint_timer_query_webgl2`, over 5 s per row:

    | Idle viewport | Frames/s | Main-thread frame time | Head draw calls/s | GPU time |
    |---|---|---|---|---|
    | Studio, before | 57 | 51 ms/s | 972 | 221 ms/s |
    | Creator, before | 57 | 60 ms/s | 966 | 437 ms/s |
    | Studio or Creator, after | 0 | 0 | 0 | 0 |

    While the idle plays the viewport still draws every refresh (56 frames/s); pausing the idle or turning it off stops drawing. Checked in the same session: presets on and off, creator exposure, a colour edit, exposure, key angle, blink value and blink study, idle on, pause, resume and off, front view, wire, hair, surface controls, point selection, a handle drag, an orbit (damping draws about 9 more frames, then stops), wheel zoom, a save load, and a switch to a second save and back. Each drew and changed the picture at once, then went idle.
- **PREV-41:** the character loader passes the resolved skin's chunks and toned colour to decal adapters, and the brow underlay reads the skin on whichever head the placement will draw: the resolved chunks with their own UVs, or the core head. A reshaped head with no readable skin colour falls back to the linear decal blend with a note. The placement is decided once per loaded skin, so the scene uses the answer the decals were projected with. Test: `tests/head-skin-placement.test.ts` (a reshaped head with mirrored UVs).
- **PREV-42:** the placement compares all of a skin component's chunks (merged in order) with the core head. Only a single chunk with the same surface goes on the core head; otherwise the chunks are drawn and the core head is hidden, so the two never overlap. A different surface reports `head-shape`. Tests in `tests/head-skin-placement.test.ts`.
- **PREV-43:** the skin adapter's toned colour is `skinBaseTexels`, evaluated only at the texels a decal reads, each once (the same values as `skinBaseImage`); the core albedo fallback is read once per scene. On a 1024² skin the underlay drops from about 320 ms to about 10 ms, most of it the nearest-vertex search. Tests in `tests/skin-material.test.ts`.
- **UI-39:** the device no longer logs. In `?verify=1`, `window.xfStudioSceneEvidence()` returns the core evidence, the character-detail evidence (now with limit codes) and the frame timing.
- **CORE-25:** `SavedAppearanceActions` derives the body (`bodySexOf`) from the applied save and passes it to the port's `setBodySex`, which only forwards it to the lighting device. `createTrustedPreviewServices` restores from a deep copy of the workspace preview. The earlier shallow copy shared its nested objects (camera, creator lighting) with the caller's workspace; no write through them was found, and a test with a deep-frozen workspace now guards the copy. Tests in `tests/trusted-preview-services.test.ts`.
- **UI-40:** `patchMean` takes explicit units. `patches.json` must declare `"units": "pixels"` or `"fractions"` (the tool refuses a file without it), and without `--k` the tool prints the exposure it assumes. Tests in `tests/creator-calibration.test.ts`.

## Fixed in claude/cleanup-window

- **PIPE-32:** the head-UV coverage reference each window preset writes for the verifier is now a crop of the head-atlas `raster` (4096 texels across; `presetCoverage` in `preset-compiler.ts`), never `rasterWindow`, which makes the maps. A fault in the window code now moves the maps but not the reference. `tests/plate-reach.test.ts` bakes the lid layer in a separate Bun process (`tests/mirrored-window-bake.ts`) with `rasterWindow` mirrored through a runtime plugin. The reference is unchanged and the verifier's mapping gate (`checkMapping`, now exported) fails. The pipeline doc now lists three kinds of builder file the verifier reads.
- **PIPE-33:** the plate's **UV footprint** (stored bounds, window, every vertex UV and triangle; `plateUvFootprint` in `plate-uv-window.ts`) is recorded beside each derived plate (`plate-uv.json`, manifest `uv` record). Older cache entries, and those whose footprint no longer matches, are derived again once; on the reference installation the plate bytes come out identical.
  - `plate-reach.ts` omits a preset whose exportable layers reach 2/255 coverage at none of the plate's sample points (the verifier's own set), as a reported omission. If nothing is left, the plain refusal says to move the shapes onto the eyelids.
  - Check plans on the plate the host's cache last prepared for the same game and recipe (both hosts; CLI `--check --plate-manifest`). Build plans on the plate it packages: from the manifest record, or by serializing an override or older plate mesh once. The resource builder requires the serialized plate's footprint to equal the planned one.
  - Check, the build result, `manifest.json` and `build.json` record `plateUv`. The host result gate compares it with the host's own copy of the prepared plate's footprint.
  - The verifier refuses an empty window map (`covered`), and an empty decoded check, with plain `VerificationError`s instead of "No values to summarise".
  - Tests are in `plate-reach`, `package-build-service`, `package-server`, `eye-plate` and desktop `check-runner`/`build`. A real Check and Build of the finish board with an added off-plate preset both omitted it and agreed on the packaged snapshot hash.
- **PIPE-34:** `bakeCollection` without a window (the Experiment 005 oracle's `tools/bake_collection.ts`) writes a plan whose every preset has `uvSpace: "head"`, matching its maps. Tested.
- **PIPE-35:** `mappingOffset` in `mod-verifier/uv-window.ts` gives a signed offset estimate. It runs a joint search over shifts of the map lookup (±8 window texels, refined 1 → 1/4 → 1/16) for the least total difference from the reference at the plate samples, using only samples near content. The gate bounds the estimate to 1 texel per axis (`MAPPING_LIMITS.offsetTexels`), and the verification report records it per window preset. Tests shift a real bake by ±2 and ±4 texels in each axis: the estimate recovers each shift with its sign, and at 2 texels the mean and far-off limits alone pass. On the real session-2 and finish-board builds every window preset's estimate is 0 on both axes, except 0.06 texel in V on the sharpest line pattern. `tools/uv-window-probe.ts` reports it.
- **PREV-48:** a changed LUT with identical source metadata swaps the grade without requesting a frame (`lighting-preset-stage.ts:68-72`). Fixed in claude/cleanup-polish (see its section).
- **PREV-49:** a failed atomic cache write drops already-extracted documents for the whole batch; cancelling a superseded LUT preparation can't stop discovery or graph loading. **Fixed** (claude/cleanup-hosts2).
- **PIPE-38:** the mapping gate accepts an all-empty window map for very faint makeup (offset null is accepted). **Fixed** (claude/cleanup-hosts2).
- **PIPE-39:** the shift estimate is unreliable for presets that barely reach the plate (0.6-0.8 texel on lossless data). **Fixed** (claude/cleanup-hosts2).
- **UI-41:** F6 region cycling skips the tour card and onboarding offer. Fixed in claude/cleanup-polish (see its section).
- **UI-42:** a tour button's `then: "next"` can double-advance after an async command (latent). Fixed in claude/cleanup-polish (see its section).
- **UI-43:** starting a tour while one runs replaces it without recording how it ended. Fixed in claude/cleanup-polish (see its section).
- **UI-44:** help and tour text restate which finishes export (extends UI-10). Fixed in claude/cleanup-polish (see its section).
- **UI-45:** idle playback runs 5-20 % slow while orbiting (scheduler resets its clock mid-frame). Fixed in claude/cleanup-polish (see its section).
- **UI-46:** a device-pixel-ratio change (monitor move, zoom) isn't picked up until reload (pre-existing). Fixed in claude/cleanup-polish (see its section).
- **UI-47:** the hover highlight stays after a drag ends until the mouse moves. Fixed in claude/cleanup-polish (see its section).

## Fixed in claude/cleanup-hygiene

- **One personal-data rule set (REL-02, REL-03, SITE-02).** `tools/private-data.json` holds the user-path patterns (Windows in any case, slash direction, JSON or JavaScript escaping, or percent-encoding; WSL `/mnt/<drive>/Users`; macOS `/Users`; Linux `/home`), the whole-folder-name placeholders, the address pattern and its exemptions, and one set of test vectors. It is written in the regular-expression subset Python and JavaScript share. `tools/check_private_paths.py`, `site/tools/privacy.ts` and `desktop/package-content-scan.ts` all read it, so the three scanners cannot drift, and each runs every vector in its own tests.
- **REL-02:** the packaged-app scan now catches lower- and upper-case, WSL, percent-encoded, `\`-escaped, macOS and Linux home paths. It passes `git@github.com:…` remotes, `pkg@1.0.0-beta.rc` versions and file names such as `icon@2x.png`. A path two patterns see is reported once, and findings stay redacted. A scan of the authoring and desktop `node_modules` (1,668 text files) found no path false positives; the only findings were real maintainers' addresses in third-party files the package allowlist doesn't ship.
- **REL-03:**
  - Addresses end at a word boundary, so an address before a full stop is caught; local parts may start with a digit. Only a following call, index or dotted name marks code (`a@np.eye(4)`, `a@np.linalg.inv(m)`).
  - The material-reference exemption covers only one label plus a game or file extension that isn't a real top-level domain (`ash_brown@long.mi`); `.app`, `.mt` and `.md` are addresses.
  - Placeholders match whole folder names, so `user.jdoe` and `name-jdoe` are flagged.
  - Exemptions are exact repository paths for specific kinds: the shared vectors, the two tests that plant personal data, and the checker's own media-folder self-test case. The repository scans clean with no licence or lockfile exemption.
  - Every tracked file that reads as UTF-8 text is scanned whatever its extension, except known binary formats.
  - `--self-test` runs the shared vectors plus the exemption and file-selection rules; `.github/workflows/docs.yml` runs it before the scan.
- **SITE-01:** the knowledge generator checks the whole `knowledge/README.md`, whose Topics summaries the index publishes. The `dist/` check reads each HTML, SVG and XML file both as written and with tags removed, so a `<wbr>` after a separator no longer hides a path. Check findings are redacted. Tests: a summary with a user path in a code span fails the build, and the same code span rendered into a page fails the check.
- **UI-36:** `unloadHead(scene)` releases only the head it is given, and only if it is still loaded (as `disconnectScene(scene)` does). `attachBrowserHead` registers that release once its load has returned. Test: releasing a head that a later load replaced leaves the current head loaded. Still latent: a stale `AttachedHead`'s other releases clear the application's service attachments whatever is attached now. `studio-startup.ts` always disposes the old head before attaching a new one, so this can't happen today.
- **UI-37:** `tests/head-camera-input.test.ts` drives the real OrbitControls through three sequences, each checked against a deliberately broken adapter:
  - pointercancel (touch, mouse and a cancelled makeup edit);
  - the first finger lifting before the second: the second continues with the first finger's slot, and slots rest only when the last finger lifts;
  - a first touch the surface editor consumed, followed by a second finger, for every modifier set whose one-finger press on makeup isn't a camera effect.

## Fixed in claude/cleanup-verifier

- **PIPE-28:** `resource-checks.ts` compares every top-level plate field the build does not own with the input (only the mesh's appearances, material entries and local materials, and the morph's base mesh reference and seed appearance, which must be the first preset's, are the build's). `plate-geometry.ts` compares the mesh render blob, the morph base blob and the morph blob with the input as whole objects; the only fields that may differ are re-derived there, never read from the package: chunk byte offsets and index offsets, topology, vertex/index buffer sizes and offsets, zero padding and buffer length, morph counts, starts, `numDiffs`, `numDiffsMapping`, the exact mapping buffer (the input's per chunk, so trailing bytes fail) and the diff buffer length. The verifier still imports nothing from the builder. Tests: `tests/mod-verifier.test.ts` "plate blobs" (every probe case: `bonePositions`, `renderLODs`, `version`, chunk `materialId`, `numDiffs`/`numDiffsMapping`, texture-diff data and buffer, morph bounding box and base texture, trailing and changed mapping bytes, starts, buffer length).
- **PIPE-29:** the head's position quantization must equal the input's (the builder now refuses a lift that would leave it instead of widening it); a morph target's delta quantization must be the input's or exactly the min/max of its lifted deltas (float32 precision), with other vector fields unchanged; and errors are capped at 0.01 mm (positions) and 0.02 mm (morph deltas) whatever the quantization. The real plate re-quantizes 24 of 105 targets (7.5 µm largest error), within the caps. Tests: a fixture whose deltas sit at the bottom of their range re-quantizes targets and verifies; a widened re-quantized target, a widened retained target, a changed W and a too-coarse input step are refused; `tests/plate-lift.test.ts` covers the builder's refusal.
- **PIPE-30:** Check, `manifest.json` and the build result record `plateLiftsMm`, and each packaged preset's identity carries its `diagnostics` (one shared `packagePresetIdentities` helper replaces five copies). The build service requires the verifier's plate lifts to equal Check's; the localhost Check gate and the shared result gate require both to equal the host's own plan. Tests in `tests/plate-lift.test.ts`, `tests/package-build-service.test.ts` and `tests/package-result-verifier.test.ts`.
- **PIPE-31:** the result gate keeps the canonical comparison and adds a lexical one: the result must name the same folders below the root (as given, or its canonical long form) as its canonical path does, so an outside path reaching in through a junction is refused. A missing result, manifest or payload gives the plain containment error. Tests: junction from outside (directly and by a same-named link), missing manifest and result, and an 8.3 short-named dist root with a long-form result (runs where the volume has 8.3 names).
- **PREV-26:** the detail request key covers an installation fingerprint: the launch-route settings, WolvenKit's identity, and modification stamps of the game's mod folder and `modlist.txt`, the MO2 profile's `modlist.txt`, the overwrite folder and a manual mod folder. A changed profile, mod list or WolvenKit prepares again. Limit: files updated in place inside an existing MO2 mod folder are not detected until restart. Test: changing `mo2ProfileId`, then the profile's mod list, re-prepares.
- **PREV-27:** superseding a preparation forgets its state at once; a `preparing` key whose run was cancelled restarts; and a new run starts only after the cancelled one has stopped on the shared cache (runs are chained). Test: V1→V2→V1 restarts V1 once its cancelled run stopped, V2 never starts, and preparations never overlap.
- **PREV-28:** `releaseDetailObject` disposes every mesh's geometry and every skinned mesh's skeleton (its bone texture) on `dispose()` and when a component fails to load. Test in `tests/character-material-adapters.test.ts`.

## Fixed in claude/bridge-hardening

Runtime bridge security review (`projects/xf-runtime-bridge`). Evidence: `bun tools/selftest.ts` passes 75 checks (19 in-process, 56 through the Bun library and both command-line clients); both lints pass; the plugin builds without warnings.

- **RB-01:** `JsonNestingDepth` pre-scans every line (strings and escapes respected, never recursive) and refuses nesting deeper than 32 before parsing; only a number, string or null `id` is echoed. The 5,000-deep tokenless request is now `bad_request` and the host keeps answering; 32 levels pass and 33 fail.
- **RB-02:** no `FlushFileBuffers`. Every wait in the pipe thread watches the stop event, which is also checked on each loop, so a client that keeps data flowing can't delay a stop either. The exception is a fixed 0.5 s linger that lets a dropped client read its last reply (such as the `bridge.kill` answer). Stopping the bridge closes the game-thread queue first, releasing any waiter; `PipeServer::Stop` warns and cancels the pipe's I/O if the thread takes over 1 s, and logs `bridge.server_stopped stop_ms=`. With a client that never reads, a KILL stopped the host in about 0.7 s (`stop_ms` about 510, mostly the linger), and shutdown with a pipe full of unread replies gave `stop_ms` 0 (the reproduction had held the host 12 s).
- **RB-03:** responses, logs, natives and `session.json` serialise with `error_handler_t::replace`; `Sanitize` cuts on UTF-8 character boundaries and turns invalid bytes into `?`; `log::Write` never throws. `Dispatcher::Handle`, both bridge threads, `Main`, the state and RTTI callbacks and all five natives run inside catch-alls that log; a failed load leaves the bridge off.
- **RB-04:** each queued task has an atomic state (queued, running, done, cancelled) changed by compare-and-swap. A task still queued at the timeout is cancelled and never runs. A running one gets a 1 s grace, then the client gets `timeout_after_start`, and the game thread logs `game.task_completed_late` when it finishes. `Close()` releases queued and running waiters at once. Unit checks cover every path; the self-test covers grace and late completion through the pipe.
- **RB-05:** the rate limit runs before the token check, so it covers unauthenticated requests. Malformed and unauthenticated requests are marked rejected: the fifth on a connection drops it, and the server waits 1 s before accepting again. Every refusal is logged under its own event.
- **RB-06:** the Bun client opens the pipe through `bun:ffi` (kernel32) instead of `node:net`, at the Identification impersonation level, and checks `GetNamedPipeServerProcessId` before sending the token. The PowerShell client also uses Identification. Both exit 3 on a wrong server and send nothing (self-tested).
- **RB-07:** `XFB_RUNTIME_DIR` is gone. Only the self-test passes a folder, explicitly, to `CreateSession`; the clients' `--runtime-dir`/`-RuntimeDir` flags are for the self-test.
- **RB-08:** before each game call, `RequireSignature` checks the static flag, parameter count and types, and return type name against red-dump-json, and refuses with `rtti_signature`. It can't catch a function whose signature is unchanged but whose behaviour changed, so the "fails soft" claims in the docs are corrected.
- **RB-09:** the event handles are guarded by a mutex in `DropClient` and `Stop`.
- **RB-10:** a failed connect calls `DisconnectNamedPipe` and retries with backoff (100 ms doubling to 5 s, stop-aware).
- **RB-11:**
  - `projects/xf-runtime-bridge/.gitignore` ignores `build/` anywhere in the project.
  - `bridge.kill` is a new *control* access class (the docs said "kill"; the code said read).
  - The test card shows `<n>ms`.
  - Both zips carry `THIRD_PARTY_NOTICES.txt` (nlohmann/json and RED4ext.SDK, both MIT, plus the SDK's bundled notice).
  - Each build embeds `XFB_BUILD=<commit>;dirty=<0|1>` in the DLL (`native/cmake/BuildInfo.cmake`). `package.ts` refuses a dirty tree, or a DLL not built cleanly from `HEAD`, and the manifest records that commit.
  - The design doc now states the security boundary: the Windows user at medium integrity.

## Fixed in claude/release-prep

- **PREV-20:** the head and everything wired to it are one object, `attachBrowserHead` (`src/browser-head-attachment.ts`), which registers a release for each connection as it is made (stage theme binding, application attachments, autosave and status subscriptions, the preview device's scene connection, the surface editor, the camera listener) and releases them newest first on any failure or on `dispose()`. The viewport device owns the scene's lifetime (`loadHead` releases an earlier head, `unloadHead` detaches the surface editor and disposes the scene), and `createScene` releases its renderer, canvas, stage and resize observer if anything fails after the renderer exists; the scene gained `dispose()`. `studio-startup.ts` keeps one `AttachedHead` and releases it before a retry. Tests: `tests/browser-head-attachment.test.ts` (a step failing after the scene loaded, then Try again attaching exactly once; a failed load; reloading over a loaded head).
- **PREV-21:** `PolledHostState` numbers requests and applies a reply or failure only if nothing newer was applied; overlapping refreshes share one in-flight request while nothing newer was sent.
- **PREV-22:** a `disposed` flag stops late replies from applying, publishing or scheduling.
- **PREV-23:** game detection and automatic preparation (including the WolvenKit-ready restart) wait for `start()`.
- **PREV-24:** a failed head returns to waiting once the host leaves `ready`, and loads when it is ready again.
- **UI-34:** the setup service counts show requests (`showRequests`); the card takes focus only when that count changes, never when it opens by itself.
- **UI-35:** the head pane's next-step button applies the setup action's capability (disabled with its reason while a step runs).
- **REL-01:** `desktop/package-content-scan.ts` scans every packaged text member of the real archive in `verify-canary.ts` for absolute user-profile paths (`C:\Users\<name>` in any drive, slash direction or escaping) and email addresses, allowing placeholders, example domains and addresses in licence text, and redacts findings for the public log. Tested in `desktop/tests/package-content-scan.test.ts`; the three canary archives built locally on 25 September scan clean and a planted path is caught.
- **PREV-27:** quick V1→V2→V1 wastes a preparation; cancelled runs overlap new ones on the shared cache (extends PREV-07). Fixed in claude/cleanup-verifier.
- **PREV-28:** character detail `dispose()` leaks each skeleton's bone texture. Fixed in claude/cleanup-verifier.
- **PIPE-30:** diagnostic lifts/overrides are recorded only in intermediate files, not Check, the manifest or the result gate. Fixed in claude/cleanup-verifier.
- **PIPE-31:** the package result gate lost its lexical containment check; a path outside dist that reaches in through a junction is accepted. Fixed in claude/cleanup-verifier.
- **SITE-01, SITE-02, UI-36, UI-37:** Fixed in claude/cleanup-hygiene (see below).
- **PIPE-25 (extended):** each V switch opens the installation synchronously on the host.
- **PREV-42:** a skin component with more than one mesh isn't placed and is drawn beside the core head (z-fight), with no limit line (`scene.ts:581`). Fixed in claude/cleanup-render.
- **PREV-43:** the toned brow base image is computed over a full 1024² image per V load (~300 ms synchronous); only ~390 vertices are sampled. Fixed in claude/cleanup-render.
- **UI-39:** production device logs ~5 KB of detail evidence (archive names) to `console.debug` on every V load. Fixed in claude/cleanup-render.
- **CORE-25:** the creator rig's body sex is a side effect in the saved-appearance port; `trusted-preview-services.ts` mutates the caller's `initial` workspace. Fixed in claude/cleanup-render.
- **PREV-44:** grading-LUT host runs WolvenKit without timeout or exit-code checks and races the shared resolver cache (extends PREV-07). Fixed in claude/cleanup-hosts (with PREV-29 and PREV-30).
- **UI-40:** calibration tool guesses box units (≤ 1 means fractions) and silently assumes the default exposure. Fixed in claude/cleanup-render.
- **PREV-31:** LUT failures stored as ready-neutral with no retry; decoded-LUT cache key lacks decoder/WolvenKit identity. Fixed in claude/cleanup-hosts.
- **PREV-32:** Xbox detection probes stale `MountedDevices` letters that a mapped network drive may reuse (can block on an offline share). Fixed in claude/cleanup-hosts.
- **PREV-33:** the setup card shows only the first unsupported message; unfinished-Epic and unreadable-registry issues never reach the user. Fixed in claude/cleanup-hosts.
- **PIPE-34:** the oracle bake with no window writes a plan claiming the plate window while its maps are head-UV. Fixed in claude/cleanup-window (see below).
- **PIPE-35:** the mapping gate misses sub-millimetre shifts (about 6 texels pass); add a signed-offset bound. Fixed in claude/cleanup-window (see below).
- **CI (release blocker):** the first Desktop release run failed on a 5.1 s exhaustive flake test under Bun's 5 s default. Tests taking about a second or more locally now carry explicit, commented timeouts (flake-field, glint oracle, mod-verifier resource failures, workspace storage budget, desktop Build deadline).

## Fixed in claude/wolvenkit-fetch

- **PREV-01:** `game-asset-export.ts` publishes an entry only when every output for its kind exists (mesh: raw, GLB, materials; morph target: raw, GLB); a partial export is returned uncached and the next request reruns WolvenKit. Regression test on one shared export cache in `tests/preview-core.test.ts`.
- **PREV-02:** export cache keys include the WolvenKit identity (PE version plus launcher/entry-DLL hash); the preview key covers the exported GLBs (head, eye, eye morph) and material exports and the tool key; manifest and render record name the tool (deriver version 3).
- **PREV-04:** `ensurePreviewCore` routes failures by type: `GameAssetExportError` codes (tool missing, runtime missing, tool failed, cancelled), storage errno codes to `preview_cache_unavailable` with plain disk/permission wording, anything else to `preview_failed`, never blaming WolvenKit.
- **PREV-05:** a resource is reported missing (and blocks until the game changes) only when the game's own RDAR archive index lacks it (`rdar-index-fs.ts`); a tool that exported nothing, or an unreadable index, is a retryable `preview_tool_failed`.
- **PREV-06:** `wolvenkit-cli.ts` owns the process, the success policy (exit code, timeout, cancellation, `Unhandled exception`, per-command accept/failure rules), missing-.NET detection, identity and the sync/async version probe; `eye-plate-wolvenkit.ts`, `game-asset-export-wolvenkit.ts` and `package-build-wolvenkit.ts` map its typed errors to their own codes. The boundary test requires every adapter to go through it.

## Fixed in claude/cleanup-core

- **CORE-01:** save status is published only on change, autosave watches content sources (`session.watch(bootstrap.collection)`) instead of the whole presentation port, and identical content is never rewritten. Test: `tests/workspace-persistence.test.ts` (fake timers, zero idle writes, resumes on edit).
- **CORE-02:** `workspace-budget.ts` bounds the stored workspace (2,000,000 code units per key; selected preset stored once with full Undo, others 5 entries, removed and recovery entries without histories, progressive trimming) and publishes `nearly-full`/`full`. A worst-case realistic workspace (6 eight-layer presets, 80 Undo entries each, 20 removed presets and 4 recovery drafts all with histories, 123M code units verbatim) stores in 1.81M at the standard level. Desktop follow-up: the storage shim should save the host file independently of `localStorage` quota errors, and may pass a larger budget.
- **CORE-10:** damaged recovery drafts and removed presets are dropped with a warning (`repaired` status); only the current draft must parse.
- **CORE-04:** checkpoints return the entry they added; transactions label and discard that entry, and the displaced oldest entry returns when an entry is discarded or undone. Test: `tests/history-limit.test.ts`.
- **CORE-06:** `capability()` applies descriptor payload ranges, `controlEdit` runs the same gate and returns a typed result, exceptions are classified by source, and control dispatch is wired inside `createTrustedAuthoringCore`. Test: `tests/control-edit-validation.test.ts`.
- **CORE-11 (partial):** the tests above, plus touched fixtures (`studio-application`, `alpha-capability-reasons`, `application-boundary-fixture`) now build through `createTrustedAuthoringCore`.

## Fixed in claude/alpha-polish

Paths are relative to `projects/xf-studio/authoring`.

- **UI-21:** a DOM-free `PreviewSetupActions` service (`src/preview-setup.ts`) owns the 3D preview first run: it follows the preparation and WolvenKit ports, finds the game folder, starts preparing by itself on first contact, loads the head, and maps every state to plain wording with one next step. The presentation reaches it only through `StudioPresentationPort.previewSetup` (detached snapshot of the card, the WolvenKit consent and the head pane; 16 typed `previewSetup.*` actions catalogued in `PREVIEW_SETUP_DESCRIPTORS`). The card and consent moved under `studio-ui` (`src/studio-ui/preview-setup-card.ts`) on design tokens; `src/browser-preview-card.ts` is gone, so no boundary exception was needed. "Not now" hides the card and the head pane offers the next step ("Set up 3D preview", "Show progress", "Try again"), which opens it again with focus on its heading.
- **UI-22:** the renderer and preview-record loader throw a typed `HeadLoadError` (`src/head-load-error.ts`: `webgl_unavailable`, `preview_damaged`, `preview_unreachable`, `head_load_failed`). A failure is no longer latched: the head pane shows plain wording (graphics driver or Remote Desktop for WebGL; "Prepare again" for a damaged preview, which asks the host's new `rebuild` action to set the ready entry aside and prepare afresh; "Try again" otherwise, then "Prepare again" after a second untyped failure). A partly loaded scene is released before a retry. Loader text is never shown.
- **UI-23 / UI-33 (polling):** `PolledHostState` (`src/host-state-poller.ts`) is the one polled host-state port behind both `PreviewPreparationActions` and `WolvenKitSetupActions`. A failed poll while the last known state was working re-arms with exponential backoff (capped at 8 s) and publishes `connection()` (failures, retrying); the card says "XF Studio lost contact with its 3D preview service. Still trying (attempt N)…" until the first valid reply clears it. Checked by stopping and restarting the localhost server mid-prepare.
- **UI-24:** `ViewportPhase` gains `preparing` and `unavailable` (`setPending`), with `message` and `progress`. The head pane uses progress styling for checking, loading and preparing, a neutral head icon while something is still needed, and the danger icon only for failures; `data-tone` is set on every paint.
- **UI-25:** Game & tools drops the revision and server-override line, the Bun field and "checked separately"; WolvenKit is "Your own WolvenKit (optional)" with "Leave this empty and XF Studio can download WolvenKit for you"; readiness reads "Ready to build your mod files". The desktop Build setup has the same WolvenKit field and hint and no Bun field. `bunExecutable` is retired from the settings (dropped on load like `pythonExecutable`); desktop Build always runs its own Bun, and `XFS_PACKAGE_BUN` is the localhost developer override. `USER_FACING_JARGON` now also matches settings revisions, server overrides, Bun and "checked separately", and a test scans `studio-ui` and the desktop bootstrap for the retired phrases. The "Head used for the eye plate" dropdown was checked in both themes: plain label, default "The head your game loads (recommended)".
- **UI-26:** `LocalSetupActions` gained `setup.update` (waits for any request in flight, loads if needed, saves only the named fields over the saved ones) and `requestRefresh()` (a refresh asked for while busy runs once the request finishes). `studio-startup.ts` no longer merges fields, queues refreshes or watches the revision; Game & tools saves through `setup.update` too.
- **UI-27:** card and consent links use `--accent-text` (`.link-button`); the card's inline stylesheet is replaced by token-based `studio.css` rules (`.setup-card`, `.consent-sheet`), documented as style-guide pattern `c-setup`.
- **UI-20:** `ViewportInputHints` keeps its own `interactive` state from `viewport.snapshot()[scope].phase`; the strip, tooltip and cursor show only while the viewport is ready, so the input subscription can no longer bring the strip back over the head pane's message.
- **UI-19:** the header category is a plain label (no menu, no informational section) while Eye makeup is the only category; the style guide's `s-category` pattern says it becomes a switcher only when a second approved category exists.
- **UI-28:** localhost opens official pages with `window.open(url, "_blank")` and cuts `opener` on the returned window, so a successful open no longer reports an error; a blocked pop-up says how to allow it.
- **UI-29:** "I already have WolvenKit" (and every "set it in Game & tools" step) dispatches `previewSetup.openSetup`; without a host setup form the Studio reveals the Mod package panel, opens Game & tools and focuses the first empty field.
- **UI-30:** the automatic-start choice is the workspace field `previewSetup.autostart` (verification-scoped with the workspace, kept in the desktop's host-owned file); the earlier page-storage value is read once as a fallback outside verification.
- **UI-31:** the site's finish grid tags Shimmer, Glossy and Colour-shifting "Experimental export" (Glitter stays "Preview study"); the "from source" callout says the preview is built from the developer's own game on first run.
- **UI-32:** the autosave tooltip no longer says "Browser"; a missing bundled WebView2 installer has its own message that doesn't blame the internet (`WEBVIEW2_INSTALLER_MISSING`); the package error's time is when it happened (`files.last.at`); the consent opens on its heading, and Esc is "Not now".
- **UI-12 (partial):** `tests/preview-setup.test.ts` (first run, Not now and re-open, lost poll with backoff and recovery, head-load failure and retry, damaged preview and Prepare again, detected folder merge, consent, catalogue and wording), `tests/local-setup-actions.test.ts`, and additions to the preparation, viewport-device, workspace and WebView2 tests.

## Fixed in claude/cleanup-core2

- **CORE-16 (and CORE-09, partly):** `requiredRecipeSchema()` (`recipe-schema.ts`) computes the schema from every layer's stored form (irregular 7, Direct 8, Clustered 9, Fine 10, game-matched optics 11) and never goes below the current schema. `applyRecipeAction` applies it to every edit and `selectGlitterModel` uses it, so no per-layer choice downgrades the recipe or invalidates another layer. Tests (`tests/finish-actions.test.ts`): Glitter models beside game-matched Glossy, Direct beside Fine on recipe-11 and recipe-10, no downgrade after removing or changing the last game-matched layer, and every finish/model the capability offers also dispatches.
- **CORE-17:** re-selecting the current finish (Satin and its alias included) is a no-op with no Undo step, so an earlier-model Glossy layer stays as it is until `layer.useGameOptics`. Colour-shift settings are remembered per preset and layer in the same editor memory as inactive Glitter models (the workspace's `glitterChoices` key gains an optional `shift` entry that older builds ignore); leaving Glitter also keeps the active model's settings.
- **CORE-18:** `StudioApplication.layerExport()` returns the layer's status in the preset-level plan (`planPresetExport`), with `blockedBy: "layer" | "preset"`; a hidden layer is judged as if shown. The Inspector shows "Left out of this preset" with the plan's reason instead of "Experimental" or "Earlier preview model".
- **CORE-08 (partial):** `undoPolicy()` reads the descriptor table (command or key variant first); the recipe and collection routing sets are typed records over the action unions (`RECIPE_ACTION_KINDS`), and the selection-only set is derived from descriptor `effect`. One visible change: the Presets "Restore" entry now reports the descriptor's `recovery` policy. Boundary test in `tests/studio-application.test.ts`.
- **CORE-21, CORE-23, CORE-24:** see the Low list.

## Fixed in claude/cleanup-pipeline2

- **PIPE-23 / CORE-19:** `planPresetExport` leaves out layers no route can carry first, then applies the colour-shift one-pigment rule to the exportable layers; a Colour-shifting layer beside Glitter exports on the Fresnel route with only the Glitter layer reported, and per-layer exclusion reasons (CORE-18) are kept. Tests in `tests/finish-export.test.ts`.
- **CORE-20 / CORE-22 / CORE-08 (finish choices):** `FINISH_EXPORT` in `finish-export.ts` is the one per-finish table (game-matched model, route, experimental flag, flat surface, Check note, catalogue summary, refusal). `layerExport`, `finishExportSummary`, `flatSurface`, `hasGameOptics` (recipe validation and `recipe-actions.ts`) and the preview's surface values (`makeup-stack.ts`) derive from it; `SUPPORTED_FLAT_FINISHES`, `unsupportedFlatLayers`, `presetRoute`, `FLAT_SURFACE` and `GAME_OPTICS_FINISHES` are gone. `layer.setFinish` accepts `FINISH_IDS` from `finish-catalogue.ts` plus the hidden legacy alias `satin`, and the Finish menu offers only catalogue entries. The verifier keeps its own restated table (`VERIFIER_FINISHES`); `tests/mod-verifier-routes.test.ts` fails if the two disagree.
- **PIPE-24:** the verifier re-derives each preset's route from its recipe (any colour shift means Fresnel with one pigment and nothing else, otherwise any Shimmer means faceted, otherwise flat; no route for Glitter or earlier models) and requires the plan's route and material entry and the compiled record to match; a missing route fails. It compares every recipe with the packaged collection the service passes, reports `presetRoutes`, and the service requires them to equal Check's. Check, the manifest's `presets` entries and the host result gate carry each preset's `route`. Tamper tests alter `plan.route`, remove it, alter the compiled record and the recipe, and build a consistent Shimmer-as-flat package; on a copy of the real finish-board build, changing one route in `build.json` failed verification.
- **PIPE-07 (verifier) and the desktop spawn helper:** `runWolvenKitSync` gives synchronous callers the shared success policy and typed errors; `src/verifier-wolvenkit.ts` injects the verifier's unbundle, serialize and export through it (the verifier directory still imports nothing), with the shared `WOLVENKIT_RUNTIME_MISSING_MESSAGE`. The desktop Bun probe uses `runProcessTree`, which stops the whole tree.
- **PIPE-26:** `incompleteHeadSource` stops Build with `plate_source_incomplete` and one next step when a scan gap may hide an archive or `.xl` file (source discovery now marks a skipped link to a plain file as harmless) or when an unreadable archive index ranks before the head's winning archive (or a head resource or plate-relevant patch source was found nowhere). The reference MO2 route's four linked non-mod files in `overwrite` do not block.
- **PIPE-27:** the typed Local setup field `eyePlateHead` (`installed` or `base-game`, default `installed`) is saved through the shared `LocalSetupActions` and shown as "Head used for the eye plate" under Game & tools; the setup view supplies its label and options, so the form and Build's message name it the same way. Both hosts pass it to the plate service; `XFS_EYE_PLATE_HEAD` remains a developer override. The desktop bootstrap's own setup fields do not show it (saving there keeps the stored value).
- **PIPE-25 (partial):** a published plate is reused without extraction, serialization or hashing when the chosen archives' path, size, time and entry paths, the patches, recipe and head choice match (`head-inputs/` records in the plate cache); mod archive hashes are memoised by path, size and time. Remaining: route resolution (discovery, index caches, `.xl` parsing, about 2 s on the reference route) still runs synchronously on the host each Build; moving it and plate derivation (PIPE-20) into a worker or the builder process is the next step.
- **PREV-03 (comment):** `preview-core-recipe.ts` now says the preview reads the base game's head while Build uses the launch route's head.

## Fixed in claude/cleanup-pipeline

- **PIPE-13:** a new Authoring workflow runs `bun test`, `bun run check` and `bun run build` on Ubuntu and the desktop typecheck (Electrobun devkit) and `bun test tests` on Windows for pushes to `main` and pull requests touching the authoring source. `XFS_REQUIRE_ORACLES=1` turns oracle, game-integration and declared private-asset skips into failures for release runs.
- **PIPE-02, PIPE-16:** the independent verifier unbundles its own copy of the archive, checks every member hash, then runs its own WolvenKit serialize and texture export on those members and the plate inputs; it parses the `.archive.xl` structurally, re-hashes plate inputs at the end, and takes the morph target count from the plate recipe. The builder no longer writes the round trip or texture export.
- **PIPE-01:** the built-in plate is cut from the head the launch route loads (generic resolver plus `.xl` patches), with topology gates, provenance in the plate and package manifests, a cache key over that provenance, a named `plate_source_modded` stop and the `XFS_EYE_PLATE_HEAD=base-game` escape hatch.

## Fixed in claude/retire-legacy

- **UI-05:** one composition root, `src/studio-startup.ts`, started by the localhost entry `studio-main.ts` and by the desktop bootstrap (which now bundles `studio-startup.js` instead of `studio-main.js`). The sidebar shell (`main.ts`, `/legacy.html`, `style.css`, eleven `*-ui.ts` modules), `/port-smoke.html` and `/application-boundary-fixture.html` were removed; `tests/trusted-studio-bootstrap.test.ts`, `tests/studio-presentation.test.ts` and `tests/studio-application.test.ts` remain the page-free port fixtures. Legacy-only `preset.expand`/`collection.filesOpen`, the workspace `panels` block, draft `expanded`/`filesOpen` flags, `captureBrowserPanels`, `setupContextMenus` and the editors' keyword-cursor fallback went with it; stored workspaces with those fields still restore (fields ignored).
- **UI-06 (data attributes):** host differences reach the root as a typed `StudioHost` (storage, budget, preparation service, shared `LocalSetupActions`, setup place, flush and preview-ready callbacks); `tests/studio-ui-boundary.test.ts` fails if the root or entry reads `dataset`, `xfDesktop*` or `xfs-desktop*`. The desktop smoke report uses the ready callback, and the WolvenKit card's `xfs-host-setup-changed` window event became the card's `onSetupChanged` callback into the root. The WolvenKit consent and .NET card now run on both hosts through the shared `src/browser-preview-card.ts`; one `LocalSetupActions` instance is shared by Build setup, Game & tools and the card, and `idle()` lets a save wait for another view's refresh instead of being refused as busy. Boot and head-load failures show plain sentences; raw error text remains only after "The 3D preview couldn't be loaded:" for loader messages, which are already plain.
- **PREV-09 and the prepared-file path:** the core head, plate, eyes and maps come only from the derived game-file preview on both hosts. Removed: localhost prepared-file precedence and `XFS_PREVIEW_CORE=derived`, the desktop five-file intake (`desktop/asset-intake.ts`, marker, dialog, endpoint, trial scripts, `review-ready-assets.ts`), `tools/export_preview.py`, the unhashed prepared render record and the prepared-eyes notice. Render records must be `game-files` with hashes and morph sources. The preparation card (`src/browser-preview-card.ts`) is shared, so localhost with no cache offers preparation from the saved settings. Real-geometry tests read the derived head and skip with a reason when no preview is prepared.
