# XF Studio presentation boundary

## Current state (25 September 2026)

- **The only shell:** `public/index.html` → `src/studio-main.ts` (localhost) or the desktop bootstrap, both of which start `src/studio-startup.ts`, the one trusted composition root, with a typed `StudioHost` (workspace storage, the 3D preview preparation service, the settings service and where paths are set). It builds `createTrustedAuthoringCore`, `createTrustedPreviewServices`, `createTrustedStudioBootstrap` and injected browser devices, and mounts `src/studio-ui/` with **only** `StudioPresentationPort`. `tests/studio-ui-boundary.test.ts` enforces the import boundary and that the root reads no page globals or data attributes.
- **3D preview setup on the port (`claude/alpha-polish`, 25 September):** the first run to an interactive head is the DOM-free `PreviewSetupActions` service (`src/preview-setup.ts`), exposed as `StudioPresentationPort.previewSetup` (detached snapshot of the setup card, the WolvenKit consent and the head pane's next step; typed `previewSetup.*` actions in `PREVIEW_SETUP_DESCRIPTORS`). The card and consent are presentation (`src/studio-ui/preview-setup-card.ts`, token-styled in `studio.css`) and import only types from the core, so the former standalone device card `src/browser-preview-card.ts` is gone and **no boundary exception is needed**. The composition root supplies the host ports, the head loader and the workspace's automatic-start preference, and mirrors the service's head state into typed viewport phases (`loading`, `preparing`, `unavailable`, `error`). Both host polling ports share `src/host-state-poller.ts`. Settings merges and queued refreshes live in `LocalSetupActions` (`setup.update`, `requestRefresh()`), not in `studio-startup.ts` (code-health UI-21..UI-26).
- **Guidance (`claude/guidance`, 25 September):** tours, spotlights and the Help view are presentation modules in `src/studio-ui/guidance/`. The runner (`GuidanceService`) is DOM-free and owns only which step shows. Buttons dispatch ordinary typed actions through `StudioRuntime`, and the Esc handler yields to gestures, menus, dialogs and text fields. The only new port surface is `port.links` (named public pages the host opens). `tests/guidance.test.ts` adds a stricter boundary for the guidance folder (value imports only from the pure input-binding helpers and the mod-branding constant; runner and data free of DOM and I/O) on top of `tests/studio-ui-boundary.test.ts`. Each anchored control needs one `rt.anchors.register(...)` line. **No boundary exception is needed.** See [feature-module platform §6a](feature-module-platform.md#6a-guidance-tours-spotlights-and-help-v1-built).
- **Feature facades and view contributions (platform step 5, 26 September):** the port gains `features()` and `feature(id)`, typed facades over each feature's own actions (capability, per-target `contextCapability`, dispatch, limits and choices); eye makeup's carries the editor view the port's `editor` used to be, its form-control transactions and catalogues, and studio-ui reads them through `StudioRuntime.eyeMakeup`. The shell's panel catalogue, factory layouts, closed-panel homes, lazily repainted panels and activity sources derive from view contributions, whose panels keep their IDs so saved layouts restore; new features use `<feature>.<panel>` IDs, which the dock accepts. Eye makeup's view (its contribution, Layers, the inspectors and the UV panel) lives in `src/features/eye-makeup/view/` and acts only through its facade (`view/actions.ts`, UI-52). The view composition list is in `src/compose/views.ts` (the catalogue, data only) and `src/compose/view-panels.ts` (factories); the composition root hands `STUDIO_VIEW_COMPOSITION` to `mountStudio`, and studio-ui reads the composed catalogue from `rt.views`. studio-ui keeps only its own view (`views/shell.ts`), its panel factories and the catalogue machinery (`views/contribution.ts`). See [step 5 status](feature-module-platform.md#step-5-status).
- **Retired 25 September (`claude/retire-legacy`):** the sidebar shell (`src/main.ts`, `/legacy.html`, `style.css` and its eleven `*-ui.ts` control modules), the `/port-smoke.html` and `/application-boundary-fixture.html` fixture pages, and the legacy-only `preset.expand`/`collection.filesOpen` actions and sidebar/scroll workspace fields (older workspaces still restore; those fields are ignored). The replaceable-presentation guarantee is carried by `studio-ui` itself plus `tests/trusted-studio-bootstrap.test.ts`, `tests/studio-presentation.test.ts` and `tests/studio-application.test.ts`, which mount or drive the port with no page controls.
- **Overhaul:** delivered and merged 24 September (`95516b2`, portrait follow-up `038054e`); see the [delivery record](ui-overhaul-2026-09-24.md) and [follow-ups](../backlog/claude-ui-overhaul.md).
- **Core defects found by the overhaul audit — all fixed 25 September:** B-1 second collection open discarded the earlier unsaved draft (`f552ff6`, persisted four-draft recovery queue); B-2 layer rename/move/Undo rebuilt every texture and reset the selected point (`da76361`, `c6b2866`); B-3 a refused file action cleared the in-flight `busy` flag (`6c0e46f`).
- **Audit API gaps and adapter notes closed 25 September:** A-1, A-2 (consequence metadata), A-3, A-5, A-7, A-8, A-9, A-10, A-12, A-13, A-14 (revision part), file-workflow IDs in the registry, B-7, control transaction versus gesture, and unowned layer edits. See [Application API additions](#application-api-additions-25-september).
- **Tests:** 790 authoring tests (including the 61 desktop tests; 782 pass and 8 opt-in real-install tests skip) with type check and bundle build, 25 September on `claude/alpha-polish`. Earlier counts in the extraction history are stage-by-stage records.
- **Open boundary work:** see [Open work](#open-work) — remaining partial audit items, the context-menu/command-registry verification and the primary review of the API-extension commit.

The binding rules are in the [architecture contract](architecture-contract.md). This document records where the code actually stands against them.

## Presentation overhaul, 24 September

The redesigned interface is the default browser entry: `public/index.html` loads `src/studio-main.ts`, a trusted composition root built from `createTrustedAuthoringCore`, `createTrustedStudioBootstrap` and the injected browser devices, which mounts `src/studio-ui/` with **only** `StudioPresentationPort`. `tests/studio-ui-boundary.test.ts` fails if a presentation module imports a core value outside the allowlist below, imports the composition root, trusted, document or device modules, or uses storage, `fetch`, workers or Three; it also asserts that `studio-startup.ts` is the only importer of the presentation and passes it only the mounted port. [Delivery record and audit](ui-overhaul-2026-09-24.md).

Read-only port extensions added for repainting views (separate commit, no recipe/SQLite/material/compiler change): `port.editor` (cached detached editor view; since step 5 `port.feature("eye-makeup").view()`), `library.summary()`, `authoring.previewState()`, `authoring.finishCatalogue()`/`glitterModelCatalogue()` and `port.status` (autosave, asset diagnostics, Glitter measurements, adapter messages). Measured motivation: `authoring.snapshot()` ≈56 ms and `library.view()` ≈30 ms per call with six presets of 80 Undo entries, versus ≈0 ms for the cached view.

**Recorded exception — presentation value imports.** Owner: the presentation (studio-ui). `studio-ui` imports three pure helpers instead of receiving them through the port: `allowsNativeTextMenu` (context-menu policy), `effectiveTheme`/`recoverDockLayout` (ui-preferences) and `EYE_MAKEUP_MOD` (the frozen mod-branding constant, so the Mod package panel can name XF Eye Artistry before any Check has run). None holds state or performs I/O. They are presentation policy/constants and may stay while pure. The former `flake-field` entry was removed once `limitsFor` (A-7) published the state-dependent Glitter bounds, and the `uv-view` `uvAspect` entry once the UV canvas filled its panel and stopped needing a layout aspect. The allowlist lives in the boundary test and must not grow without an entry here.

Core defects B-1, B-2 and B-3, confirmed during the overhaul, were fixed on 25 September (see the summary above); reproductions are in the [delivery record](ui-overhaul-2026-09-24.md#bugs-and-interactionrestoration-defects).

## Application API additions (25 September)

All additive; no recipe schema, SQLite format, raster arithmetic, compiler or package behaviour changed. Tests: `tests/application-api-gaps.test.ts`, `tests/studio-ui-logic.test.ts`, `tests/glitter-measurements.test.ts` and the coordinator/UV-view/import-boundary tests.

| Audit item | Contract |
|---|---|
| Dependency direction | `PathCommand` lives in `bezier-path.ts`. `tests/architecture-import-boundary.test.ts` fails if any core module imports a `*-ui` module, `studio-ui/`, `context-menu` or a browser entry. |
| Glitter measurements | `GlitterMeasurements` owns freshness (optical/mask key at the current tier), the dense threshold and layer eligibility; both shells read it. `previewOpticalKey` is pure in `makeup-dependencies.ts`. |
| A-1 dirty state | `library.persistence()`: `savedRevision`, `baseline` (`none`/`unknown`/`known`), `dirty`, `dirtyPresets`, `structureDirty`. Baselines are recorded on save/open/initialize; a reloaded draft fetches its revision once. Unknown is reported, never guessed. |
| A-2 consequences | `authoring.consequences({action}|{file}|{request})`: what is replaced, durable writes, recoverable state pushed out of reach, the recovery path and whether to confirm. |
| A-3, A-9 async work | Accepted collection requests carry a `requestId` in progress and outcome. `files.activity()` is one list of file and collection work; `cancel` always refuses honestly because the local server cannot abort a started save or build. |
| A-5 coordinates and navigation | Recipe actions `point.move`, `point.insert`, `point.setTangent`, `shape.transform`, `field.setOrigin`, `field.setVector` (one labelled Undo each, parser-validated); `camera.navigate` (orbit/dolly/pan within the controls' limits); `viewport.uvNavigate` (pan/zoom). Empty-UV menus still offer no insertion because a hit carries no coordinate. |
| A-7 limits | `authoring.limitsFor(target, kind, variant)`: static ranges plus state-dependent bounds (Glitter flake size versus count, move positions, mode requirements) with units. |
| A-8 validation | Capability refusals carry `issue: {code, field, message}` (`required`, `name.blank`, `name.too-long`, `range`, `mode`, `format`); reason codes derive from it. |
| A-10 Undo/Redo | `history.redo` (named `recipe.redo` until step 5) and `authoring.history()` with labels. Redo is session-only and valid only while the recipe is what the last Undo/Redo produced. Cancelling a gesture or form transaction never creates Redo. Undo/Redo are refused while a transaction is open. `authoring.historyTimeline()` publishes the whole timeline (stable step IDs, labels, times, current index, redo count, trimmed flag) and `history.jumpTo {entryId}` moves to any listed step atomically under the same rules; the History panel uses only these. |
| A-12 readiness | `previewReadiness.layers`: per-layer `ready`/`updating`/`blocked`/`disabled` and texture size; unattributable queue work marks every enabled layer updating. |
| A-13 selection | `viewport.snapshot().uv.selection` says whether the selected point and warp are in view (either mirrored instance); `viewChanged()` publishes UV view changes. |
| A-14 revisions | The collection revision that binds open menus advances only on content/identity changes (`contentVersion()`), not on save progress or list refreshes. |
| File workflow IDs | `FILE_DESCRIPTORS`, `fileKinds()`, `fileDescriptors()` and a flat `registry()` over actions, requests, gestures and file workflows. |
| Input bindings | Pure `input-bindings.ts` catalogue shared by the gesture adapters, shell/viewport/tab/row key handlers, hints, cursors and labels. Studio-ui imports its pure helpers by value (allowlisted in `studio-ui-boundary.test.ts`: derivation and matching functions only, no state). `viewport.input()`/`subscribeInput()` is a separate read-only channel of held modifiers and adapter hover/gesture reports; `preferences` gains `inputHints.set`. See [input bindings](input-bindings.md). |
| Adapter notes | Gesture cancel accepts Escape with any modifier held and Ctrl/Cmd+Z in either case (`gesture.cancel` binding, via `gesture-cancel.ts`). Starting a gesture commits an open form transaction, and form edits are refused during a gesture. With a loaded collection and no selected preset, content actions, controls and gestures are refused with a reason. |

## Open work

1. **Legacy shell: retired** 25 September, with its fixture pages and legacy-only actions (see Current state). Startup wiring now exists once (`studio-startup.ts`; code-health UI-05), and desktop decisions reach it as a typed host object rather than data attributes (UI-06).
2. **B-17 Shift overload on the head: resolved** with option (b). Shift-drag on makeup used to rotate while Shift-drag off makeup fell through to camera pan, and Shift-wheel was always consumed. Now Shift always means a shape gesture in both viewports and does nothing off makeup, through the [input binding catalogue](input-bindings.md). The rejected options were (a) documenting the overload, (c) letting Shift-wheel over empty background zoom the camera and (d) moving shape gestures to another modifier or an explicit tool mode.
3. **Partial audit items.** A-4 typed adapter messages (severity/code), A-6 labels for every action and choice, A-11 per-layer export preview beyond the finish catalogue, A-15 typed per-asset records. A-14's disclosure move is moot: `preset.expand` and `collection.filesOpen` were removed with the legacy shell, and their stored fields are ignored on restore.
4. **Verify the context-menu and command-registry claims** end to end: every menu, palette, shortcut and button path must dispatch the same validated action and show the same disabled reason. Label coverage for every context candidate and activity-source coverage for every action ID are now tested; `actionsFor` remains representative, some domain refusals still rely on message-text reason codes, and non-piercing renderer asset choices still lack option ports.
5. **Primary review** of the read-only API extension commit `ebe6a1f` (and the one-line file-snapshot change) separately from presentation commits.
6. **Geometry view is type-level read-only only.** The detached `AuthoringGeometry` cache is updated in place during gestures and is not runtime-frozen; staleness relies on version publication and identity checks. Make adapter signatures read-only where helpers permit.
7. **Performance:** `CollectionService.capability()` re-validates the whole workspace including Undo histories (~27 ms).
8. **Device ownership stays concrete by design:** the Three scene, mask worker, canvases and editors are constructed by device adapters; `ViewportAdapter.detach()` is destructive teardown only, never a dock move; a hidden host should avoid unbounded rendering work. Async package cancellation is not supported by the server — show truthful busy/result/failure and retry after completion. Editor-change propagation into collection state is still coordinated through bootstrap wiring rather than one authoritative subscription.
9. **Removed exception — the view composition list lived in `studio-ui/`.** Its removal criterion was met on 26 September by the step-5 file moves: eye makeup's view is in `features/eye-makeup/view/`, the view list is beside the other composition lists in `compose/`, and the root hands it to `mountStudio`. The import-boundary tests keep studio-ui from reaching `compose/` or any feature, even transitively.
10. **Facade isolation is enforced (UI-52).** Eye makeup's view dispatches, asks capabilities (general and per target), limits and choices only through its facade (`features/eye-makeup/view/actions.ts` over `rt.eyeMakeup`; feedback is the shell's `rt.report`). Platform actions it offers (Add preset, the toast's Undo) go through the shell's runtime. `tests/studio-ui-boundary.test.ts` fails when a feature view uses `port.authoring` or dispatches one of its own kinds through the shell. Still shell code: the palette's eye-makeup entries (`layer.add` and the finish commands in `studio-ui/app.ts`, through `rt.addLayerCapability` and `rt.dispatch`), `KEY_BINDINGS`, the context menus in `target-menus.ts` (generic, through `contextFor`/`dispatchContext`), the guidance tours and control anchors that name eye makeup's panels, and the `Frame`/`rt.editor` reads of eye makeup's view in the shell. These move with commands, bindings, tours and help as view contributions. Views read a look's newer-version lock from the facade's explicit `locked()` (UI-53), never from an `unavailable` refusal.

## Strict boundary for a replaceable presentation

- Application/core: recipe and collection state, validation, Undo transactions, async collection coordination, workspace composition and preview request policy. It should publish immutable snapshots and typed actions with target-aware capabilities. It must not import DOM nodes, browser events, Three scene objects, raw worker messages or HTTP implementation details.
- Rendering and device adapters: Three scene, texture/canvas/worker resource ownership, picking and coordinate mapping, file picker/download and HTTP transport. These may use browser APIs but must present narrow typed ports to application policy. Keep mask-worker cancellation, complete-bundle publication and fairness exactly as currently tested.
- Replaceable UI: component tree, controls, menus, docking, theme, focus and layout. It may query snapshots/capabilities, dispatch actions and attach viewport ports. It must not mutate `Layer`/`Recipe`, call SQLite/store methods, infer package eligibility from finish labels or steer workers directly. The `studio-ui/` presentation, the only shell, obeys this through `StudioPresentationPort`.

## Extraction history (24 September 2026)

The extractions that made the presentation replaceable, in order. Test counts are as recorded at each stage. The sidebar/accordion markup, `main.ts`, the `*-ui.ts` controls, `/port-smoke.html` and the boundary fixture referred to below belong to the legacy UI, retired on 25 September; they were an implementation of that UI, not a design constraint.

**First extraction.** `authoring/src/collection-actions.ts` exposes typed in-process commands for preset add/copy/rename/move/remove/restore/select/expand; collection rename, open, undo-open and recipe import; and save-result reconciliation. The adapter returns a detached read-only-by-copy view, provides `capability(action)` with a disabled reason for known invalid targets, and can notify subscribers after successful commands. It calls the existing `CollectionSession`, so draft switching, per-preset editor memory, stable identities, recovery and in-flight save reconciliation still follow the same validated rules. `collection-ui.ts` now reaches these operations through `CollectionApplication`'s narrow view port.

`authoring/src/editor-actions.ts` provides typed `layer.edit` and `layer.setEnabled` operations over recipes, plus target-aware capability checks. The returned recipe is validated; its source is not mutated. `main.ts` routes layer-list actions, including visibility, through that path. `RecipeHistory` now owns the existing deduplicated, bounded 80-entry history and returns detached snapshots. Structural layer changes still trigger the original full slot replacement; visibility still redraws only its affected slot.

The second extraction adds `authoring/src/engines/layered-makeup/recipe-actions.ts`. It exposes target-aware typed actions and capability reasons for layer selection, point selection/removal, Bézier conversion/mode, warp selection/add/remove/clear/reach, pigment strength and edge softness, colour/opacity/symmetry/finish, and all current Glitter preview-model settings. It validates a proposed recipe before publishing and keeps untouched layer object identities so deferred per-layer renders are not lost if another layer changes before the next frame. The controller returns detached read-only snapshots and notifies subscribers after successful actions. The present UI adapters now dispatch these actions; a discrete action records one recipe Undo entry, while slider gestures still use their existing begin hooks so a continuous interaction produces one entry. Glitter model comparison memory remains workspace-only; only the selected model/settings enter the portable recipe.

The third extraction puts UV and on-head gesture proposals through `RecipeActions.applyGesture`. The adapters still perform input capture, picking, coordinate conversion, mirrored mapping and gesture bookkeeping, but they no longer write live recipe points, fields or whole shapes. Each proposed edit carries its layer identity (and point/field identity where relevant), is parsed against the current recipe schema before publication, and schedules only its affected layer. Existing gesture start/cancel hooks retain the one-step Undo and Escape behaviour. `ViewportAdapter` gives the two editors explicit attach/detach/resize/cancel/capture lifecycle; detach removes their listeners and render resources. `PreviewActions` provides DOM/Three-independent commands, detached camera/preference snapshots and capability reasons for FOV, front view, exposure, key light, surface guides, wireframe, normals, eye optics and brow/lash/hair visibility. Orbit/pan camera state is read through its injected port when snapshotted, including changes made by the renderer controls.

The fourth extraction adds `CollectionService` and an injected `CollectionTransport`. List/load/save/copy/export/import/package sequences, legacy-draft merge, immutable request snapshots, busy/progress/success/error state and capability reasons now live outside `collection-ui.ts`. The service preserves editor changes made while a save is in flight and refuses to reconcile a save response onto a different collection identity. Package checks/builds use the unsaved draft snapshot without writing SQLite. Current partial export keeps eligible Matte/Satin/Metallic content, reports omitted preview-only content with warnings, and returns `no_exportable_content` only when nothing eligible remains. The current view still reads a selected local file and initiates a download, but it no longer chooses request order, draft reconciliation or package result text. The HTTP adapter retains the current local endpoints and SQLite rules.

The fifth extraction adds `MotionActions`, `PreviewQualityActions` and `SavedAppearanceActions`. Motion restore, pause, head/facial contribution switches, blink commands and readable state are independent of the controls; the renderer's phase and animation implementation remain injected. Quality tier validation, selection and recoverable error state are similarly separate, while `main.ts` still owns worker cancellation, texture disposal and render ordering through an injected replacement callback. Saved-V parsing, validation, application and suggested eye selector value form one command; the view only selects a local file and paints the result. `PreviewActions` now also covers eye-shape and piercing visibility; every creator choice on the shown V, a piercing style included, is the character context's (`CharacterContextActions`, the `characterContext` family), whose large reads (`characterPanel()`, `characterView()`, `characterChoices()`) are frozen and shared rather than copied per paint. These services publish detached snapshots, subscriptions and capability reasons. No portable recipe or SQLite format changed.

The sixth extraction adds `AuthoringDocument` and `WorkspacePersistence`. The former is the live in-process owner of recipe, active layer/point, selected warp and Undo history; `main.ts` reads and publishes these through it instead of retaining parallel top-level variables. It provides change subscriptions, detached snapshots, validated atomic restore and the original bounded/deduplicated history. Gesture proposals still preserve live layer identity and explicitly publish their in-place change. The latter owns the 180 ms autosave, explicit activation after startup, page-hide/visibility flush, isolated verification key, and the protected-storage policy when restore failed. It receives an injected workspace capture and storage port. Workspace and recipe schemas remain unchanged.

The seventh extraction adds `AuthoringGestures` and `AuthoringRenderScheduler`. UV and on-head editors now hand begin/apply/commit/cancel events to one DOM-free gesture transaction service; it checkpoints once, validates the source and stable target before each proposal, and restores Undo only for an accepted edit in the still-current layer. The editors retain pointer capture, coordinate conversion, wheel-burst timing, and stale-context checks, and call commit on release or burst completion. `AuthoringDocument` publishes typed action/gesture rendering effects. The scheduler subscribes to those effects, coalesces active-layer and gesture renders by live layer identity, renders inactive/immediate changes directly, and provides explicit stack-order and active-first full rebuilds for structural changes and quality recovery. Its port still calls the unchanged renderer/raster work in `main.ts`; neither raster arithmetic nor worker cancellation changed.

The eighth extraction adds `AuthoringControlEdits`, `WorkspaceComposer` and a high-level preview policy. Colour, opacity, pigment, softness, warp reach and Glitter form controls now pass through typed begin/edit/commit/cancel transactions with one Undo per continuous gesture, empty-edit cleanup and stale-layer guards. `control-edit-ui.ts` translates pointer, key, change and blur events; the application service owns history. `WorkspaceComposer` builds a detached saved state from document, preview, motion, quality, collection and layout ports. Before preview assets finish loading, it preserves prior camera and scroll state while accepting current authoring edits; afterward it uses the typed preview/motion snapshots. Only the layout adapter reads panel DOM state. `planLayerPreview` exposes missing/recover/unavailable/request outcomes with active priority, optical-map need, disabled-placeholder release and capacity reason; `main.ts` still owns canvases, workers, cancellation and complete texture publication. No recipe, SQLite, compiler or raster arithmetic changed.

The ninth extraction adds `StudioApplication`, a single in-process, DOM-free facade over the existing document, recipe/layer, gesture/form, collection, preview, motion, quality and saved-appearance services. It publishes detached aggregate snapshots and subscriptions, exact action/request IDs, target-specific candidate actions with current capability reasons, typed dispatch outcomes, collection async outcomes, and an opaque gesture entry point that resolves live layer/point/field identity internally. Direct recipe dispatch records one Undo for content edits, while form controls use explicit begin/edit/commit/cancel; collection removal/open retain their separate recovery policies. Existing collection async operations and preset edit, layer finish/symmetry, point removal, Glitter model, quality and lighting controls now exercise the facade. Core validation and renderer ownership did not move. This is an incremental seam: other controls and both gesture adapters still call their older services directly.

The tenth extraction routes both UV and on-head editors' mutation proposals through `StudioApplication.beginGesture/applyGesture/endGesture`. The proposals contain values and target IDs, never the live `Layer`, point or field objects; the facade owns those identities in an opaque session, checks target continuity and exposes a queryable gesture capability. The adapters retain geometry reads, coordinate mapping, pointer capture, wheel timing and their existing stale-context guards. A compile-time complete registry now describes all 53 public top-level action IDs, nine collection request IDs and four gesture proposal IDs, with scope, payload fields, nested variants, Undo/recovery policy and async effect. The facade supplies structured reason codes for known target, busy, limit, mode, asset and readiness failures while preserving the underlying message. `application-boundary-fixture.html` is a minimal replacement presentation over the facade without the current sidebar, Three renderer, worker or browser storage. Build it with `bun build src/application-boundary-fixture.ts --outdir public/build --target browser`, then open `/application-boundary-fixture.html?verify=1` on the local authoring server. It is a contract fixture, not a proposed visual design.

The eleventh extraction adds `AuthoringGeometry`, a versioned detached view for both UV and on-head drawing, picking and stale-pointer checks. Repeated animation-frame reads return the cached view without cloning the live recipe. A validated in-place gesture refreshes only its affected layer and retains captured point/field/array identity; a recipe replacement or path-point array replacement invalidates the corresponding gesture context. The application facade still owns the live gesture references. `AuthoringLayerActions` now owns layer capability checks, the single Undo checkpoint and structural versus single-layer publication; `main.ts` supplies only a renderer-resource reset callback. `StudioApplication.contextCapability(target, action)` validates scope, hit-target match and descriptor payload before calling the domain capability, while `choicesFor` reports live capability for enumerated choices such as finishes, Glitter models and preview quality. The production Add/Duplicate layer and Remove point disabled states now query application capabilities instead of duplicating limits in `main.ts`. These queries remain advisory; dispatch and parser validation are authoritative.

These are internal commands, not a network or plugin execution interface. The collection/layer action IDs are `preset.edit`, `preset.select`, `preset.expand`, `collection.rename`, `collection.filesOpen`, `collection.open`, `collection.undoOpen`, `collection.importRecipe`, `collection.saved`, `layer.edit`, and `layer.setEnabled`. Recipe action IDs are `layer.select`, `point.select`, `point.remove`, `path.edit`, `field.select`, `field.add`, `field.remove`, `field.clear`, `field.setReach`, `pigment.edit`, `softness.edit`, `layer.setColor`, `layer.setOpacity`, `layer.setSymmetry`, `layer.setFinish`, `glitter.selectModel`, `glitter.setClassic`, `glitter.setIrregular`, and `glitter.setDirect`. Collection, layer and recipe capability checks are synchronous; validation remains authoritative at dispatch. A future menu may query capabilities, then dispatch the same command. A stale target after the query is still rejected at dispatch.

`AuthoringDocument.replaceRecipe` also atomically validates a structural or Undo replacement and clamps its selected layer and point. This fixed an orphan selection in the replacement-presentation fixture after Add layer → Undo; the fixture now returns to four layers, six active points and 0.85 opacity.

The twelfth extraction adds `AuthoringPresentation`, a narrow read-only TypeScript port over cached detached geometry and primitive selection state. The production layer list, property controls, warp selectors, recipe download and verification diagnostics now read that port; those UI paths no longer receive a mutable live recipe. UV/head coordinate adapters keep their detached identity-stable geometry view for pointer capture. Switching collections now calls the document's atomic `restore` and resets renderer resources, instead of individually assigning history, field selection, recipe and selected point in `main.ts`. The render/worker and application services still use the trusted live document internally. Type-level readonly protects new UI code at compilation, while the detached cache is intentionally updated in place during gestures and therefore is not runtime-frozen. This closes the live-recipe read path for current production control adapters, not the whole shell boundary.

The thirteenth extraction adds `AuthoringPreviewCoordinator` with a typed `PreviewRenderPort`. Quality assessment and tier ownership, active-layer request priority, disabled-slot release, capacity failure, recovery rebuild, stack reset, stale-size/slot publication gating and Ready/Updating/error status decisions now live in a DOM-free application service. `AuthoringRenderScheduler` calls this coordinator, and quality controls read its assessment/status. `main.ts` still constructs the worker and Three scene, owns canvas/optical texture publication and implements the concrete port. A quality switch cancels pending raster work and disposes old canvases before an active-first rebuild; a structural stack reset uses stack order. `RasterClient` still validates the complete result bundle against the request/version and never publishes cancelled or partial work. The coordinator adds a tier/slot check before the device adapter sees a result. Raster arithmetic, fairness, compiler and recipe formats did not change.

The fourteenth extraction adds `StudioFileOperations`, a DOM-free file and package workflow service with a typed `StudioFilePort`. It exposes import/export capabilities and outcomes for recipe, collection and saved-V data, delegates mask baking to a renderer port, and retains detached package check/build results, operation progress, error codes and collection-open recovery capability. The browser adapter still owns hidden file inputs, blob download and the one-off mask worker/canvas. The service delegates collection validation, partial-package eligibility, SQLite revision handling and recovery to the existing `CollectionService` and application callbacks; it does not duplicate those domain rules. Production controls now call the file service instead of reading files or downloading JSON themselves. The state snapshot stores only outcome metadata for exports, avoiding an extra retained copy of large JSON. Portable formats and export quality remain unchanged.

**Context-target slice (integrated between the fourteenth and fifteenth).** An independently integrated context-target slice adds `StudioApplication.contextQuery`, `boundActionCapability` and `dispatchContext`. A device/DOM adapter supplies a hit for empty UV, shape, point, tangent, warp field, layer, preset or collection; the application binds it to collection/preset and geometry revision, offers only current actions with live capability reasons, and rechecks the binding and final domain parser at dispatch. This is action discovery **after** a hit, not geometric picking or a menu UI. Empty UV honestly offers no standalone edit command. The combined main branch has 310 passing authoring tests, type check and build.

The fifteenth extraction adds `CollectionApplication`, a DOM-free composition service that constructs and owns `CollectionService`, injects transport, and bridges the trusted `AuthoringDocument` export/restore operations. The replaceable `collection-ui.ts` view receives a narrow `CollectionViewPort` with detached collection state, subscriptions, typed commands and capabilities, and file-operation results; it no longer constructs the service or receives editor read/show callbacks. The composition root initializes the service and refreshes the view once the restored collection is ready. Conflict reconciliation, draft recovery, preset switching, unsaved-draft package snapshots and SQLite rules stay in their existing services.

The sixteenth extraction adds `ViewportAttachment`, a typed layout-facing port for the existing head and UV hosts. A replacement layout can attach or rehost either same DOM host, request a resize, cancel input, subscribe to phase changes, and read detached readiness/error, dimensions, capture and camera/UV view state without importing Three, workers or the authoring document. Rehost cancels the relevant gesture before moving; it never invokes `ViewportAdapter.detach()`, which remains a destructive editor-disposal path. A layout must call `resize` after any further sizing/reordering following rehost. `createScene` now skips zero-size device resizes and retains a finite camera aspect while hidden; a deferred initial/front framing request is applied when the host first becomes visible unless a saved camera has been restored. The current shell still provides the browser host-move and renderer/editor device callbacks. This is an attachment contract, not a dock UI or new geometry policy.

The seventeenth extraction adds read-only `ViewportAttachment.contextAt(kind,x,y)`. The UV adapter reuses its existing handle priority and painted-shape test, returning canonical point/field IDs, tangent side, mirrored instance and an honest empty-UV hit. The on-head adapter reuses its existing anchored/projected handle checks and plate/head/eye occlusion before returning the same stable identities; background or occluded surfaces return no hit. The attachment sends the hit to `StudioApplication.contextQuery`, which binds it to the current collection, preset and geometry revision and supplies existing actions/capabilities. Hit queries never select a point, checkpoint Undo or initiate a gesture. Right-click remains a pan input on UV, and native text editing menus remain available. No second geometry evaluator or menu UI was introduced.

The eighteenth extraction closes four current presentation gaps. Undo (`history.undo`, named `recipe.undo` until step 5) has live application capability and dispatch, with the existing atomic recipe replacement/resource reset retained behind the composition callback; the current shortcut/button and replacement fixture now use that action. `ViewportAttachment.uvCommand` exposes Both, Single, Other and Fit through the retained UV editor, preserving its view persistence, wheel zoom and right-pan. Saved-V import passes the already-applied eye-shape suggestion through `StudioApplication.recordAppliedSavedAppearance`, updating selection without applying geometry a second time. Piercing style/material labels are detached through `StudioApplication.snapshot().previewOptions`, using the same renderer-provided choices for dispatch validation; choices remain preview-only. No portable recipe, package or scene material semantics changed.

The nineteenth extraction adds `StudioPresentationPort` and `createStudioPresentation` as a narrow composite entry point for a replacement shell. It wraps only public methods from the application, collection, file, viewport and UI preference services; the shell receives no `AuthoringDocument`, live recipe reference, Three object, worker, transport or SQLite handle. Its aggregate snapshot and individual read methods are detached and typed read-only. Current action/context/gesture transactions, library requests, file/package outcomes, safe same-host viewport rehosting, UV view commands and system/light/dark preference actions remain independently callable. `main.ts` constructs the port after trusted services are wired; in `?verify=1` it exposes the port for isolated acceptance without changing the current controls. `UIPreferenceActions` is now constructed from the restored workspace, included in `WorkspaceComposer` capture and subscribed to persistence, so a future theme/layout action survives reload. A focused fixture uses only the composite port after setup to exercise layer and preset edits, Undo, UV commands and stale context, preview quality, recipe download, package Check result and theme preference. The dock engine must implement theme styling, layout semantics and on-screen recovery using the existing versioned preference contract; this port intentionally supplies no dock-tree policy.

The twentieth extraction moves authoring construction into `createTrustedAuthoringCore` and collection/file/presentation assembly into `createTrustedStudioBootstrap`. The latter accepts device ports and a presentation mount callback; it does not read legacy control IDs or import `main.ts`. Production `main.ts` uses both factories, while a separate test constructs and mounts the complete presentation port with no DOM controls and exercises add/Undo, recipe export and theme choice. Trusted service handles remain inside the bootstrap return object for the composition root; only the narrow `StudioPresentationPort` is passed to the mount callback. This is a real boot path for a replacement UI with its own device adapters, not a copied legacy UI entry point. The complete authoring suite passed 323 tests, type check and bundle build after providing the same three ignored local GLB fixtures.

**Browser file/workspace devices (after the twentieth).** The browser file/workspace extraction moves picker, download and one-off mask-worker mechanics into `browser-file-device.ts`, and workspace restoration, panel capture, autosave event wiring and write transport into `browser-workspace-device.ts`. Both receive their elements and state ports from the shell and contain no legacy control IDs. The existing `main.ts` still supplies its current input/detail/panel elements, so current behavior and storage keys remain stable. Contract tests exercise selection/cancel, verification-key isolation, activation/ready capture and ID-free panel geometry; the full authoring suite passed 326 tests, type check and bundle build after supplying the three ignored local GLB fixtures. Browser file-picker automation remains unverified in this stage.

The twenty-first and twenty-second extractions provide injected browser device adapters for head/UV host construction and rehosting, file picker/download, workspace storage/autosave, mask workers, generated textures and preview quality transitions. Device adapters retain browser, Three and worker lifetimes; the application coordinator retains request/cancellation/publication policy. Isolated browser checks reached Ready with the actual head and expanded plate and completed 1K→512 preview quality replacement without errors. These checks still used the existing presentation.

The twenty-third extraction moves saved-V, preview and motion service construction and restoration behind `createTrustedPreviewServices` and `createBrowserScenePreviewPorts`. Restored saved appearance applies before optics, eye shape and optional details; motion restores before neutral-space camera state. The old controls paint detached snapshots afterward. An isolated browser check reached Ready with paused idle, stationary head/facial motion and saved 42° FOV. The integrated branch passes 329 tests, type check and build. The last gate, an alternate browser entry mounting only `StudioPresentationPort`, then passed through `/port-smoke.html` ([port acceptance](ui-port-acceptance-2026-09-24.md)). Current coordinate drags remain owned by mounted editors; they are not arbitrary coordinate commands.

## Opus handoff gate assessment, 24 September (historical)

This gate passed and the overhaul was delivered. It is kept as the record of what the handoff relied on.

| Brief prerequisite | Current evidence | Gate |
| --- | --- | --- |
| UI-independent application state, commands, Undo, async work | `StudioApplication` aggregates detached snapshots, subscriptions, typed command/request routing and contextual checks. `CollectionApplication` owns trusted collection construction. A second browser entry mounts only the composite presentation port and completes real edits and async library/package tasks. The production shell remains a composition root. | **Pass for current workflows** |
| Owned recipe, raster, SQLite, save and export semantics | Schemas and evaluators unchanged; the combined tests, typecheck and bundle build pass. Partial package export uses explicit omission warnings. Preview tier replacement and file/package workflows run through typed application ports without moving device ownership. | **Pass for this checkpoint** |
| Viewport lifecycle and geometry-safe commands | `ViewportAttachment` exposes same-host reparenting, resize/cancel, phase/error, capture, detached view state and read-only UV/head hit queries. The independent entry mounted the real head/UV editors and completed pointer edits with Undo. Picking and camera orbit/pan remain concrete adapters; Opus supplies the dock layout. | **Pass for handoff** |
| Replaceable presentation boundary, inventory and full action catalogue | `/port-smoke.html?verify=1` boots trusted core and browser devices independently of `main.ts` or its control IDs, mounts only `StudioPresentationPort`, and reads typed actions, file/library state, context hits and aggregate preview readiness. File workflow IDs remain a separate documented contract. | **Pass for current workflows** |
| Handoff acceptance baseline | The [combined production pass](ui-handoff-acceptance-result-2026-09-24.md) covers dense/empty presets, reorder, worker recovery and package output. The [independent-port pass](ui-port-acceptance-2026-09-24.md) covers real head/UV edits with Undo, reload, library conflict/recovery, unsaved Matte/Glitter Check/Build hash and omissions, plus typed Updating→Ready at 1K/2K. Native saved-V file selection and live Escape were blocked by browser automation; focused tests and earlier evidence cover their contracts. | **Pass with recorded automation limits** |
| Exact Claude Opus 5.5 xhigh harness | Initially logged out; later on 24 September authenticated and validated with `claude-opus-5-5` at xhigh, then used for the delivered overhaul. | **Pass (later on 24 September)** |

The action families are aggregated by `StudioApplication`, while `CollectionActions`/`CollectionService`, `RecipeActions`, `PreviewActions`, `MotionActions`, `PreviewQualityActions` and `SavedAppearanceActions` retain their domain rules. The executable registry now records IDs, fields, scope, variants, Undo and async category for every implemented command. It is a static schema, not a substitute for domain validation or renderer-provided option lists. The facade does not yet expose a complete target-specific example and disabled reason for every parameterized action.

At handoff, Opus received the independent entry, typed action/file catalogue, composite port and versioned workspace preference boundary. The viewport attachment/picking interface delivered read-only hits; current editor gestures stay inside its stable adapter. Higher-level coordinate actions and renderer asset choices are later extensions. The last package result can outlive a subsequent draft edit and must be labelled historical or checked for freshness. Do not make game rendering, hypothetical future menu choices or unsupported package cancellation a prerequisite.

## Verification

Current: see **Tests** under Current state. The overhaul's own acceptance (24/24 isolated `?verify=1` checks) is in the [delivery record](ui-overhaul-2026-09-24.md#acceptance--isolated-verify1-disposable-library-throwaway-chrome-profile).

**Stage-by-stage record (historical).** At the seventeenth extraction the full authoring suite passed (318 tests), along with TypeScript check and browser bundle build. Contract tests cover document snapshot isolation, atomic restore, Undo, persistence policy, gesture/form transaction grouping and cancellation, stale-target safety, workspace composition, layer render ordering and preview request policy. Facade tests cover detached snapshots, nonselected target capabilities, complete descriptor coverage, structured known failure codes, one-step layer Undo, form and opaque gesture transactions, stale gesture rejection and async collection outcomes. The new contracts cover cached/detached geometry, in-place gesture identity, path replacement invalidation, layer transactions and concrete target/payload/choice capabilities. Isolated `?verify=1` browser checks verified a UV edit and one Undo, curve insertion and one Undo, opacity change/Undo, opacity survival across reload with Undo history, and on-head point drag followed by one Undo. Stage 9 also verified preset duplication, finish change/Undo, quality change to 1K, async library refresh and reload persistence. Stage 10 repeated UV and on-head drag/Undo after moving both mutation paths to opaque sessions. Its standalone fixture exercised preset duplication, layer addition, point removal, transactional opacity/Undo, preview-quality selection and an async collection refresh without the production sidebar or renderer. Stage 11 repeated UV and on-head drag/Undo after detached geometry routing, and checked Add layer → Undo in the replacement-presentation fixture with coherent active point and opacity state. Stage 12 checked layer selection and property painting, opacity Undo, two-way preset switching with restored active layer, and UV/on-head point drag with one Undo each after routing production controls through the read port. Stage 13 contract tests cover quality-switch cancellation order, recovery, capacity error, disabled placeholders, stale-result rejection and resource-aware status; isolated `?verify=1` browser checks passed rapid 2K→512 switching, Ready status, UV drag/Undo and on-head drag/Undo. Stage 14 contract tests cover recipe/collection/saved-V acquisition, mask and JSON downloads, detached package results, partial omissions, unsaved-draft snapshot and collection-open recovery. Isolated `?verify=1` browser checks completed recipe and mask downloads, no-exportable-content error, a partial check retaining Matte while omitting Glitter, and collection export. Stage 15 contract tests cover detached collection state, editor restoration during preset switching, unsaved-draft package snapshots, conflict retention and undo-open recovery. In two isolated browser tabs, saving one revision made the other tab's save report a conflict without discarding its draft; refresh exposed the newer revision, and undo-open restored the stale draft after opening it. Stage 16 lifecycle tests cover targeted gesture cancellation, same-host rehosting, detached view state and hidden-size guards. In the isolated browser, moving the head host into a zero-size hidden slot retained its finite camera aspect and original WebGL canvas; moving it into a visible slot updated aspect from measured dimensions. UV view state and canvas identity survived a separate move. After restoring both hosts, UV and on-head point drags each accepted and each Undo returned to the original point. Stage 17 tests exercise mirrored canonical hits, tangent-over-point priority, warp origin/vector classification, head/eye occlusion, shape interior and revision-stale bound options. In isolated `?verify=1`, UV/head point, mirrored point, warp, shape and empty UV queries returned stable IDs and the current geometry revision without selection or Undo changes. Right-click did not edit; right-drag UV pan still worked, and native text context menus remained unblocked. The isolated worktree initially lacked ignored preview PNGs; after copying those local fixtures from HQ without tracking them, the head and expanded plate loaded and the on-head edit could be checked. This is browser behavior, not game-rendering proof. Hair and piercing preview assets remain unavailable in that worktree; saved-V import was not exercised in this stage.

