# First eye-makeup game smoke: offline preflight

25 September 2026. **No game or MO2 launch, profile switch, save change, or installation was performed for this check.** This is a source/file preflight for one future controlled session, not evidence that ArchiveXL registered the selector or that a material rendered.

## Exact current state

The existing diagnostic MO2 profile `XF Studio diagnostic 2026-09-25` is present but `ModOrganizer.ini` still selects `2025 (again)`. Neither MO2 nor Cyberpunk was running at inspection. The diagnostic modlist SHA-256 is `3b63b84fe9fcecbae282a1aef5a9c9f86b55578ca7ff3eb79e7424bea3e538ef`; the untouched source-profile modlist remains `64edf00004f8a283a173671d70390d7b16d22cd64da92af7d05e66682537ab4b`. The diagnostic profile has 920 enabled entries among 1,003 listed mods and enables the dedicated XF Studio mod and versioned ArchiveXL/TweakXL/Codeware folders while disabling their three old entries. Its `LocalSaves=false` setting means this profile **does not isolate saves**. Use a disposable test save or a verified backup; never overwrite Nathan's only copy.

The promoted four-preset package is the private candidate ending `1790252959113814200`, **not** the older similarly named package directories. Its source collection hash is `fce9e6107706f81e6a7d9ae28842baa207e5794bde38149cd9d3790de98066ad`. The enabled MO2 files exactly match its manifest: `xfs_c0ec3546e3fac43e78c19a65d20383d41.archive` is 847,872 bytes, SHA-256 `8965de41a484b2bd853b6bb12dc78daf7ddead97b89742a4ff93039cd889fcb8`; the 286-byte `.archive.xl` is `5789485c2578d5f990a5de6a1e6dc54f89727686f530055c5e4ccb920ae848cb`. Its existing independent verifier reports four presets and 16 exact unpacked resources. The manifest's `installed:false` describes its original build output and was not rewritten when a copy was promoted to MO2.

All 44 files in the three versioned framework folders still match the private install manifest by length and SHA-256: ArchiveXL 1.27.3 (28), TweakXL 1.11.4 (7), Codeware 1.20.5 (9). The physical RED4ext DLL still reports 1.30.0 and hashes to `59309a78a9606481d13f87e8fbf9ea9c0b735115389ded0295537180c9e52511`. For the package's exact archive/XL filenames and the three framework DLL paths, a read-only search over currently enabled MO2 folders and the direct game tree found one provider per path and no exact-path competing provider. This cannot establish winners *inside* archives, ArchiveXL merge behavior, or what the next launch actually loads. Fresh logs must establish those facts. No new framework release lookup was performed; this verifies the already selected local packages, not whether newer releases exist.

## Finish coverage: current candidate versus prepared alternative

The promoted four-preset candidate covers **Metallic, Matte and Off**, but **not Satin**. Its only Satin layer, `Inner light`, is disabled in `Verification — metallic copy`; the other three looks use Matte. The user-facing finish name must not be inferred from a disabled layer or the compiler's general Satin capability.

An ignored **separate candidate** now permits a single-session Matte/Metallic/Satin comparison once it is deliberately staged. `experiments/005-preset-collection/make_finish_smoke_fixture.py` derives a five-preset copy from the checked-in sample without editing a Studio draft/library. It gives the copy a new collection namespace and adds `Verification - satin control`, where only the original `Inner light` Satin layer is active. Preflight accepted all five presets with zero omissions. The local package builder and independent verifier passed: five presets, 19 exact unpacked resources, archive SHA-256 `c44ee9eac3342bdab94504cd3db6531be590e6b502d2dcdd843693767c012afa` (860,160 bytes), `.archive.xl` SHA-256 `b846f57136c153a33677be1db6389dcea3e2f10ff71e1b34ff26065ef2ac5637` (286 bytes). The fixture copy (`projects/xf-studio/authoring/data/runtime-preflight/satin.collection.json`) and package (`projects/xf-studio/dist/xfs_c6c9d0bebb6d454a98937b1b5b470796c-1790281763198477800/`, build tree beside it under `projects/xf-studio/build/`) remain **ignored and local**; they were moved from the original worktree into the main checkout on 25 September; no profile contains the new namespace, and `installed:false` / `gameRenderingVerified:false` remain accurate. The fixture file's current raw SHA-256 is `647e6fc1edf6abea8a7e33663afab456c91bf7dace989805c5b17fa648966d5f`; checkout line endings may change that raw source hash on reproduction, so always use the manifest of the actual candidate under test.

The new Satin layer covers 11,484 1024² source texels; its covered roughness bytes are 97 with metalness 0. The Matte controls cover the same count at roughness 224/metalness 0, while the Metallic control covers 11,670 at roughness 69/metalness 166. Thus the new variant is a nonempty, distinct compiled material input. These are byte-level compiler outputs, **not** proof of distinct perceived finishes under game lighting.

For a **single** five-look selector test, review a fresh, separately named diagnostic profile and dedicated mod that enables only the new namespace's archive/XL pair, with the existing `XF Studio` mod disabled there. Do not simply enable both collections and misread two selectors as one. Re-run the read-only exact-path/provider/hash checks after any staging. The existing promoted profile, authored library, source collection and save files must remain intact. Until that separate staging is complete, use the already promoted four-look candidate for Matte/Metallic/Off and defer Satin rather than claiming it was tested.

## One controlled session card

1. **Before launch:** record the selected MO2 profile, executable route, game-file version, exact candidate manifest/hash, enabled modlist hash and expected preset IDs. Verify effective MO2 origins if the UI exposes them; the offline folder scan is not a substitute. Make a recoverable test save and note its slot/path because profile-local saves are off. Capture a baseline with `python tools/capture_session.py --label xfs-before --profile "<diagnostic profile name>"`. The helper copies existing logs, which may predate this session, and never launches or selects a profile.
2. **Load gate:** launch through the deliberately selected MO2 profile/recorded executable. Record fresh game, RED4ext, ArchiveXL, TweakXL, Codeware, redscript and CET versions/logs, the selector's display label and its options. If the selector fails to register, capture the expansion/error logs and stop material conclusions; offline archive verification did not prove registration.
3. **Selector/clearing:** choose Off, the Metallic control, one Matte look, Satin control **only if the five-look candidate was staged**, then Off. Run Matte A → Matte B → Off and B → A → Off; reselect after clearing. Confirm exactly one complete look is visible and Off removes all makeup geometry/colour. Record displayed names and the source preset IDs, not just list positions (indices can change).
4. **Appearance:** hold the same V, head pose, camera/FOV and light where possible. Capture lossless frontal and three-quarter views at face and close-eye scales for each tested finish and Off; add a side view for surface attachment and eyelid clearance. Compare under a second useful light, plus a short zoom/mip sweep and default idle/blink. Distinguish finish response from shape coverage, plate contact, brow/lash cards and image exposure. This fixture cannot test Shimmer, Glitter, Glossy or Colour-shifting.
5. **Persistence:** leave and re-enter customization, then reload the disposable save; record whether the chosen selector identity and look persist. Do not treat one customization screen cycle as save-proof. Note any old Eye Artistry selector, duplicate XF selector or archive collision separately.
6. **After:** capture `python tools/capture_session.py --label xfs-after --profile "<diagnostic profile name>"` and screenshot filenames/times. Compare log timestamps/headers and hashes against the before capture; do not label old logs as new runtime evidence. Record what visibly rendered separately from log registration and offline verification. Restore the original MO2 profile when finished; do not remove a staged mod with an unreviewed save dependency.

For the elevated brow/lash question, use the same fixed V/style/pose and capture neutral front plus three-quarter frames of the Arkhe brow and Soft Natural lash options with their exact names, active XLs, fresh loaded-resource logs and lighting/camera notes. A matched Off comparison is useful where the game offers it. The browser preview's source candidates are still ambiguous for one brown-lash gradient; a game screenshot alone cannot prove which archive supplied it. See [brow/lash evidence](../eye-artistry/brown-liquorice-profile-overlap.md) and the broader [validation card](../../docs/validation.md#prepared-single-session-test-card).

No one dependency appears missing by exact installed-path check. Remaining pre-launch decisions are which candidate/profile to use, confirming the effective virtual origins, choosing the MO2 executable and safeguarding a test save. Remaining runtime unknowns are actual framework load, ArchiveXL selector expansion, resource winners, finish appearance, clearing, deformation and persistence.
