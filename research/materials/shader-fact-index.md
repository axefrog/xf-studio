# Shader fact index: which Studio code relies on which engine fact

The [materials and shader study](../backlog/materials-shader-re.md) asks for this index so that a corrected fact reaches every adapter that relies on it. Each row names one engine fact, its grade and where it is documented, then the preview material (browser) and the export compiler or verifier that depend on it. Paths are under `projects/xf-studio/authoring/src/`; "verify" means `features/eye-makeup/verify/`, "engine" means `engines/layered-makeup/`.

**When a fact changes:** update its source page, then every file in its row, then the tests those files name. Rows marked **changed in this study** are the ones the [skin](shader-skin.md), [hair](shader-hair.md), [eye](shader-eye.md), [decal](shader-decal.md) and [multilayered](shader-multilayered.md) references confirmed or corrected on 26 September 2026. The [metal and glass](shader-metal-glass.md) rows were added on 27 September 2026; no Studio code relies on them yet.

Grades: **[observed]** compiled 2.31 program or installed resource; **[source-supported]**; **[hypothesis]**.

## G-buffer and lighting (all families)

| Fact | Grade | Documented in | Preview | Export / verify |
|---|---|---|---|---|
| GBuffer0 stores `sqrt(linear albedo)`; `post_gbuffer` decals blend colour in that square-root space | [observed] | [materials §2.2, §2.4](../../knowledge/materials-and-shaders.md#24-what-a-post_gbuffer-decal-does-to-the-pixel-under-it) | `face-decal-material.ts` (`gbufferColour`, `forwardDecal`), `engine/render/plate-blend.ts`, `engine/render/plate-composite.ts`, `brow-material.ts`, `hair-colour-model.ts` (brow blend) | `engine/preset-compiler.ts` (√colour premultiplied, √coverage in alpha), `engine/flat-mip-chain.ts`, `engine/route-mip-chains.ts`; restated independently in `verify/texture-checks.ts` |
| A decal never changes the pixel's lighting class, profile slot or `.w` payloads: makeup on skin is lit as skin | [observed] | [materials §2.4](../../knowledge/materials-and-shaders.md#24-what-a-post_gbuffer-decal-does-to-the-pixel-under-it) | `face-decal-material.ts` and `engine/render/plate-blend.ts` light decals with `patchSkinLight` from `skin-material.ts`; `decal-underlay.ts` supplies the skin under each decal vertex | `engine/finish-export.ts` (finish calibration against skin) |
| Dielectric F0 fixed at 0.04; GGX with α = r²; renormalised Burley diffuse (the Eye class uses Lambert instead: [eye §6.1](shader-eye.md#61-direct-light)) | [observed] | [materials §2.3](../../knowledge/materials-and-shaders.md#23-the-deferred-lighting-model) | `skin-material.ts` (`LIGHT`), `eye-material.ts` (`xfsEyeBRDF`, `eyeDirectLight`), `studio-environment.ts` | `engine/finish-export.ts` (roughness values per finish) |
| Subsurface specular = two GGX lobes at `r·roughness0`, `r·roughness1`, × (1 + `lobeMix`)/2 from the pixel's `.sp` slot | [observed] | [skin §6.1](shader-skin.md#61-direct-light) | `skin-material.ts` (`skinLobes`), profiles carried by `render-detail.ts` (`RenderSkinProfile`) from `character-detail-service.ts` | — |
| SSS is a screen-space separable blur of **irradiance**, with albedo applied after (post-scatter); per-channel strength `P` lerps unblurred → blurred; specular unblurred. The blur rejects only non-Subsurface and unlit (red 0) neighbours (no depth test), renormalises per channel, and taps whole pixels. **Changed in this study** (mechanism decoded; blur rules on 27 September) | [observed] | [skin §6.3.1](shader-skin.md#631-the-separable-blur-gpu), [§6.3.5](shader-skin.md#635-combine) | `skin-material.ts` approximates it with a per-channel wrap from `falloff`/`blurSize`: **a stand-in, not the mechanism**; the port is planned in [skin §11](shader-skin.md#11-preview-port-plan-screen-space-scatter-in-threejs) | — |
| The SSS kernel is Jimenez et al.'s reference kernel over d'Eon and Luebke's skin profile (five Gaussians, red weights for all channels): `falloff` (sRGB-decoded, ≥ 0.009) sets the per-channel width, `blurSize` scales the offsets only, `diffuse` (sRGB-decoded) is the strength `P`. Tables of 25/17/11 samples, chosen by `SubsurfaceScatteringQuality` 2/1/0; a 511-sample table feeds the stochastic blur. **New in this study** | [observed] in the 2.31 executable; the match to the reference code [source-supported] | [skin §6.3.2–6.3.4](shader-skin.md#632-the-kernel-table-cpu) | Not yet used: `skin-material.ts`'s wrap reads `falloff`/`blurSize` byte/255 undecoded; the planned `skin-scatter-kernel.ts` ([skin §11](shader-skin.md#11-preview-port-plan-screen-space-scatter-in-threejs)) | — |
| SSS screen radius = `x · blurSize · w · cb0[3].x / (100 · depth)` pixels, with `w` a render-settings float × 1/6 | [observed] formula; `w · cb0[3].x` [hypothesis] (parity fit pending) | [skin §6.3.3](shader-skin.md#633-quality-radius-and-scale) | Planned port assumes one kernel unit = `blurSize` mm until fitted | — |
| The SSS combine's extra term is `SubsurfaceSpecularTint` (0.21, 0.26, 0.29) blended with a luminance-preserving copy by `SubsurfaceSpecularTintWeight` 0.3; `cb6[0]` is 0 outside debug views, so albedo is plain GBuffer0². **New in this study** | [observed] values; the term's input (probably reflections) [hypothesis] | [skin §6.3.5](shader-skin.md#635-combine) | Not modelled (Three's image-based light is untinted) | — |
| A pixel whose metalness exceeds 0.1 skips SSS entirely | [observed] | [materials §2.3](../../knowledge/materials-and-shaders.md#23-the-deferred-lighting-model), [skin §6.3](shader-skin.md#63-blur-kernel-and-combine), [decal §6](shader-decal.md#6-what-the-pixel-under-a-decal-ends-up-with) | `skin-material.ts` (wrap off above 0.1), used by `engine/render/plate-blend.ts` on the blended metalness | `engine/finish-export.ts`: Metallic at 0.65 crosses it at 15 % coverage; Colour-shifting's `FRESNEL_SURFACE` metalness is 0.08 so it never crosses it (0.25 crossed it at 40 % linear coverage until [decal §10](shader-decal.md#10-recommended-changes-ranked) rank 2 was applied) |
| Skin GBuffer2.z (`0.4 + 0.6·vertexColour.G`) weights a **sun transmission** term in the `UseTranslucency` SSS setup; the global and local light read GBuffer2.z only for Foliage. **Changed in this study** (was "reaches no lighting") | [observed]; variant selection [hypothesis] | [skin §6.4](shader-skin.md#64-translucency-the-usetranslucency-setup) | Not drawn (no adapter relies on it) | Decal surface coverage blends it toward 1/3, removing transmission under the decal; negligible on eyelids and lips (thick geometry) |
| Hair lighting class: Karis R/TRT/multiple-scatter model; sun intensities `cb0[12]` (`GlobalLight/*`), local-light intensities `cb0[13]` (`LocalLight/*` 0.35, 0.8, 0.47) with the same shape registers; every hair option's default and register read from the executable. **Changed in this study** (local-light path decoded; pairing and defaults from the executable, 27 September) | [observed] | [hair §6.2, §6.4](shader-hair.md#64-option-values-and-registers-executable) | `hair-shading.ts` (`xfsHairDirect`, `HAIR_LOCAL_LIGHT`), `hair-colour-model.ts` (`HAIR_LIGHTING_VANILLA` with the `LocalLight` triple, graded [observed]; `hairDirectLight`); tests `hair-colour-model.test.ts`, `hair-shading.test.ts` | — |
| Hair environment path: the hair model once for `L_e = V` minus its along-strand part, × 2π × diffuse irradiance E; `EnvProbe/R`, `/TRT` lobes widened by `AdditionalAreaRoughness` 0.1; ambient diffuse × C × albedo (albedo twice). In the program read (`m_shaderLightIntegrate_NoEnvProbes`), E is the global six-colour ambient cube (`GlobalShaderConsts` 21–26) taken along `L_e`. **New in this study** (27 September; E identified the same day, [creator lighting §10.3](../../knowledge/creator-lighting.md#103-how-the-lighting-integrate-pass-adds-indirect-light)) | [observed]; technique name [hypothesis, strongly supported] | [hair §6.5](shader-hair.md#65-environment-path) | `hair-shading.ts` (`HAIR_LIGHT_GLSL`: the model for `L_e` with the `EnvProbe` triple and `AdditionalAreaRoughness`, lit by the scene's irradiance along `L_e`), `hair-colour-model.ts` (`hairEnvironmentLight`, the `env*` values); tests `hair-colour-model.test.ts`, `hair-shading.test.ts` | — |
| Local-light per-light hair factor applies only under `Hair/Debug/DebugSwitch1` (false), so vanilla ignores the CPU's hack curve. **New in this study** | [observed] | [hair §6.2](shader-hair.md#62-local-lights) | Not modelled (correct) | — |
| Character creator and mirror lights are all local; SDR display transform and grading LUT | [observed]/[source-supported] | [creator lighting](../../knowledge/creator-lighting.md) | `creator-lighting.ts`, `grading-lut.ts`, `linear-display.ts` | — |
| Lighting-integrate variants: `m_shaderLightIntegrate` (probes: per-probe reflections and six-colour cubes, global-cube and world-map fallback), `_NoEnvProbes` (= `_CubeIBL`; one global six-colour cube in `GlobalShaderConsts` 21–26, along the normal for diffuse and the bent reflection for specular), `_NoAmbient` (no cube). The program earlier studies call "the ambient composite" (`10393055107398307099`) is `_NoEnvProbes`. **New** (27 September) | [observed] programs; names by descriptor layout [hypothesis, strongly supported]; which variant a render target uses [hypothesis] | [creator lighting §10.3](../../knowledge/creator-lighting.md#103-how-the-lighting-integrate-pass-adds-indirect-light) | `studio-environment.ts` (the studio stage's PMREM room stands in for probes); the creator preset's optional uniform cube ([creator lighting §11](../../knowledge/creator-lighting.md#11-an-environment-for-the-creator-preset-recommended)) | — |

## Resource model and textures

| Fact | Grade | Documented in | Preview | Export / verify |
|---|---|---|---|---|
| A template is identified by its own `CMaterialTemplate.name`, not its depot path | [observed] | [materials §3.1](../../knowledge/materials-and-shaders.md#31-where-they-are-and-how-templates-map-to-programs) | `render-templates.ts`, `character-material-adapters.ts` | `verify/resource-checks.ts` |
| Leaf-wins `.mi` chain over template defaults | [source-supported] | [materials §1.3](../../knowledge/materials-and-shaders.md#13-how-an-instance-chain-resolves) | `material-template.ts` (template defaults), `skin-material.ts` (`SKIN_TEMPLATE_DEFAULTS`), resolver output in `character-detail-service.ts` | `package-resources.ts` (instance values) |
| Whether a texture is sRGB-decoded follows the XBM's `isGamma` | [observed] | [materials §5](../../knowledge/materials-and-shaders.md#5-texture-conventions) | `character-detail-loader.ts` and `character-material-adapters.ts` (`textureColourSpace`), `skin-material.ts`, `hair-shading.ts` | `engine/preset-compiler.ts`, `package-resource-builder.ts`, `verify/resource-checks.ts` |
| Normal maps are RG with Z reconstructed; blue ignored; no Y flip in the programs | [observed] | [materials §5](../../knowledge/materials-and-shaders.md#5-texture-conventions), [skin §4](shader-skin.md#4-texture-packing-and-colour-spaces) | `skin-material.ts` (`xfsUnpackRG`), `face-decal-material.ts`, `eye-material.ts`, `layered-material.ts` | `engine/preset-compiler.ts` (facet normals), `engine/route-mip-chains.ts` |
| Mesh-local material entries, `@` template entries and `*` soft paths (ArchiveXL) | [source-supported] | [materials §1.4](../../knowledge/materials-and-shaders.md#14-mesh-side-binding) | — | `package-resources.ts`, `verify/resource-checks.ts` |

## Skin (`skin.mt`)

| Fact | Grade | Documented in | Preview | Export / verify |
|---|---|---|---|---|
| Roughness **R** base, **G** = metalness slot (and wet non-porosity), **B** detail mask; bias `saturate(R·(1 + B·(lerp(lo, hi, f) − 1)))`. **Confirmed in this study** | [observed] | [skin §4, §5.2](shader-skin.md#52-micro-term-roughness-and-cavity) | `skin-material.ts` (`skinRoughness`, `metalnessFactor = R.y`), `decal-underlay.ts` (skin roughness and metalness under decals) | `engine/finish-export.ts` (finish roughness calibrated against skin ≈ 0.6 under the lids) |
| Tone: `w = \|TintScale\|·Mask.R`, multiply (+) or overlay (−); `TintColor` encoding byte/255 | [observed]; encoding [hypothesis] | [skin §5.3](shader-skin.md#53-tone) | `skin-material.ts` (`tintChannel`, `SKIN_TINT_ENCODING`), `decal-underlay.ts` via `skinBaseTexels` | — |
| Detail and micro normals use slope-sum (partial-derivative) blending; micro atlas halves at `(1.4 + b)` and `(1.0 + b)`; micro normal lerps right→left, micro term left→right. **Confirmed in this study** | [observed] | [skin §5.1–5.2](shader-skin.md#51-normal) | `skin-material.ts` (`xfsBlendNormal`, `SURFACE`) | — |
| Secondary albedo is multiplied by the **toned base**, weighted by `w·SecondaryAlbedoTintColorInfluence` | [observed] | [skin §5.4](shader-skin.md#54-blood-flow-and-secondary-albedo) | `skin-material.ts` (`skinBaseColour`) | — |
| Wrinkle normals and blood flow are driven per vertex by UV-rectangle regions × animation float tracks, zero at rest. **New in this study** | [observed]; track source [hypothesis] | [skin §5.6](shader-skin.md#56-the-wrinkle-driver-vertex-program) | `skin-material.ts` leaves them undrawn (faithful at rest); an animated-wrinkle feature would need the regions | — |
| Wetness darkens porous and glosses smooth skin (`MaterialModifiersConsts[3]`). **New in this study** | [observed] | [skin §5.7](shader-skin.md#57-wetness-rain) | Not drawn (faithful when dry) | Test cards must record weather |
| Head and body share one skin model and tone chain; the body has no skin type | [observed] | [skin §3.2](shader-skin.md#32-body-one-skin-model) | `skin-material.ts` for body parts (body track) | — |

## Decals (`mesh_decal` family, the export route)

The evidence for every row is the [decal reference](shader-decal.md).

| Fact | Grade | Documented in | Preview | Export / verify |
|---|---|---|---|---|
| Colour and surface coverage = contrast-adjusted alpha **squared** × secondary mask, squared after filtering; normal alpha is the **raw** colour-map alpha (or `NormalAlphaTex.R`); target alphas default 0 and are not saturated in the program. The gradient-recolour, multitinted and morph templates use **linear** coverage. **Confirmed in this study** (every template read) | [observed]; blend clamp above 1 [hypothesis] | [decal §4.3, §2.1](shader-decal.md#43-coverage) | `face-decal-material.ts` (`decalCoverage`, `decalTargets`), `engine/render/plate-blend.ts` | `engine/preset-compiler.ts`, `engine/flat-mip-chain.ts`, `package-resources.ts` (target alphas); `verify/texture-checks.ts` |
| Roughness and metalness are separate R maps × scale + bias, and **share one blend alpha with GBuffer2.z**: a decal cannot write roughness without also blending metalness. **New in this study** (shared alpha) | [observed] | [decal §4.6](shader-decal.md#46-roughness-and-metalness) | `face-decal-material.ts`, `engine/render/plate-blend.ts` | `engine/finish-export.ts` (`flatSurface`), `engine/preset-compiler.ts`, `verify/resource-checks.ts` |
| `NormalsBlendingMode` 1: reoriented normal mapping with the G-buffer normal as base; alpha × `saturate(50 − 50z)` (full at 11.5°); the **encoded** normal is lerped, so a faded facet keeps about alpha × its tilt; `NormalsBlendingModeAlpha` below 1 lerps towards a **mirrored** decal normal, so it must stay white. **Changed in this study** (tilt crush, mirror) | [observed] | [decal §4.5, §9.2](shader-decal.md#45-normal) | `face-decal-material.ts`, `engine/render/plate-blend.ts` and `plate-composite.ts` (`MODE1_FULL_TILT`), `engine/route-mip-chains.ts` (`previewFacetChains`) | `engine/finish-export.ts` (faceted route), `engine/preset-compiler.ts` (mode 1, alpha texture left white), `engine/route-mip-chains.ts`, `glitter-route.ts`, `package-resources.ts` |
| `mesh_decal` transforms texture UVs by `UVScale`/`UVOffset`/`UVRotation` (half-turns) around 0.5, then folds with `sign·frac`; the gradient-recolour, multitinted and emissive-subsurface templates do not; **the double-diffuse template does**. **Changed in this study** (double diffuse, rotation units) | [observed] | [decal §4.2](shader-decal.md#42-uv-transform-and-flipbook) | `face-decal-material.ts` | `engine/plate-uv-window.ts` (its 1/64 margin keeps the plate off the fold's jumps), `engine/finish-export.ts` (`ROUTE_UV_WINDOW`); independently `verify/uv-window.ts` |
| The Fresnel colour `FresnelColor·intensity·w·saturate(\|1 − N·V\|^exponent)` is added before the square root; `w` is a camera-distance fade from a vertex program shared byte for byte by `mesh_decal_blendable` and `…_gradientmap_recolor_blendable`. The gradient one has linear coverage and no UV transform; **`mesh_decal_blendable` has squared coverage, the UV transform and per-texel base colour**. **Changed in this study** | [observed] | [decal §5.2](shader-decal.md#52-the-fresnel-colour-mesh_decal_blendable-_gradientmap_recolor_blendable), [colour-shift design](finish-designs/colour-shifting.md) | `engine/render/fresnel-tint.ts`, `engine/render/plate-blend.ts` | `engine/finish-export.ts` (`fresnelMaterial`, `FRESNEL_FADE`; a move to `mesh_decal_blendable` is [decal §10](shader-decal.md#10-recommended-changes-ranked) rank 4), `engine/preset-compiler.ts` |
| Double diffuse: coverage `(p + (1 − p)·s·I)²`; colour = powder term + `saturate(GradientMapIntensity · gradient)` × texture RGB, so intensity 2 clips gradient channels above 0.5; `DepthThreshold` default 0; plain program `7624081209775720613` (the often-cited `8834363738920290566` is its Discarded twin); gradient sampler clamps (last texel at `GradientMapUV` 1); secondary RGB unread; normal frame T, cross(N, T)·w. **Changed in this study** (clip, program identity; sampler, frame from [experiment 025](../../experiments/025-brow-facts/README.md)) | [observed] | [decal §5.1](shader-decal.md#51-mesh_decal_double_diffuse-brows-36-lip-styles), [eyebrows §3](../../knowledge/brows.md#3-material-and-colour) | `brow-material.ts` (clamps; fixed roughness 0.8, no normal write), `face-decal-material.ts` (`doubleDiffuseCoverage`) | — (no brow export yet) |
| "Discarded" variants are the plain program plus a per-draw temporal-dither dissolve (`MaterialModifiersConsts[0].z/.w`), not an alpha test. **New in this study** | [observed]; which draws use it [hypothesis] | [decal §2.3](shader-decal.md#23-what-the-discarded-variants-are) | Not modelled (correct for a fully shown part) | — |
| `mesh_decal_emissive_subsurface`: colour = `EmissiveEV × EmissiveColor` (a plain multiplier: **EV 0 is black**), opacity = `MaterialModifiersConsts[2].x` × mask, no UV transform. **New in this study** | [observed]; the modifier's value on the player [hypothesis] | [decal §5.4](shader-decal.md#54-emissive-decals) | — | `glitter-route.ts` and experiment 021's accent (`EmissiveEV` 1), `export-diagnostics.ts` and `verify/resource-checks.ts` (both refuse an accent at 0 or below), `verify/glitter-checks.ts` |
| `mesh_decal_wet_character` writes surface alpha 1 and scrolls V with time (rain streaks) | [observed] | [decal §5.5](shader-decal.md#55-other-variants) | — | Rejected route ([glossy design](finish-designs/glossy.md)) |
| No specular antialiasing; the mip chain is the only filter a mod controls; the mode-1 gate reads the filtered z | [observed] | [glitter in game §1](../../knowledge/glitter-in-game.md#1-from-flake-texture-to-screen-pixel), [decal §7](shader-decal.md#7-alpha-coverage-and-mips) | `engine/route-mip-chains.ts` | `engine/flat-mip-chain.ts`, `engine/route-mip-chains.ts`, `glitter-route.ts`; `verify/glitter-checks.ts` |
| The vanilla eye-makeup, lip, freckle and pimple meshes sit 0.40 mm off the head along its normals, morphs included (brows 0.35 mm, scars 0.31 mm, facial tattoos about 0.2 mm) | [observed] | [materials §2.4](../../knowledge/materials-and-shaders.md#24-what-a-post_gbuffer-decal-does-to-the-pixel-under-it) | Face decal meshes carry their own lift | `plate-lift.ts`, `package-resource-builder.ts`, `export-diagnostics.ts`; `verify/plate-geometry.ts` |
| Vanilla calibration: eye makeup 0.50 at full surface weight with ×30 noise coverage; matte lipstick roughness 1.0 at 0.35–0.4; glossy lipstick keeps the skin's roughness (+ metalness 1 at 0.05); gold and silver blush metalness 1 at roughness ≈ 0.51 if read raw (≈ 0.22 if the sampler decodes the sRGB-flagged texture; open). **New in this study** (as a set) | [observed] values | [decal §3](shader-decal.md#3-instance-chains-on-the-player) | — | `engine/finish-export.ts` (`FINISH_EXPORT` values) |
| Draw order by `materialPriority` first; within a priority unknown | [observed] / [hypothesis] | [head CC rendering §3](../../knowledge/head-cc-rendering.md#3-the-head-decal-family) | `render-templates.ts`, `character-detail-plan.ts`, `platform/scene/character-renderer.ts` | `package-resources.ts` |

## Hair (`hair.mt`, `.hp`)

| Fact | Grade | Documented in | Preview | Export / verify |
|---|---|---|---|---|
| Truncated profile lookup, overlay with root-to-tip as base, vertex-red shadow, `\|c\|` | [observed] | [hair §3.3](shader-hair.md#33-colour) | `hair-colour-model.ts`, `hair-shading.ts` | — |
| `.hp` bake: stops sorted and rescaled to span 0–1, sampled at `t = k/N`, 8-bit interpolation truncated, exact sRGB decode; texel 0 = N. **Changed in this study** (read from the executable, 27 September; was a fitted hypothesis) | [observed] | [hair §7](shader-hair.md#7-hp-profiles-and-their-resolution) | `hair-colour-model.ts` (`rescaledStops`, `bakeHairProfileBytes`, `bakeHairProfile`, `sampleStopsEncoded`), `hair-shading.ts` (`sampleHairGradient`, `hairProfileTexture`); `tools/hair-profile-swatch-fit.py` (the decoded-bake hypothesis); tests `hair-colour-model.test.ts`, `hair-shading.test.ts` | — |
| Dithered coverage with a shared per-pixel threshold (nested), remap by `AlphaCutoff`; 3-deep k-buffer, near-opaque (> 0.98) eviction | [observed] | [hair §3.1](shader-hair.md#31-hair_alpha_accum-2782105832921211528) | `hair-colour-model.ts` (`HAIR_DITHER`, `hairResolvedCoverage`), `hair-shading.ts` (alpha-to-coverage; no k-buffer) | — |
| Strand direction from `Flow` (sRGB) along bitangent/tangent | [observed] | [hair §5](shader-hair.md#5-strand-direction-flow-and-roughness) | `hair-shading.ts` | — |
| Lashes: constant ID (`grey.xbm`, R 107/255, linear) and root-to-tip (`white.xbm`) placeholders, so ID sample 52 and root-to-tip sample 126 set the lash colour: `brown_liquorice` base (172, 130, 15), Alliekat (59, 28, 0) | [observed] | [hair §9](shader-hair.md#9-lashes) | `hair-colour-model.ts`, `hair-shading.ts`, the generic `hair-strand` adapter in `character-material-adapters.ts` | — |
| `Scattering` and `VertexColorStrength` have no effect on direct light | [observed] | [hair §6.3](shader-hair.md#63-what-the-light-does-not-read) | Not used (correct) | — |
| Cap: `mesh_decal_gradientmap_recolor` indexes a per-colour gradient by an ID map, square-root blend, linear unsaturated coverage. **Changed in this study** (plain-template program `5232451138945967528` read) | [observed] | [hair §8](shader-hair.md#8-the-scalp-cap), [decal §5.3](shader-decal.md#53-the-gradient-recolour-trio) | `character-material-adapters.ts` (`hair-cap-decal`), linear blend (approximation) | — |

## Eyes (`eye.mt`, `eye_gradient.mt`, `eye_shadow.mt`)

The eye tests are `projects/xf-studio/authoring/tests/eye-material.test.ts`, `eye-shading.test.ts` (ranks 4–5 against the reference's formulas), `webgl-eye.test.ts` (the GPU probe) and `render-detail-record.test.ts` beside them.

| Fact | Grade | Documented in | Preview | Export / verify |
|---|---|---|---|---|
| A chunk's role comes from its template (`eye`/`eye_gradient` eyeball, `eye_shadow` shell, `multilayered` design), never its index: the male mesh swaps chunks 1 and 2 | [observed] | [eye §3](shader-eye.md#3-instance-chains-on-the-player) | `render-templates.ts`, `character-material-adapters.ts` (`eyeball`, `eyeShell`, `layered`) | — |
| Raw U > 0 selects the "Right" parameters, which drive the character's **left** eye; colour, normal and mask at `(fold(u), 1 − v)` outside the iris; roughness at the raw UV, not refracted. **Confirmed in this study** (side naming new) | [observed] | [eye §5.1, §4](shader-eye.md#51-side-and-fold) | `eye-material.ts` (`EYE_SURFACE`, `eyeSampleCoordinates`, `eyeColourCoordinate`, `xfsEyeTurn` per side); geometry keeps raw UV0 (`render-detail.ts`, `game-asset-export.ts`) | — |
| Iris colour = `lerp(Albedo, atlas(mask.R, row), mask.A)` in linear light; the row is the engine's index for `IrisColorGradient`; the atlas is built on the CPU | [observed] read side; bake [hypothesis] | [eye §5.6](shader-eye.md#56-colour-and-the-gradient-lookup) | `eye-material.ts` (`bakeGradientRamp`, `gradientTexture`, `irisBaseColour`), `render-detail.ts` (`gradients`), `render-templates.ts` | — |
| `IrisMask` and the Rebecca normal map are gamma-flagged data; both argue for raw sampling. **Changed in this study** (second resource) | [hypothesis] | [eye §4](shader-eye.md#4-texture-packing-and-colour-spaces) | `eye-material.ts` (`IRIS_MASK_ENCODING`), `character-material-adapters.ts` (`eyeball`: the same switch for `IrisMask` and `Normal`) | — |
| Roughness = `RoughnessScale · R` (sclera 0.050, iris 0.155) | [observed] | [eye §5.7](shader-eye.md#57-roughness-and-metalness) | `eye-material.ts` (`roughnessmap_fragment`, `xfsEyeRough`), **on by default** (off: the earlier flat `EYE_FLAT_ROUGHNESS` 0.18); `character-renderer.ts`, `workspace-state.ts` (`eyeOwnRoughness`) | — |
| Refraction is near-pure parallax (index ratio 0.97; 1° of bending at 30°); the iris is a remap 1.8 mm under a virtual 15.2 mm sphere. **New in this study** (magnitudes) | [observed] arithmetic | [eye §5.3](shader-eye.md#53-eye-frame-refraction-and-parallax) | `eye-material.ts` (`EYE_GLSL_FUNCTIONS` `xfsEyeIrisPlane`, `irisPlaneCoordinate`, `EYE_TEMPLATE_DEFAULTS`) | — |
| Two normals: N2 (iris relief, refracted) for diffuse and the sun cut; N1 (analytic cornea bulge up to +25°, bubble on the sclera) for specular and ambient | [observed] | [eye §5.4–5.5, §6](shader-eye.md#6-lighting-the-eye-class) | `eye-material.ts` (`EYE_SURFACE`, `xfsEyeCornea`, `corneaNormal`; N1 as Three's shading normal); `character-material-adapters.ts` (`Normal`, `NormalBubble`) | — |
| Eye-class light: Lambert on N2, GGX on N1, visibility `0.25/((N1·V + N2·L)(1 − α/2) + α)`, exp2 Fresnel, no N·L; sun shadow zeroed where `sunDir·N2 < 1e-4` | [observed] | [eye §6.1–6.2](shader-eye.md#61-direct-light) | `eye-material.ts` (`RE_Direct_XfsEye`, `xfsEyeBRDF`, `eyeLightsBeginChunk`: the cut on Three's sun and directional lights); `studio-environment.ts`, `creator-lighting.ts` supply the lights | — |
| Eye ambient × (1 + `cb6[9].w`), AO bypass by `cb6[9].z`; eyes are outside SSS and get no rain wetness. **New in this study** | [observed]; option pairing [hypothesis] | [eye §6.3–6.4](shader-eye.md#63-ambient-probes) | `eye-material.ts` (`EYE_AMBIENT_BOOST` 0.1 on the indirect light; no AO map); `skin-material.ts`'s SSS wrap is correctly not applied to eyes | — |
| The per-eye engine vectors turned by `EyeHorizAngle*`: the preview takes the eyes' shared forward and the pupil-to-pupil lateral from the eyeball mesh (vanilla pupils 3.59° outward = the circle designs' ±3.6), turning outward | [resource] measurements; identity and sign [hypothesis] | [eye rendering §2.2](../../knowledge/eye-rendering.md#22-arithmetic-step-by-step) | `eye-material.ts` (`eyeAxes`, `prepareEyeballGeometry`, `EYE_AXIS_TURN`), `character-detail-loader.ts`, `platform/scene/head-rig.ts`, `skin.ts` (`skinMatrix`) | — |
| `S = T2 × A` runs against the exported mesh's V (the literal iris is mirrored and the limbus blend crosses the pupil); in game the iris is not mirrored | [observed] arithmetic; [wiki] image; cause [hypothesis] | [eye §5.3](shader-eye.md#53-eye-frame-refraction-and-parallax) | `eye-material.ts` (`IRIS_PLANE_ORIENTATION` "as-mesh", `ensureEyeTangents`) | — |
| The "waxy eye" is the sphere-normal diffuse, a too-rough (0.18) and too-bright lobe, and no parallax, not SSS. **New in this study** | [hypothesis] from the comparison | [eye §9](shader-eye.md#9-the-waxy-eye-preview-artefact) | `eye-material.ts` (ranks 4–5, built: the fix awaits test asks 10–12) | — |
| The morph's `baseTexture` can replace `Normal` (vanilla: flat; ArchiveXL's fix copy: the material's own) | [observed] resources; binding [hypothesis] | [eye rendering §1.3](../../knowledge/eye-rendering.md#13-the-morph-resource-can-replace-the-eyes-normal-map) | `resource-graph.ts` (`baseTexture`, `baseTextureParam`), `character-detail-plan.ts` | — |
| `eye_shadow` is a forward `One/SrcAlpha` shell: Mask R shadow to the mean of `ShadowColor^2.2`, G/B wet highlight, no Fresnel, no environment, fog-scaled | [observed] | [eye §7](shader-eye.md#7-the-wetness-shell-eye_shadowmt), [materials §4.4](../../knowledge/materials-and-shaders.md#44-eye_shadowmt--eye_shadow_blendablemt--the-eyes-occlusion-and-tear-shell-not-cosmetic-eyeshadow) | `eye-material.ts` (`createEyeShellMaterial`, `shellAlpha`, `shellSpecular`); render order 99 in `platform/scene/character-renderer.ts` | — |
| `eye_shadow_blendable` has the same pixel program and no MeshSkinned compilation. **New in this study** | [observed] | [eye §7.1](shader-eye.md#71-eye_shadow_blendablemt) | — | A glossy-makeup shell must use `eye_shadow` (materials open question 6) |

## Layered materials

The evidence is the [multilayered reference](shader-multilayered.md) and its [line-level evidence](multilayered-shader-evidence.md).

| Fact | Grade | Documented in | Preview | Export / verify |
|---|---|---|---|---|
| Multilayered: per-pixel front-to-back coverage shares, levels as clamped scale/bias, colour mask from the roughness map; no refraction or Eye light on layered eyes | [observed]; CPU mappings [hypothesis] | [multilayered §5](shader-multilayered.md#5-the-g-buffer-program) | `layered-material.ts`, `layered-setup.ts` | — |
| Every multilayered G-buffer pass is opaque, depth-writing, in the depth prepass and Standard class; the only transparency is a whole-draw dissolve, so on a face plate it would replace the skin. **New in this study** | [observed] | [multilayered §3, §8](shader-multilayered.md#8-what-this-means-for-makeup-on-a-face-plate) | `layered-material.ts` (an opaque Standard part) | The XF Eye Artistry export stays on the decal family (`engine/finish-export.ts`) |
| Layer textures use `SampleGrad`; normal and microblend gradients × `NormalsTextureDDXYMultiplier` / `MicroblendsTextureDDXYMultiplier` (default 1), a per-material mip bias. **New in this study** | [observed] | [multilayered §5.4](shader-multilayered.md#54-sampling-explicit-gradients-with-two-lod-multipliers) | Not honoured: the bake samples base levels and Three builds mips (correct at 1) | — |
| Clear coat: the coat amount of layers in `[CoatLayerMin, CoatLayerMax]` is stored in GBuffer2.z; the `unlit` pass (depth `Equal`) adds a coat lobe weighted by `saturate((z − 1/3)·1.5) × Opacity`, dual-source. **New in this study** | [observed] | [multilayered §6](shader-multilayered.md#6-the-clear-coat-multilayered_clear_coat) | Not drawn (`render-templates.ts` has no clear-coat adapter) | — |
| `multilayered_baked` reads a virtual-texture surface cache filled by the same layer arithmetic. **New in this study** | [observed]; switch-over [hypothesis] | [multilayered §7](shader-multilayered.md#7-the-baked-surface-cache-multilayered_baked) | One bake covers both paths | — |

## Metal and glass

The evidence is the [metal and glass reference](shader-metal-glass.md). Neither template has a preview adapter yet (`render-templates.ts` does not list them, so their chunks stay hidden); the Preview column names the code a new adapter would change.

| Fact | Grade | Documented in | Preview | Export / verify |
|---|---|---|---|---|
| `metal_base`: colour `saturate(BaseColor × BaseColorScale)`; metalness and roughness each the **R** of their own map through `saturate(x·scale + bias)`; RG normal, Z reconstructed, XY × `NormalStrength`; UV × `LayerTile` except `Emissive`; plain Standard G-buffer | [observed] | [metal and glass §3.1, §3.3](shader-metal-glass.md#33-the-g-buffer-program-observed) | Not drawn; a new `metal-base` adapter in `character-material-adapters.ts` and `render-templates.ts` (ranked plan §7) | — |
| `metal_base` Discarded variant: `discard` where `BaseColor.a < AlphaThreshold` (0.38) when `MaterialModifiersConsts[0].x ≠ 0`; the cyberware decal chunks set `enableMask` 1 | [observed]; `enableMask` → flag [hypothesis] | [metal and glass §3.3](shader-metal-glass.md#33-the-g-buffer-program-observed) | Not drawn; would be Three's `alphaTest` | — |
| `metal_base` emission: only when `EmissiveEV > 0`; colour lerps toward `EmissiveColor × BaseColor`, GBuffer2.w = flag + `sqrt(EV/10)`; the composite's per-frame scale | [observed]; `2^EV` [hypothesis] | [metal and glass §3.6](shader-metal-glass.md#36-emission-observed) | Not drawn (no character instance uses it) | — |
| `glass_onesided` writes no G-buffer: forward `transparent` pass, `out = radiance + background × T` (dual source), `T = lerp(1, tint′·(1 − mask), Opacity)`, no Fresnel on transmission | [observed] | [metal and glass §4.3](shader-metal-glass.md#43-the-transparent-program-observed) | Not drawn; needs a two-pass forward adapter (WebGL has no dual-source blend) | — |
| Glass F0 from `FresnelBias` (0.25 / **0.08** / 0.04 at 0 / 1 / 2); `IOR` only moves the distortion; roughness `saturate(GlassRoughnessBias + R)`; Karis analytic environment BRDF; **sun-only** direct light, no local lights | [observed] | [metal and glass §4.3](shader-metal-glass.md#43-the-transparent-program-observed) | Not drawn | — |
| Gorilla Arms glass: `GlassSpecularColor` black, so no reflection; tint (240, 235, 228), `IOR` 1.32, `BlurRadius` 1 | [observed, resource] | [metal and glass §5](shader-metal-glass.md#5-what-the-character-assets-set-observed-resource) | Not drawn | — |
| The creator box has no probe, baked GI, area environment, fog or bounce in either world. The walls are beyond the render target's 9 m depth cut. Only a world-wide ambient cube remains, bounded to about 4 % of the key-lit forehead by the session-2 mirror frames. So the creator preset's `scene.environment = null` is the right default: metals show only spot highlights, and glass reflects nothing under the rig. **Changed** (27 September; was "what the creator scene's probes hold is unknown") | [observed, resource]; the global cube's value [hypothesis], bounded [runtime] | [creator lighting §10–11](../../knowledge/creator-lighting.md#10-indirect-light-what-reaches-v), [metal and glass §6](shader-metal-glass.md#6-how-far-the-preview-is) | `lighting-preset-stage.ts` (keep null; add the optional uniform cube `a`, default 0), `studio-environment.ts`; affects `layered-material.ts` metals | — |

Related: [skin reference](shader-skin.md) · [hair reference](shader-hair.md) · [eye reference](shader-eye.md) · [decal reference](shader-decal.md) · [multilayered reference](shader-multilayered.md) · [metal and glass reference](shader-metal-glass.md) · [materials and shaders](../../knowledge/materials-and-shaders.md) · [backlog](../backlog/materials-shader-re.md).
