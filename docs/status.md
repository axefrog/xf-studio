# Current state — 2026-09-25

**Expanded plate sampled packed pass, 25 September:** A localized correction now passes the unchanged independent gate on actual WolvenKit mesh+morph readback: zero new finite contacts or neighbor violations across 107 static cases, 73 selected poses and 664 dense idle frames, with exact native skin bytes in both serialized buffers. The narrowest sampled edge slack is +0.539 µm. The private candidate remains experimental pending continuous-motion and combined-scene visual checks; it has not entered preview, package or game. [Reproducible packed evidence](../experiments/012-native-plate-bootstrap/README.md#local-packed-resource-candidate-sampled-gate-passes--25-september).

**Saved brow/lash source follow-up, 25 September:** A 1,161-archive selected-MO2-route scan still finds unique installed Arkhe Fuller brow and icxrus lash app providers, but it also finds an enabled Alliekat `brown_liquorice.hp` candidate alongside the vanilla profile pinned by Studio. Their gradient inputs differ substantially. The resolver does not yet model base-versus-mod precedence, and no runtime binding or game colour has been observed, so production tint remains unchanged. Two copies of the brow double-diffuse base template are byte-identical, excluding that duplicate as a default-parameter difference. [Source hashes, comparisons and next proof](../research/eye-artistry/brown-liquorice-profile-overlap.md).

**Native eye finite clearance, 25 September:** Source-pinned geometry at nine default-idle phases shows the saved five-morph head has 165–255 eye/head triangle-intersection pairs per phase with an opaque-ray exposure witness; the neutral head has 0–11. Lash and wetness chunks also overlap the head under this geometric test. These pair counts do not establish visible game artifacts or material-alpha behavior. All reported witnesses passed an independent overlap check; production rendering remains unchanged. [Study and limits](../experiments/015-native-eye-assembly/finite-clearance.md).

**Expanded plate packed search, 25 September:** Three measured WolvenKit mesh+morph readback compensation trials retained exact native skin bytes in both buffers, topology, UVs and 105 morphs, but none passed the unchanged independent 107 static / 73 selected-pose / 664 dense-frame gates. The closest has one edge breach of 0.291 µm and one contact at frame 299. No plate was promoted; the next fit must target those localized packed witnesses with positive margin. [Trial evidence](../experiments/012-native-plate-bootstrap/README.md#measured-resource-readback-compensation-three-rejected-trials--25-september).

**Native eye assembly, 25 September:** An offline study bound the installed 57-joint female eye mesh to the pinned 663-phase idle. Eye-joint and shared eyelid-joint motion agree with the current preview attachment, but 184 pupil-weighted native eye vertices deviate from a rigid eye by up to 0.345 mm. This is numeric evidence, not a renderer or game-parity result; native lash/wetness materials, eyelid clearance, source gradient filtering and in-game appearance remain open. [Reproducible evidence](../experiments/015-native-eye-assembly/README.md).

**Desktop update restart guard, 25 September:** The disabled updater now refuses an injected apply until a nonce-bound workspace write and acknowledgment complete, and while package or registered install work is active. Pending saves veto `before-quit` synchronously. Focused tests cover the host policy; signed archive authenticity, a private feed and installed A→B/rollback evidence remain open. [Trial gate](../research/authoring/desktop-update-ab-gate.md).

**Desktop update gate, 25 September:** The unsigned, feedless Electrobun 2.0.1 canary cannot safely run a native installed A→B update. The current three-consent state-machine tests pass, but archive authenticity, a signed private feed, standard-user data preservation and native failure restoration remain open. The updater remains disabled. [Concrete signed trial plan](../research/authoring/desktop-update-ab-gate.md).

**Desktop close-save gate, 25 September:** the installed Electrobun WebView2 window now cancels native close until it forces the latest workspace capture and receives an authenticated acknowledgment of the validated host write. A disposable trial deliberately delayed a 71 KB unsaved draft write: the window stayed open, then closed only after the 282nd preset and selection reached disk; relaunch restored both. A simulated save failure kept the window open with a visible error, and retry saved/reopened the 283rd preset. Forced termination, power loss and updater restart remain separate gates. [Installed close trial](../projects/xf-studio/authoring/desktop/README.md#desktop-close-flush-acceptance-25-september).

**Installed desktop WebView and restart acceptance, 25 September:** disposable Electrobun installs exercised the visible UV editor (edit, Undo, SQLite Save and reload), Check, five-file private preview intake and **Build mod files → Build now**. The one-preset Build produced a manifest, archive and ArchiveXL file with matching hashes and no omissions; Check after Build agreed on the packaged snapshot. Full app restart initially exposed an origin-bound draft bug because the loopback port changes. A host-owned, validated workspace file now restores saved and unsaved presets and selection across restarts; a second installed canary verified both. Standard-user installation, real updates and game rendering remain unproved. [Installed trial and limits](../projects/xf-studio/authoring/desktop/README.md#installed-webview-acceptance-and-restart-repair-25-september).

**Native preview input and plate gates, 25 September:** the fixed vanilla brown-eye gradient has a repeatable, private three-stop source-input extraction, but shader sampling, gaze and iris optics are not yet mapped into the preview. A direct transfer of the earlier rejected morph-aware plate correction onto the exact native 1,620-vertex cut failed the unchanged geometry gates: seven edges exceeded the neighbor cap, two static eye morphs introduced contacts, and dense idle frames 298–299 introduced three contacts. Neither experiment changed production assets or proves game rendering. [Eye inputs](../experiments/014-native-eye-gradient/README.md), [plate diagnostic](../experiments/012-native-plate-bootstrap/README.md#native-topology-correction-transfer-diagnostic--25-september).

**Native plate packing gap, 25 September:** a constrained repair of that direct transfer passed all 107 static, 73 selected-pose and 664 dense idle checks in numeric and float32 GLB form. The independently checked WolvenKit mesh/morph readback retained exact native skin bytes, UVs, triangles and 105 morph identities, but position readback changed enough to fail: 14 static edge-limit violations, one static new contact and 359 contact occurrences across 111 dense frames. Base and morph readback both contribute. The packed resource is rejected; the next correction must optimize and verify its actual serialized geometry with a measured margin. [Numeric, float32 and packed evidence](../experiments/012-native-plate-bootstrap/README.md#constrained-native-repair-numeric-pass-packed-failure--25-september).

**Eye and brow source traces, 25 September:** the installed compiled `eye_gradient` G-buffer variant uses iris-mask red as the gradient-atlas lookup coordinate and its alpha to blend gradient RGB over base eye colour; roughness reads red and scales it by the material parameter. Generated atlas interpolation and native gaze assembly remain unknown. A reproducible probe of the current default idle found a changing right outer-brow wrinkle output, but no proven binding from that solver track to the head's wrinkle material maps. The two supplied game frames appear to include a contour/spacing change that wrinkle shading alone cannot establish. These are offline diagnostics, not production renderer changes. [Eye shader trace](../experiments/014-native-eye-gradient/compiled-shader-and-gaze.md), [brow signal evidence](../research/animation/brow-idle-gap.md#25-september-reproducible-processed-wrinkle-probe).

**Desktop updater gate, 25 September:** the Electrobun host now exposes installed/available update state and separate consent actions, with a two-version fake-port acceptance harness. Real updating remains disabled: the canary is unsigned and has no authenticated private feed or installed A→B evidence. About shows that gate, and localhost remains independent. [Update trial](../projects/xf-studio/authoring/desktop/README.md), [architecture](../research/authoring/desktop-packaging.md).

**Native preview core prototype, 25 September:** a separate offline candidate now exports the installed 2.31 female head and eye meshes plus six vanilla head/eye maps into private GLB/PNG outputs without Nathan's Blender source. Independent source/link/pixel checks pass and two runs produced identical candidate hashes. The brown-eye gradient, gaze assembly, optical shader, accepted plate clearance and mod-provider resolution remain open; this candidate is not a complete five-file Studio intake or production preview. [Source chain and proof](../experiments/013-native-preview-core/README.md).

**First-run preview source gap and UV-only mode, 25 September:** a fresh desktop install now opens the UV editor, with mask work, Undo, collections and package Check available while the 3D head is explicitly unavailable. Its intake dialog can import five prepared GLB/PNG outputs and reload into the full preview. The current generator still starts from Nathan's private Blender head/plate and atlases; a clean user's game installation is a plausible local source, but native eye export, map conversion and accepted plate clearance are unfinished. An offline native-head prototype now derives the same 3,010 triangle selection without the private `.blend`, preserving UVs, 105 morphs and packed skin bytes in both resource buffers, but its neutral cut has no approved posed clearance and is not used by preview/export. No game assets are bundled. [Native candidate](../experiments/012-native-plate-bootstrap/README.md), [desktop first run](../projects/xf-studio/authoring/desktop/README.md#uv-only-first-run-25-september).

**Installed desktop Build acceptance, 25 September:** an earlier disposable Electrobun canary ran its authenticated host Build with the private plate, game and WolvenKit 9.0.1 inputs. Its four-preset candidate had 16 verified resources and zero omissions; resource hashes matched the localhost candidate, while `.archive` index timestamps prevented byte-for-byte identity. App-only uninstall retained its private candidate/report; game/MO2 files were untouched. The newer installed WebView button-flow result is recorded above. [Installed trial evidence](../projects/xf-studio/authoring/desktop/README.md#installed-build-acceptance-25-september).

**Lip light and ownership study, 25 September:** matched diffuse/reflection views isolate the current browser's narrow bright lower-lip edge in its reflection term. The visible upper/lower lips and nearby skin share the large head UV island; a broad geometric exclusion band protects the lips across six pose/camera checks but also excludes surrounding skin. There is no validated semantic mask, mouth/teeth ownership or safe diffuse-transport gate, so production SSS was not changed. [Topology and acceptance evidence](../projects/xf-studio/authoring/evidence/render-fidelity-ownership-gate-2026-09-25.md).

**Isolated MO2 diagnostic promotion, 25 September:** the offline-verified four-preset package is now copied to a dedicated `XF Studio` mod and a new `XF Studio diagnostic 2026-09-25` profile. The new profile disables the legacy Eye Artistry development mod and enables XF Studio. The original `2025 (again)` modlist still has SHA-256 `64edf00004f8a283a173671d70390d7b16d22cd64da92af7d05e66682537ab4b`; both promoted package files match the candidate manifest, and a private completion receipt exists with no pending journal. Three official stable framework releases were then added as separate MO2 mods and enabled only in this diagnostic profile, leaving the old folders and original profile untouched. A read-only reconstruction found one enabled provider and no exact-path competitor for the three framework DLLs and two XF Studio package files; live MO2 origins and game-loaded versions remain unobserved. MO2's selected profile was not switched, and the game was not launched. [Framework record](../research/authoring/framework-diagnostic-profile-2026-09-25.md), [transfer and recovery](validation.md#prepared-single-session-test-card).

**Portable package-build foundation, 25 September:** localhost and desktop now use the same partial-export filter and final package identity gate. The desktop includes an asset-free, integrity-checked tool bundle and a bounded Python process-tree adapter. Build is enabled only after host-owned private storage, configured game/plate files, validated WolvenKit CLI and Python dependencies pass readiness checks; browser requests carry the collection alone. Its output must match the source/filtered identities, exact archive/XL file set, lengths and hashes before becoming a private candidate. The installed-app Build with real private inputs remains untested; neither host installs the candidate. The merged suite passes 428 Studio and 20 desktop tests, both typechecks and the browser build. [Current pipeline](../research/authoring/studio-to-mod-pipeline.md), [desktop boundary](../research/authoring/desktop-packaging.md).

**Matched-pose render study, 25 September:** the isolated lip/eye/brow comparison page now selects neutral or saved five-morph pose for both panes together. The saved lip view explicitly withholds its neutral-pose fixed-crop pixel count, avoiding a misleading comparison. Both poses were visually checked in the private asset-backed study. This changes no production material or game-parity claim; the next diagnostic should isolate painted colour from reflected light before attempting a skin-shader change. [Study evidence](../projects/xf-studio/authoring/evidence/render-fidelity-study-2026-09-24.md).

**Desktop core preview intake, 25 September:** the asset-free first-run now inspects a user-selected prepared folder for five fixed core preview outputs and copies structurally valid GLB/PNG files into fresh private Electrobun user data. A different valid generation is accepted; matching the recorded hashes is optional diagnostic information. Corrupt or missing files stay diagnostic and existing private trees are preserved. Structural checks and output hashes do not establish ownership or game fidelity. The installed private-asset path remains untested. [Boundary and evidence](../projects/xf-studio/authoring/desktop/README.md#core-preview-intake-25-september).

**Integrated verification, 25 September:** the primary checkout now passes all 423 Studio tests, 15 desktop tests, both typechecks and the browser build. An isolated asset-backed browser session confirmed the selected point and Ready preview survive layer rename/Undo. The private Electrobun canary passed its seven-file/no-private-input gate and remains unsigned and uninstalled; exact build hashes are in the [desktop record](../projects/xf-studio/authoring/desktop/README.md).

**File-action concurrency, 25 September:** a refused file request now returns its busy or unavailable reason without clearing an active mask bake or overwriting the last completed result. Only the request that acquired the file transaction releases it; focused tests cover overlapping refusal, successful completion, error and cancellation. File and collection progress still have separate activity models. [UI audit](../research/authoring/ui-overhaul-2026-09-24.md).

**Desktop Check responsiveness, 25 September:** the Electrobun host now validates, filters and runs its 32-pixel preflight in a dedicated Bun worker with a 15-second deadline. Timeout, cancellation and worker failure return an error without a partial result; concurrent Check requests are refused. The existing localhost parity test, pathological worker deadline test, bundled-worker test, desktop typecheck and dev build pass. The full authoring suite passed 394 tests and had five known failures from absent ignored private GLB fixtures. The installed-app Check path remains to be exercised. [Desktop boundary](../projects/xf-studio/authoring/desktop/README.md).

**Desktop package Check, 25 September:** the Electrobun host accepts authenticated collection-only Check requests and uses the shared TypeScript finish filter plus 32-pixel compiler preflight in its bounded worker. A partial-export fixture matches localhost on retained identities, omissions and filtered SHA-256; Check needs no source checkout or external build inputs. At this checkpoint Build still returned 503; the newer portable Build foundation above supersedes that limitation when its readiness gates pass. Check is available once the editor opens with private preview assets; the missing-assets first-run screen remains setup-only. Desktop host tests, both typechecks and the dev bundle passed. [Boundary and limits](../projects/xf-studio/authoring/desktop/README.md).

**Layer edit preview repair, 25 September:** layer rename and its Undo preserve completed makeup textures; reorder and structural Undo retain canvas/material/optical resources by stable layer ID. Only changed, new or unfinished layers request new raster work. A surviving selected point remains selected through rename, move and Undo. Focused tests, typecheck and browser build pass; the full isolated-worktree suite passes 394 tests with five missing private GLB fixture failures. A live asset-backed `?verify=1` check confirmed point 3 stays selected and preview stays Ready through rename and Undo. [Preview contract](../research/authoring/preview-quality-contract.md#layer-identity-reconciliation--25-september-2026).

**Collection draft recovery, 25 September:** opening or importing successive collections now keeps up to four browser drafts in a reloadable recovery queue. Repeated *Recover previous draft* reaches earlier unsaved edits and per-preset Undo; leaving a recovered draft keeps it in the queue. The Library panel warns only when another open would evict the oldest draft. Focused collection tests pass; the full isolated-worktree suite passes 388 tests with five missing private head/plate GLB fixture failures. Typecheck and browser build were run for this checkpoint. [Audit](../research/authoring/ui-overhaul-2026-09-24.md).

**Simpler first-run makeup shape, 24 September:** a browser workspace with no saved draft now opens an unsaved, saveable collection containing one mirrored, four-point matte eye area. Add/Reset layer use the same contour. Existing workspaces, legacy recipe-only drafts and the historical four-layer sample remain unchanged. A fresh isolated browser view showed the contour on both eyes; 391 integrated Studio tests, typecheck and browser build passed. [Editor use](../projects/xf-studio/authoring/README.md#use).

**Private first-game diagnostic stage, 24 September:** the four-preset, 16-resource, zero-omission package was copied into a separate ignored scratch MO2 root with a receipt and matching SHA-256 hashes. The copied profile disables the legacy Eye Artistry development mod and enables XF Studio; the original `2025 (again)` modlist retains its original SHA-256. This scratch root lacks the other 920 enabled mods and cannot launch as an MO2 instance. The 25 September isolated promotion above supersedes its earlier no-live-install status. Framework metadata in the source profile is ArchiveXL 1.26.3, TweakXL 1.11.3, Codeware 1.20.3 and redscript 0.5.31; update and pin the applicable versions before a single controlled runtime smoke. [Diagnostic card](validation.md#prepared-single-session-test-card).

**Desktop first-run setup, 24 September:** the Electrobun host stores the same private local-setup schema as localhost in its own `Utils.paths.userData`. Missing preview assets do not prevent setting the game/mod paths: the first-run screen opens an authenticated setup form with revision-safe save and backup recovery. About reports path readiness. At that checkpoint both mod Check and Build were unavailable; the 25 September Check slice above supersedes that capability state. Host tests, authoring typechecks and an asset-free browser first-run review passed; a private canary passed its package allowlist. No installer was run and the desktop still cannot build a mod. [Trial and exact evidence](../projects/xf-studio/authoring/desktop/README.md).

**Saved brow gradient comparison, 24 September:** a fresh, separate extraction
of the game and installed Alliekat `brown_ombre` XBMs found different containers
but pixel-identical 32×4 base images. The Studio's saved brow already samples
those exact base pixels at the material's constant gradient coordinate, so
swapping between these two PNGs cannot explain its close-up colour disparity.
Smaller mips and one downgrade flag differ; actual runtime binding and shading
still need a matched game capture. No preview material or geometry changed.
[Evidence and limits](../research/eye-artistry/brown-ombre-gradient-equivalence.md).

**Desktop canary, 24 September:** a [private XF Studio Windows setup ZIP](../projects/xf-studio/authoring/desktop/README.md) now builds with pinned version/channel/build metadata and checks that private preview assets, SQLite data and an update feed are absent. The UI shows packaged version and user-data location. This canary is unsigned and uninstalled; clean-machine dependency acquisition, installed AppData behavior, data-preserving uninstall, updater trials and a public release remain open.

**Local package setup, 24 September:** localhost now saves game/build and direct/MO2 source paths in private OS user settings through the Mod package panel. Build resolves those settings for each request, with existing `XFS_PACKAGE_*` server overrides taking precedence; there are no developer-specific path fallbacks. Readiness explains why Build is unavailable, while Check stays available without those inputs. This does not install a mod, prove tool-version compatibility or establish a runtime archive winner. [Settings boundary](../projects/xf-studio/authoring/LOCAL-SETTINGS.md).

**Public site and local preview, 24 September:** the Opus-designed [GitHub Pages site](https://axefrog.github.io/xf-studio/) and [authoritative HTML style guide](https://axefrog.github.io/xf-studio/style-guide.html) are live; the first main-branch workflow succeeded and both URLs returned HTTP 200. The new Studio UI is served at `http://127.0.0.1:4317/` and an isolated verification workspace at `http://127.0.0.1:4318/?verify=1`. A saved panel layout remains as authored until the user selects *Reset this layout*. The initial Electrobun host runs locally with private assets and a clean missing-assets first-run state, but installer, updater and public release remain open.

**Project path migration, 24 September:** the XF Studio project now lives at `projects/xf-studio`. Current experiment imports, local build entry points, the Pages workflow/site links, plate intake references and documentation links follow the new path. Existing recipe/workspace IDs, browser keys, `XFAS_DATA_DIR`, game depot names, source art names and captured historical output paths retain their identities. The SQLite library received an integrity-checked backup before the ignored local assets were moved. In the primary checkout, all 370 Studio tests, typecheck, browser build and Pages verification pass; a four-preset package built and independently verified at the new path with no omissions. It remains a private, uninstalled candidate without in-game rendering evidence.

**UI/UX overhaul delivered for review, 24 September:** on the main branch, Claude Code 2.1.281 with `claude-opus-5-5` replaced the fixed sidebars with a dockable/floating/magnetic/tabbed panel workspace whose snapping uses cursor position only, target-aware context menus, a command palette and system/light/dark themes. The new default entry mounts only `StudioPresentationPort` (legacy shell at `/legacy.html`); a boundary test enforces the presentation's imports. Read-only port extensions (cheap editor/library/preview reads, finish catalogue, autosave/asset status) are a separate commit with tests; no recipe, SQLite, material or compiler behavior changed. The self-contained [interface style guide](../projects/xf-studio/authoring/public/style-guide.html) is authoritative and test-synchronized. An isolated `?verify=1` acceptance passed 24/24, including a live Escape during a UV drag, the large-overlap snapping case and real export/import file flows; saved-V import and Build were not exercised. Follow-up: the factory layouts now give the head a full-height portrait column (wide) or a portrait cell beside an always-visible UV map (compact); saved layouts restore unchanged and *Reset layout* adopts the new default. The audit confirmed three core defects for follow-up (second collection open loses the earlier draft; rename/Undo rebuild all textures and reset the point; a refused file action clears `busy`). [Delivery record](../research/authoring/ui-overhaul-2026-09-24.md).

**WolvenKit 9.0.1 toolchain check, 24 September:** the official Windows Console ZIP was verified against its published SHA-256 and installed beside CLI 8.17.4. A private four-preset build with 9.0.1 passed the independent verifier and reproduced the same 16 unpacked depot paths and payload hashes as the latest validated 8.17.4 build. The whole archive hash changed with index metadata; production defaults and runtime status did not change. [Exact evidence](../research/authoring/wolvenkit-9-roundtrip-2026-09-24.md).

**Desktop and install architecture, 24 September:** A [design checkpoint](../research/authoring/desktop-packaging.md) specifies separate authoring, environment/source, verified package, MO2/direct install and update boundaries. It preserves independent localhost use, makes external paths configurable, and gates an Electrobun 2.0.1 Windows trial against the real Studio workload with Electron fallback. Installation, updater, tool migration and a public release remain unimplemented; the current package is private, offline verified and not game-tested.

**Generated archive path gate, 24 September:** the local package builder now refuses any generated resource tree that differs from the filtered plan or contains noncanonical paths, unsupported extensions, symlinks or duplicate depot hashes before WolvenKit packs it. The independent verifier rechecks the recorded path/hash/content inventory and exact unpacked member set. A fresh private four-preset build verified 16 resources with the same path/SHA-256 pairs as an earlier build; the partial-export preflight tests still pass. No installation or game test occurred. [Evidence and source trace](../research/authoring/archive-path-gate-2026-09-24.md).

**Fixed-pose brows and Shimmer export diagnostics, 24 September:** A private [brow comparison](../research/eye-artistry/brow-fixed-pose-ab.md) now renders the same saved morph and camera against the current and base skin maps. The brow silhouette and alpha coverage remain identical while the apparent edge changes, so the earlier “wrong brow texture” hypothesis is not supported by this test; direct Three normal input made the cards too silver and was not adopted. A separate [Shimmer on-plate package](../experiments/011-shimmer-plate-comparison/README.md) was independently rebuilt and verified offline: one selector contains Off, flat and four normal sign/blend variants on duplicated plate mesh/morph resources, with all 105 morphs and native model buffers preserved. It is an experimental package for a controlled game comparison, not a production export. The main authoring suite passes 333 tests, typecheck and build.

**UI redesign preparation, 24 September:** Nathan has elevated a professional, restrained Cyberpunk UI overhaul. A [capability inventory](../research/authoring/ui-capability-inventory.md) and source-checked [action catalogue](../research/authoring/ui-action-catalogue.md) describe current commands, capability gaps and expansion areas independently of controls. The [Opus 5.5 brief](../research/backlog/claude-ui-overhaul.md) requires an authoritative HTML style guide, context menus, persistent system/light/dark choice and cursor-position dock/float/magnetic/tab panel behavior. DOM-free services now own collection, authoring document/history, gesture/control transactions, render scheduling, workspace persistence, saved-V, motion, quality, preview coordination and typed file/package work; `StudioApplication` aggregates read-only state, typed actions and a static descriptor registry. Production controls read detached geometry and state; context queries use the existing UV/head pickers to bind actual hits to current actions and revalidate stale targets without changing selection or Undo. A viewport port rehosts the same head/UV hosts through hidden and visible dock slots. The current Undo, UV view, saved-eye and piercing choices have typed paths; a composite [presentation port](../projects/xf-studio/authoring/src/studio-presentation.ts) supplies detached authoring, library, file, viewport and theme/layout preference state to a replacement UI, and the preference action now saves through workspace persistence. The combined branch passes 329 tests, type check and build. The [isolated cross-surface pass](../research/authoring/ui-handoff-acceptance-result-2026-09-24.md) found no new app defect: partial Check/Build matched, library conflict/recovery and reload worked, and preview quality reached Ready without stale output. Browser file selection was automation-blocked. A DOM-free trusted core and bootstrap can mount a replacement presentation from that port; the [isolated browser smoke check](../research/authoring/ui-bootstrap-smoke-2026-09-24.md) confirmed the existing UI still boots, edits, undoes and restores its UV view without browser warnings. Browser viewport, file picker, workspace persistence, preview raster/resources and saved-V/motion/preview service restoration now have trusted adapters. An independent browser entry now boots from the public presentation port without legacy controls, and its isolated acceptance passed typed edits/Undo, reload, library conflict/recovery, partial Check/Build agreement and public preview readiness. The [port acceptance record](../research/authoring/ui-port-acceptance-2026-09-24.md) states the saved-V picker and live Escape automation limits. The authenticated Opus 5.5 handoff completed, its changes were reviewed and merged, and the [architecture contract](../research/authoring/architecture-contract.md) and [AGENTS.md](../AGENTS.md) remain binding for future UI work.

**Mod export clarity and partial packages:** the former “Check game package” action is now **Check mod export**, beside **Build mod files** and editable collection export. Both actions use a package-only copy: active unsupported finish layers are omitted, along with any preset left without active exportable layers. They name every omission; Matte/Satin/Metallic content that stands on its own can still be built. An entirely empty filtered package, invalid input or failed verifier is refused. The authored draft and SQLite revision stay unchanged. The offline four-to-three-preset test verified 13 resources and matched Check to the Build manifest on omissions, identities and hashes; no game install occurred. [Partial-export checkpoint](../research/authoring/partial-mod-export-checkpoint.md); [pipeline guide](../research/authoring/studio-to-mod-pipeline.md).

**Glitter preview suite:** Classic, Irregular raster, Direct-light, Clustered and Dense fine studies share one model selector. Model-specific controls appear for the selected study; switching is undoable, and inactive choices are remembered locally across reload. A portable recipe/SQLite preset contains only its selected model. The suite changes no game-export support: Glitter remains preview-only. Full authoring regression before the action extraction: 247 tests, typecheck and build passed; subsequent focused package/action/Glitter tests and build passed. [Usage and limits](../projects/xf-studio/authoring/README.md).

**Isolated lighting study:** a separate [locked A/B page](../projects/xf-studio/authoring/public/render-fidelity-study.html) varies one browser material term while holding the neutral head, saved-eye maps, camera and lights fixed. At a fixed lip crop, a near-white highlight count fell from 182 to 0 when replacing the mapped skin roughness with uniform 0.85; flattening the normal map left it at 183, and removing all room lighting reduced it to 23 while darkening the face. This narrows the browser lip-seam lead to roughness/reflection interaction, not a production fix. Exact saved-eye roughness R and G looked nearly identical under one view; the inspected game shader still specifies R. Saved skin/mouth assembly, eye optics and REDengine parity remain open. [Measurements and limits](../projects/xf-studio/authoring/evidence/render-fidelity-study-2026-09-24.md).

**Saved skin/teeth source trace:** the captured `skin_type_05` resolves to a D05 head appearance with local Arkhe complexion, packed-normal, microdetail and skin-profile alternatives; teeth use a separate appearance/morph/mesh/material chain. The inherited game roughness texture's green channel is zero, so it cannot be fed directly into Three.js's green-channel roughness slot. The browser's current old Blender skin maps are a different source chain. The [resource audit](../research/eye-artistry/saved-skin-resource-chain.md) records exact hashes and MO2/direct-route candidates while keeping runtime winners and game shader channel handling explicitly unresolved; no production material changed.

**Skin shader and current source-order audit:** in the inspected installed 2.31 skin G-buffer variant, the main packed normal reads R/G and reconstructs Z; base roughness reads R, with B gating a spatial detail-bias term. The game's G channel is not the base roughness for this variant. The current MO2 profile has one visible Arkhe candidate for each saved D05 colour/normal/microdetail/profile input, but its mod list changed after the captured save, and neither historical nor live loaded bytes are proved. This supports a hash-checked, isolated A/B adapter before any production material change. [Channel and source limits](../research/eye-artistry/saved-skin-shader-and-winner.md).

**CharacterCreator rendering reference:** Nathan's separate project was inspected read-only. It has isolated lighting comparisons and a diffuse-only, depth-guided screen-space scattering approximation that keeps specular sharp. Its own tests rejected an earlier local skin-response trial and exposed lip-geometry and eye-visibility limits. These are useful methods for an XF Studio A/B study, not portable calibration values. The current XF lip seam remains more directly associated with its roughness/reflection path, so source-map channel/UV correction comes before any optional scattering trial. [Read-only comparison and provenance](../research/eye-artistry/charactercreator-rendering-reference.md).

**Isolated source-map comparison:** native preview head geometry and UV0 matched the base-game D05 head by 6,917 position/UV signatures and all 13,186 triangle signatures. A hash-checked, ignored five-map intake now lets the separate study page compare old Blender inputs with base D05 and current-MO2 Arkhe candidates, decoding packed normal RG and roughness R explicitly. In a fixed browser lip crop, the old-map white-pixel measure was 97, versus 17 for base D05 and 3 for the Arkhe candidate; the material appearance also changed substantially. These are one-view browser observations, not effective runtime winners, REDengine shading or a production renderer fix. [Visual and numeric evidence](../projects/xf-studio/authoring/evidence/render-fidelity-source-maps-2026-09-24.md).

**Diffuse-only SSS gate:** the CharacterCreator-inspired blur remains an experiment proposal. The current comparison page lacks separate diffuse/full/depth render targets and a lip/eye ownership mask; a screen-space blur here would cross material boundaries and misstate fidelity. The [gate note](../projects/xf-studio/authoring/evidence/diffuse-sss-gate-2026-09-24.md) records required buffers, camera views and a lower-bound memory estimate before a truthful on/off study. No SSS toggle or production renderer change was made.

**Packaging architecture guide:** [From collection to mod candidate](../research/authoring/studio-to-mod-pipeline.md) traces editable recipes, immutable library revisions, the package-only partial filter, the compiler's ordered layer merge, archive resources, independent verification and the still-unproven game boundary. Its three Mermaid diagrams were rendered and visually reviewed at normal and reduced widths, then the affected diagrams were rerendered and reviewed after partial export landed. [AGENTS.md](../AGENTS.md) requires keeping the guide and visual evidence current with every pipeline change.

**Local package workflow, 24 September:** Studio offers **Check mod export** and **Build mod files** for the current draft, including unsaved edits, without changing its SQLite revision. A separate CLI also accepts exported collections. The server uses its own tool paths, checks original and filtered snapshot hashes, builds eligible content in isolated ignored intermediates, runs the independent Experiment 005 verifier, then promotes only the verified archive and ArchiveXL declaration to ignored project `dist`. A complete four-preset CLI build verified 16 unpacked resources; a partial three-preset build verified 13. An isolated browser build retained an unsaved rename while its saved revision stayed unchanged. Manifests record omissions and `installed: false` / `gameRenderingVerified: false`; no game installation occurred. [Workflow and limits](../research/authoring/local-package-build.md); [browser evidence](../projects/xf-studio/authoring/evidence/package-ui-bridge-2026-09-24.md); [prepared runtime test card](validation.md#prepared-single-session-test-card).

**Plate crease candidate rejected after packing:** a shallow `h091_eyes` correction at vertex 119 passed all 107 static and 73 sampled-pose gates in floating-point research. The separate mesh/morph round trip preserved exact native skin bytes in both buffers, topology, UVs and all 105 morphs, but float32 import already breached four neighbor limits; serialized position error then produced two new static contacts, five posed contact occurrences and 18 neighbor-limit violations. The owned master remains unchanged. Future candidates need explicit post-packing slack. [Numeric study](../experiments/006-plate-clearance/research/crease-surface-checkpoint.md); [serialized-resource failure](../experiments/006-plate-clearance/research/crease-resource-roundtrip.md).

**Later post-packing plate candidate also rejected:** a quantization-aware local base/morph correction survived mesh/morph serialization with exact native skin bytes, intact topology/UVs and all 105 morphs. It passed 107 static cases and 73 selected idle poses with measurable packed margins. A denser check of all 664 baked 30 Hz idle frames found **three new plate/head contact occurrences at frames 298–299**, independently confirmed by finite-triangle separating axes. Thus the apparent 73-pose success was insufficient. The owned master and current package remain unchanged; a revised candidate must pass this denser gate before visual/runtime review. [Postpack checkpoint](../experiments/006-plate-clearance/research/postpack-crease-checkpoint.md); [full-frame failure and reproduction](../experiments/006-plate-clearance/research/dense-idle-contact-gate.md).

**Glitter export boundary:** an actual recipe-10 mask now feeds a separate, labelled stock 2.31 material study. Six generated textures and two material instances round-trip locally, but the study does not map the browser's direct-light facet settings into the game shaders. Strong baked normal tilt and bright emission disappear by the 128px source-map tier; Glitter remains explicitly rejected by the production compiler. [Recipe-driven measurements](../experiments/009-glitter-game-fixture/recipe-driven-boundary.md). A reviewer fix ensures custom-recipe study runs cannot overwrite the tracked baseline reports without an explicit default-sample update.

**Shimmer game-material boundary:** a separate real-recipe fixture feeds the current legacy Shimmer optical bake and exact shape mask into the installed 2.31 `mesh_decal` inputs. Five XBM maps and one material instance round-trip; decoded strong normal slope falls from 16.0% at 1024 to zero at 256. This measures source/serialization behavior, not a lit on-plate result; normal sign/blend, mixed-finish overlap and appearance remain unresolved. Production Shimmer export stays rejected. [Fixture and limits](../experiments/010-shimmer-game-adapter/README.md).

**Glossy game-material boundary:** a Studio Glossy recipe now drives an opt-in stock 2.31 blendable-decal fixture. The exact mask, colour, roughness and metalness survive a local three-XBM/material round trip; independent main-checkout verification passes. Decoded squared-alpha coverage retains 99.9% at 1024 but only 63.3% at 32, as filtering encoded alpha before squaring loses thin coverage. More fundamentally, this template has one roughness-controlled reflection, whereas the Studio preview has a separate clearcoat reflection. It is a pigmented single-lobe approximation, not a faithful game material or runtime result. Production Glossy export remains rejected. [Recipe fixture and measurements](../experiments/008-glossy-decal/README.md).

**Colour-shifting material lead:** the installed 2.31 `mesh_decal_gradientmap_recolor_blendable` MeshSkinned pass adds a masked Fresnel RGB term driven by view angle, offering a possible limited two-tone tint. Its one additive hue is not Studio's thin-film iridescence or general multichrome; no material instance or game comparison has been made. The Colour-shifting export guard remains. [Compiled-program and template audit](../research/materials/colour-shift-game-feasibility.md).

**Saved brow/lash audit:** the installed Arkhe style-18 primary and secondary alpha mip chains differ from a simple 2× box reduction by at most one byte per pixel through level 10. That rules out a large *authored mip* density change as the cause of the thicker-looking Studio brow, without proving equivalent engine filtering. Soft Natural's preview tint remains approximate; no source-backed colour correction emerged. A matched game capture with effective resource bindings is the next gate. [Measured mip study](../research/eye-artistry/brow-lash-mip-gate.md).

**Later piercing colours checked:** the current preview already derives colour from the source material chain rather than the black creator-option swatches. Isolated verification rendered PRC Blue/Wood and vanilla Blue, checked nonblack values for every later manifest option, and passed targeted tests/typecheck/build. No code change was needed; an older open tab may require reload to receive the prior correction. This remains an approximate preview, not a game-material match. [Audit](../research/jewellery/prc-catalog-audit.md).

**Saved-eye archive provenance:** an opt-in, read-only RDAR index scan now separates physical archive candidates, route-visible files, a conservative source-derived winner and an explicitly absent runtime observation. Under the selected MO2 profile and three supplied base roots, each of Nathan's saved Kala eye diffuse/normal/roughness hashes has one indexed Kala archive candidate among 1,161 inspected containers; the direct-game route has no matching diffuse candidate. This narrows the private source chain without proving loaded bytes or ArchiveXL's final transformations. Ten focused tests pass. [Catalog boundary and limits](../research/character-customization/catalog-prototype.md#focused-archive-hash-stage--24-september-2026).

**Head-preview and customization foundation:** Nathan prioritizes a close-to-game, toggleable preview of all head details alongside eye-makeup work. Saved-V hair and piercing context, a jewellery construction-set design, a data-driven creator preset editor and provider-neutral mod discovery are now in the [roadmap](../research/backlog/jewellery-and-customization.md); these are staged work, not a request to interrupt current makeup acceptance. Legacy Eye Artistry layers are historical save references, not a new content or compatibility target; an archive-driven selector catalog may encounter them incidentally. The canonical [modding docs clone](../inventory/dev.md) was fast-forwarded to `be2f44ee`, including the guide screenshots. CCXL and resource-chain audits distinguish file relationships from effective installed winners and runtime behavior.

**Coordinated workstreams:** code-changing subagents work in separate `D:/Dev/worktrees/<slug>` branches; the main checkout receives reviewed, verified commits. Accepted tracks include an [opt-in glossy decal material fixture](../experiments/008-glossy-decal/README.md), a [saved brow/lash material correction](../research/eye-artistry/brow-lash-fidelity.md), [plate contact trials](../experiments/006-plate-clearance/README.md), and three opt-in browser Glitter models. No plate replacement or game-exportable Glitter has passed its acceptance gates.

**Data-driven creator catalog foundation, 24 September:** a [read-only offline prototype](../research/character-customization/catalog-prototype.md) scanned Nathan's selected MO2 profile (999 rows, 2,154 candidate files) and 240 active customization declarations. Vanilla female `eyes_color` has 71 choices; an enabled Unique Eyes to CCXL resource adds 39, with per-choice source provenance. A focused composition of the installed ArchiveXL app-path fix, transitive `player_customization.app` scope and Unique Eyes overlay now finds **one source-derived exact match** for Nathan's saved `eye_16_diffuse` app hash. It still makes no effective archive-winner or current runtime-selector claim and edits no saves.

**Catalog launch-route boundary:** the read-only probe can now select MO2-profile or direct-game physical-file views explicitly. Direct mode omits staged MO2 files while retaining manually deployed game files; MO2 mode requires its root and profile. This makes its inventory conditional on a declared launch route without claiming a past session used that route. A new route-conflict test brings the focused suite to six; archive-hash winners, Vortex deployment and ArchiveXL's full merge still await implementation. [Contract and reproduction](../research/character-customization/catalog-prototype.md#reproduction-and-boundaries).

**Saved-V hair preview advanced:** a matching `lm097_hair` / `38_ash_brown` source supplies an optional, persistent, skinned hairstyle toggle. Exact unsigned app hash and definition select hash-verified ignored local assets; multi-entry manifests can switch saved styles within resource limits. [The profile audit](../research/eye-artistry/saved-hair-profile-resolution.md) identifies `38_ash_brown` as island_dancer's CCXL colour over MELUMINARY geometry, among six enabled colour packs. The browser now samples source strand ID/root-to-tip maps through AshBrown profile stops and shades the cap from its mask/gradient. A controlled isolated visual probe showed varied brown cards without shader errors; actual save import in that probe was blocked by Chrome extension file access. Main integration passed 214 tests, typecheck and build. Exact REDengine shader/flow response, physics and game appearance remain unverified. [Intake and visual limits](../research/eye-artistry/saved-v-hair-preview.md).

**Hair material follow-up, 24 September:** the missing MELUMINARY Flow map was verified from the installed archive, but 262,142 of 262,144 decoded pixels are identical. A temporary Three.js anisotropy/shininess trial either overexposed the cards or looked like the current preview after tuning, so no speculative material change was retained. The existing AshBrown profile-driven hair stays in place; matched game lighting and hair physics remain open. [Flow evidence](../research/eye-artistry/saved-hair-profile-resolution.md#flow-map-and-specular-follow-up-24-september).

**Saved hair cap restored:** the source cap mesh uses U coordinates 2–3 while its mask is a repeating 0–1 tile. Clamping the mask sampled its black edge and hid the scalp cap. The browser now repeats only the cap mask in U; an isolated cap-only before/after showed the fitted brown cap appear, and the full saved hair still renders without console errors. The strand cards already sampled their source alpha/ID/gradient textures. Lighting, transparency and physics remain approximate. [Measurements and visual evidence](../projects/xf-studio/authoring/evidence/hair-cap-wrap-2026-09-24.json).

**Brow/lash preview correction, 24 September:** the saved Arkhe brow now uses its exact hash-verified primary/secondary alpha maps and installed brown-ombre gradient; the old diffuse-green opacity made the brow too full. The saved Soft Natural lashes use an explicitly approximate colour from their `brown_liquorice.hp` profile and constant strand textures. Controlled browser comparison moved the brows darker/slimmer. A fresh [style-18 shape audit](../research/eye-artistry/brow-shape-followup.md) finds that the installed mod copies vanilla brow vertex/morph data, with no evidence for a separate thinner mesh. Shading, mip filtering, runtime winners and a matched-camera game comparison remain open; no geometry was thinned from the unmatched screenshots. [Resource chain and measurements](../research/eye-artistry/brow-lash-fidelity.md).

**Saved lash opacity checked:** the browser already loads the exact Soft Natural alpha image; its RGB channels are identical greyscale and its PNG alpha is solid, so switching to PNG alpha would incorrectly make solid cards. The current `#302d29` tint remains labelled approximate. Installed game hair shader variants use separate alpha accumulation and colour blending, but their runtime binding for this lash is unproven; no speculative rendering change was made. [Source/channel and compiled-pass audit](../research/eye-artistry/lash-material-followup.md).

**Jewellery design input documented:** a [construction-set proposal](../research/jewellery/construction-set-design.md) now incorporates Nathan's [36 locally collected earring photographs](../research/jewellery/earring-reference-inventory.md), broadening the proposed primitives to freeform frames, multi-rail hoops, bead spines and repeated chandelier/fringe branches. It is research for later discussion, not an implemented editor or clearance to copy product/mod assets; original photo authorship remains unresolved.

**Vanilla piercing preview accepted as context:** the current 2.31 female creator resource derives 14 styles and 16 colours per style; four hash-verified local morph meshes and each `.app` component mask drive an optional, persistent preview selector. Nathan's saved V has no matching vanilla piercing choice, so the selection is viewport-only and does not alter the imported character data. Isolated browser checks covered two styles, colour/toggle reload and idle attachment; 207 main tests, typecheck and build pass. Material/colour response, PRC's effective override and game rendering remain [unverified](../research/jewellery/vanilla-piercing-preview.md). Local extracted assets are ignored and excluded from release.

**Private PRC preview slice:** enabled eagul nose slots 50, 72 and 74 appear beside vanilla choices, with hash-verified ignored skinned/morphed assets and the shared Piercings toggle. A mod-aware export resolved slot 50's item-local linked mesh and slot 74's framework linked mesh, which game-only export could not see. Isolated browser checks covered visible placement, toggle, reload, idle attachment and vanilla fallback. This is a bounded private reference, not a full PRC catalog, proven runtime winner or redistributable asset set. [Source chain, evidence and limits](../research/jewellery/prc-preview-slice.md).

**PRC selector audit:** the enabled item candidates occupy only slots 50, 72 and 74. The framework's 16 female appearances each include the complete 128-slot bank with one shared colour choice, so the studio's separate slot previews are diagnostic rather than the game's combined option. The stud's second mesh chunk resolves its own `default__02` material and is currently approximated as silver. A [source-backed catalog boundary](../research/jewellery/prc-catalog-audit.md) records the required winner and per-chunk material work before claiming a faithful aggregate preview.

**Combined PRC preview:** the optional `PRC · combined 50+72+74` choice displays all three privately resolved active candidate slots together under one shared approximate framework colour. All 16 inspected female definitions have complete ordered banks; separate single-slot inspection remains available. Isolated and main verification-browser checks covered shared tint, toggle, idle attachment and reload, without console errors; 234 tests, typecheck and build passed at that checkpoint. Effective archive winners and exact game material response remain unresolved. The source assets stay ignored. [Evidence](../projects/xf-studio/authoring/evidence/prc-aggregate-2026-09-24.json).

**PRC stud second material resolved as a source category:** all 16 inspected slot-50 mesh appearances use `default__02` on the small second chunk. The enabled stud recolour archive's `.mi` links to the game's brushed-silver earring multilayer setup, independently of the framework's shared colour. The private preview now keeps that chunk approximate silver while the main stud follows the selected framework colour. Local intake and main verification-browser checks of gold single/combined choices passed without console warnings; the isolated draft was restored to Saved V / Off. Exact REDengine response and effective archive winner remain open. [Material trace and limits](../research/jewellery/prc-catalog-audit.md#stud-second-chunk-material-resolution-24-september).

**Vanilla piercing black-colour fix:** Nathan's choices 8–16 were black because the game's creator swatches are literal black placeholders. The local intake now resolves their `.app` mesh appearance through `.mi`, `.mlsetup` and `.mltemplate` palettes and records separate representative preview tints. All 126 affected style-colour pairs produce nonblack material colours, and both vanilla-v2 and PRC manifests validate together. The isolated browser showed blue/wood choices; wood grain and multicolour masks remain approximate. Main integration passed 216 tests, typecheck and build. [Source chain and limits](../research/jewellery/vanilla-piercing-preview.md#colour-correction-24-september).

**Opt-in Glitter browser models integrated:** recipe-7 preserves a measured irregular-raster study; recipe-8 adds direct-light UV-cell glints; recipe-9 adds a separate, finer clustered-glint option without changing older looks. The new mode breaks up many large angular highlights, but central-lid coverage remains visibly sparser than Nathan's reference photos. Nathan's 500,000 setting is now labelled field density with actual eye-region/shape/atlas counts; no UI claim equates candidates to visible specks. The compiler still rejects Glitter for game export. The combined main tree passed 228 tests, typecheck and build. [Clustered visual/performance evidence](../research/materials/clustered-direct-glint-study.md), [original direct-light study](../research/materials/direct-glint-browser-checkpoint.md), [raster visibility audit](../experiments/007-irregular-glitter/visibility-audit.md).

**Fine-speckle Glitter follow-up:** opt-in recipe-10 adds a denser field of tiny tilted facets and a sparse larger-flake population to retain some glints at full-face scale. A fixed-camera browser comparison measured more bright glitter pixels at close and face views with similar 180-frame timing; these pixels and candidate counts are not individual flake counts. The main verification page compiled it without console errors, and Undo restored the prior Matte draft. Some angles still look like metallic frost; the photographic target and game material remain unproven, and the compiler still rejects Glitter. Main integration passes 233 tests, typecheck and build. [Visual measurements and limits](../research/materials/fine-speckle-browser-study.md).

**Glitter game-material follow-up, 24 September:** a third material-only 2.31 fixture adds an independently generated axial-height normal map to the earlier fixed-normal PBR and sparse-emission controls. Three material instances and seven textures round-trip through WolvenKit; the axial map's strong source-level tilt falls from 57.9% at 1024 to zero at 64 under BOX reduction. This is a filtering warning, not game-rendered evidence. The [authoring/export bridge](../experiments/009-glitter-game-fixture/authoring-export-bridge.md) makes the browser-to-stock-material loss explicit; production Glitter export remains blocked pending recipe-driven map validation and a controlled game comparison. [Fixture audit](../experiments/009-glitter-game-fixture/shader-and-minification-audit.md).

**Next accepted makeup/plate findings:** an [offline 2.31 Glitter material comparison](../experiments/009-glitter-game-fixture/README.md) round-trips two material instances and six independently generated textures. Its resolved PBR facets and sparse emissive accents still need a single controlled game comparison; neither matches the browser's subpixel glint shader by construction. A [bounded plate correction trial](../experiments/006-plate-clearance/research/finite-correction.md) failed full sampled contact and static smoothness gates. An independent certificate finds 25 edges where a common base-position-only correction cannot satisfy all tested morphs while their deltas stay unchanged. No replacement plate was adopted.

**Plate and game-material follow-up, 24 September:** the 25 impossible base-only edges localize to 13 narrow eyelid UV groups. A [morph-aware numeric trial](../experiments/006-plate-clearance/research/morph-aware-checkpoint.md) removes all 25 edge failures while retaining displacement budgets, but still produces seven new static and 48 sampled-idle finite contacts after a local rescue. The independent verifier reproduced this failure in the main checkout. The master plate remains unchanged. The [2.31 Glitter shader/minification audit](../experiments/009-glitter-game-fixture/shader-and-minification-audit.md) pins the emissive pass's white secondary-mask default and shows source normal-tilt signal collapsing under BOX reduction; it is offline evidence, not game-rendered Glitter.

**Finite-triangle plate follow-up:** a [bounded local correction](../experiments/006-plate-clearance/research/finite-pair-followup.md) improves the rejected numeric field from seven static / 48 sampled-pose new contacts to four / 14, with zero displacement or neighbor-limit violations. Independent verification reproduced those counts from the ignored field. Contact gates still fail, so no master or game resource changed; the remaining folds need a broader constrained solution.

**Coupled eye-morph plate trial:** a [bounded optimization](../experiments/006-plate-clearance/research/coupled-morph-optimization.md) clears all four remaining new static contacts while preserving the original displacement and neighbor limits, but six new contacts remain across 73 sampled idle poses. The independent full verifier reproduced the result in the main checkout. The numeric field is rejected; no master or game resource was changed. A different plate/decal representation may be needed at the remaining tight crease.

**Residual plate crease localized:** the six posed intersections involve five plate faces and seven vertices within an 8 × 9-texel UV box at 2K. A bounded one-vertex eye-morph correction clears those six but creates eight new contacts at other idle frames; the independent 107-static/73-pose verifier reproduced that tradeoff with displacement and neighbor gates intact. This candidate is rejected too, and no plate master or game resource changed. A small crease-specific surface representation is the next research direction. [Case-level evidence](../experiments/006-plate-clearance/research/six-contact-followup.md).

**New community glitter reference:** Nathan supplied island_dancer's readable Substance Designer graph, and island_dancer clarified its core method: triangle/square primitives, axial gradients, randomized tiling and height-to-normal conversion. Light/dark greyscale selections multiply-blend two colours; patterned roughness/metalness are suggested extensions, not saved outputs. The file is retained locally, not distributed; [rechecked analysis](../research/materials/island-dancer-glitter-graph.md) and [credit](community-credits.md) record the lessons and unverified render/licence limits. The [polygon-facet study](../experiments/007-irregular-glitter/uv-cell-glint-findings.md#polygon-and-fine-speckle-follow-up) informed an opt-in browser model; neither is yet a game-exportable material.

**Current authored recipe intake:** Nathan's latest `xfs/recipe-6` export round-trips unchanged. It is the same nine-knot Backdrop used in the measured bake acceleration, and its three enabled raster outputs match the earlier 1K/2K hashes. Two glossy eyeliner layers remain unexportable through the deliberately limited flat game adapter. The private recipe stays outside Git; [intake and exact boundary](../research/authoring/current-recipe-intake.md).

**Glossy export boundary:** the installed game's compiled `mesh_decal_wet_character` post-G-buffer shader writes roughness/metalness target alpha as `1.0` across the rasterized plate, independent of the eyeliner colour mask. On the full eye plate this risks changing surrounding skin and lower makeup. The stock wet decal is therefore not a safe shortcut for the current glossy recipe. Current 2.31 `mesh_decal_blendable` instead masks those targets and could support a **labelled single-lobe approximation**; it is not a proven clearcoat export. [Compiled evidence and next comparison](../research/materials/glossy-decal-feasibility.md).

**Glitter direction check:** a fixed-facet [CPU oracle](../experiments/007-irregular-glitter/glint-oracle-findings.md) demonstrates reflection detail lost when subpixel normals are averaged before shading. A separate [UV-cell direct-light shader pilot](../experiments/007-irregular-glitter/uv-cell-glint-findings.md) runs on the skinned/blinking head at 1K/2K, with visible view/light-dependent flecks and no observed console errors, but still looks coarser than the photographic target and is not a game material. [REDengine inspection](../research/materials/redengine-glint-feasibility.md) finds a feasible resolved-flake decal path and emissive alternatives, not a demonstrated fine-glint shader injection route. Preserve the production glitter/export guard and prepare one offline-validated game comparison.

**Fine glitter lit-head checkpoint:** 160k/350k region-limited fields, covered-only normal averaging and a two-reflection lighting study are reproducible on the local head at 1K/2K. Smaller irregular flecks and independent flake highlights improve the visual target, but subpixel facets still read partly as pale paint; neither normal averaging mode is ready to replace production glitter. The study page is isolated from the user's draft. [Comparison and evidence](../experiments/007-irregular-glitter/lit-head-findings.md). The later oracle/pilot/engine inspection above continues this investigation before an export model is frozen.

**Makeup bake acceleration:** Nathan's supplied nine-knot Backdrop now computes in Chrome at about 0.97 seconds at 2K, versus 7.13 seconds in the original synchronous worker benchmark, with identical texture hashes. Conservative pruning, prepared edges, exact mirrored-pixel reuse and fair task yielding remove repeated work. Actual editor completion and cancellation verified; 182 tests, typecheck/build pass. Complex fields are still not instantaneous. [Measurements and contract](../research/authoring/raster-performance.md).

**Bezier eye-overlap fix:** tangent arms now project from their real parent knot's deformed triangle, so eye-plate holes no longer block dragging or re-grabbing. Parent head/eye visibility and actual paint-point constraints remain. Real saved-V browser checks pass, including Undo/Escape. A repeated-skinning performance regression was caught and removed with cached exact deformed visibility: paused/animated cadence returned to about 16.7 ms. [Cause, design and evidence](../research/authoring/projected-tangent-controls.md).

**Irregular glitter study:** a deterministic convex-fragment generator and linear-light colour compositor first ran offline, with sparse/dense variants, stable flakes across resolutions and bounded tile scratch. Measurements removed the old lattice but exposed four-sample edge quantization; sixteen samples improved coverage consistency at higher bake cost. The opt-in raster candidate is now in the editor, though its visual result remains below the references. [Foundational study](../experiments/007-irregular-glitter/findings.md), [browser checkpoint](../research/materials/glitter-browser-checkpoint.md).

**Preview quality:** persistent 512/1K/2K/4K generated makeup textures now appear in a collapsible group beside lighting. Masks and optical maps rebuild together in cancellable background jobs, with hardware/budget checks and disabled resource release. Browser checks include actual 4K glitter maps, downgrade, capacity recovery, reload and independent 2K export. 160 tests, typecheck/build pass. Complex 4K masks can still bake slowly. [Contract/evidence](../research/authoring/preview-quality-contract.md). Separate addable softness controls are explicitly on hold while Nathan evaluates per-point softness.

**Directional softness and crisp UV controls:** per-point edge widths now work independently of pigment, with preserved old looks, Undo, reload, library save and shared compiler sampling. The UV pane now renders paths/handles at actual display resolution, fixing blur in wide sidebars and high-DPI displays. 141 tests, typecheck/build and isolated browser checks pass. [Contract and evidence](../research/authoring/directional-softness-contract.md).

**Sidebar width follow-up:** fixed 640px caps removed. Panels can use the available window width while retaining viewport/opposite-panel minima; dragging redistributes space, Escape restores both panels and wider preferences survive reload. Browser drag/keyboard/reload checks and workspace tests pass. [Details](../research/backlog/viewport-and-editor-controls.md#sidebar-width-follow-up--23-september-2026).

**Cancellable mask checkpoint:** obsolete worker jobs now yield and cancel, publish only complete current masks, retain other layers during active-layer priority, and recover lazily after failure. Rapid browser pigment edits cancelled two stale jobs; preset switching and 2048 mask export passed. See [scheduling evidence](../projects/xf-studio/authoring/evidence/raster-scheduling-2026-09-23.json). [Directional softness research](../projects/xf-studio/authoring/tools/softness-study/README.md) recommends a provisional linear width field and documents opposing-edge coupling; no production softness controls changed. [Glitter review](../research/materials/glitter-reference-review.md) identifies regular disk placement, radial shading and missing independent flake colour; the replacement material remains to implement.

**Shape gestures checkpoint:** drag active makeup to move it, Shift-drag to rotate around the selected point, Shift-scroll to scale. UV scroll zooms and right-drag pans, with persisted view state and independent Undo semantics. Both UV and real-head browser checks pass. The new UV triangle index preserves exhaustive lookup results while greatly reducing measured search time. [Contract, API and limits](../research/authoring/shape-gesture-contract.md); [evidence](../projects/xf-studio/authoring/evidence/shape-gestures-2026-09-23.json). Directional softness, glitter refinement and raster cancellation remain editor/material work.

**Shape-scale precision follow-up:** Shift-scroll now scales by about 2% per ordinary wheel notch in both UV and on-head editors, with proportional fractional trackpad movement and a per-event cap. Ordinary UV wheel zoom is unchanged. Focused gesture and isolated browser Undo checks pass; the full 200-test authoring suite, typecheck and both browser bundles pass.

**Bézier editing checkpoint:** selected points now have UV and on-head tangents, with Smooth, Symmetric and Corner modes. New layers use recipe-5; old recipes retain their automatic curves until explicitly upgraded. Splitting a Bézier section preserves its continuous shape, and handles survive Undo, reload and SQLite reopening. 101 tests, typecheck/build and isolated browser checks pass. [Evidence and limits](../projects/xf-studio/authoring/evidence/bezier-controls-2026-09-23.json). Whole-shape transforms, UV pan/zoom, directional softness and the supplied glitter correction remain queued. Extreme valid handles expose raster/guide latency; cancellation and indexed UV lookup need follow-up.

**Continuous pigment checkpoint:** the point-weight seam now has a production fix. New layers use smooth gradients; existing designs preserve exact legacy masks until explicitly upgraded using Smooth point gradients. Point pigment, blend and edge softness are labelled separately. Recipe-4 persists the choice through workspace, SQLite and portable exports; Undo restores the old mode. 85 tests, typecheck/build, model preview and isolated save/reload/reopen checks pass. [Evidence](../projects/xf-studio/authoring/evidence/continuous-pigment-2026-09-23.json). Bézier/corners, directional softness, whole-shape transforms and UV navigation remain next editor work.

**Plate diagnostic:** denser views expose ten additional face/poses. Four crease vertices still fail the mixed-margin model even at half the original positive clearance; independent checks pass for all 30 cases and the prior 1,625 accepted vertices. [Report](../experiments/006-plate-clearance/research/visibility_margin.md). No geometry changed; the next useful investigation is finite-triangle contact/distance, not another smaller infinite-plane margin.

**Latest interaction/material requests recorded:** whole-shape dragging, Shift+wheel scaling, Shift+drag rotation about the active point, and UV wheel zoom/right-drag pan are queued in the [path controls backlog](../research/backlog/path-and-falloff-controls.md). The deferred [Opus brief](../research/backlog/claude-ui-overhaul.md) now requires context menus and a discoverable typed action API across UI capabilities. Five [glitter references](../research/backlog/glitter-visual-references.json) are preserved locally with hashes and attribution gaps; the current regular-dot material needs a fine, irregular-flake pass. Native browser menus are now suppressed on non-text surfaces; text editing menus and viewport right-drag pan remain functional. Typecheck/build and isolated browser checks pass; [evidence](../projects/xf-studio/authoring/evidence/context-menu-2026-09-23.json). Existing makeup-first priority remains.

**Multiple-warp checkpoint:** each makeup layer now supports 0–8 independent controls, with add/remove, individual reach, selected influence guides and mirrored UV/surface dragging. `xfs/recipe-3` migrates v1/v2 single-field designs with byte-identical fixture masks, retains old SQLite revisions and saves selection per preset/layer. 73 tests, typecheck/build, browser Undo/Escape/save/reload and compiler coverage checks pass. The service was restarted after verified SQLite backups. [Evidence](../projects/xf-studio/authoring/evidence/multiwarp-2026-09-23.json). Continuous weights, Bézier/corners and directional softness remain next editor work.

**Brow follow-up evidence:** supplied game frames show a changing brow-to-eye gap; nonzero motion in our current clip does not reproduce that by itself. [Animation audit](../research/animation/brow-idle-gap.md) records the missing wrinkle outputs and remaining graph/deformation checks. [Texture audit](../research/eye-artistry/brow-texture-audit.md) finds the intended installed Arkhe FULLER/style-18 candidate, but the preview samples green as opacity and omits secondary/normal inputs and the Alliekat gradient override. Confirm actual shader coverage before changing the renderer; work remains queued after makeup.

**Plate classification:** all 40 faces around the ten fixed-shell failures become exposed during sampled motion. Three failures retain wholly exposed certificates, so dropping hidden constraints alone cannot meet the original bounds. [Classification and next experiment](../experiments/006-plate-clearance/research/fixed-failure-classification.md). No geometry was changed.

**Latest execution order:** finish makeup work; then eyebrow shape investigation and correct eyebrow/eyelash colours; then optional hair-mesh rendering; then other queued work. The reported missing movement above the eyes during idle is recorded with the brow fidelity investigation. Hair preview is now explicitly requested, while later feature authoring still requires discussion.

**Latest product direction:** ONE eye-makeup selector chooses complete studio-authored presets. A local SQLite library and portable recipes retain editable source; a future compiler exports only authored combinations. This supersedes the old four-selector/palette matrix. [Product decision](../projects/xf-studio/data/product-direction.md) records stable identity/collection requirements and the later feature order. Piercings/earrings, eyebrows, cheek makeup, hair, facial expressions/custom idles, then full-body tattoos each require discussion before implementation.

**Editor checkpoint:** makeup layers now support add, duplicate, rename, remove (including the last layer), pointer reorder, up/down controls and Undo. Variable stacks propagate through masks, animation, persistence and SQLite revisions. Both sidebars resize with pointer/keyboard and retain widths; scrollbars are themed. New `xfs/recipe-2` files support 0–32 layers, with legacy four-layer imports preserved. Preset accordions/collection editing now work; see the collection checkpoint below. Brow thickness and accurate brow/lash colours are now explicitly tracked with Nathan’s preserved comparison in the [fidelity queue](../research/backlog/preview-fidelity.md).

**Matte flicker correction:** adaptive camera depth precision reduces missing full-plate diagnostic pixels from 12,641 to 20 across 36 measured camera/idle/eye-shape cases. Narrow-FOV matte preview checked in the isolated browser; 37 tests and typecheck/build pass. This is a browser depth-buffer correction, not a solution to all geometric contacts. [Evidence and reproduction](../projects/xf-studio/authoring/README.md#matte-flicker--preview-depth).

**Collection checkpoint:** accordions now support preset add/copy/rename/reorder/remove/recovery above compact layers. Per-preset drafts and Undo survive switching/reload; SQLite v2 stores atomic immutable collections with stale-write protection and preserves legacy revisions. Portable collection import/export and build-plan export are connected. Layer drag now shows a floating label/insertion marker, and the ⋯ menu exposes Rename. 41 tests, typecheck/build and isolated browser checks pass. A real editor-authored four-preset collection plus Off compiled into an 856,064-byte archive; all 16 payloads and 105 morphs passed offline verification. Nothing installed. [Evidence](../projects/xf-studio/authoring/evidence/preset-collections-2026-09-23.json). The follow-up state for idle, FOV/framing/zoom and pan is [recorded](../research/backlog/viewport-and-editor-controls.md).

**Idle controls checkpoint:** Pause/Resume and independent Head movement / Facial movement controls now work; disabled idle resets time/pose while retaining subset choices. Paused pose, framing and settings survive reload, and makeup remains editable. 46 automated tests and browser checks pass. Offline sampling of 222 phases in two customization states finds fixed structural head, varying gaze/jaw/lids, finite vertices and zero pause/reset errors. [User guide](idle-animation-guide.md), [asset evidence](../projects/xf-studio/authoring/evidence/idle-controls-offline-check.json).

**Brow idle gap audited:** the current 22-second default clip does move all brow bindings, but its brow-card trajectories are subtle. A separate four-second game `ui_closeup_shot_eyes` clip has much stronger bilateral brow-raise tracks, plausibly relevant to Nathan's in-game eye close-ups. The live character-creator graph/clip layering is not yet proven, so no clip was substituted. Offline controls verification now covers all 390 brow-card vertices in neutral and saved-face poses; both modes passed. [Measurements and limits](../research/animation/brow-idle-gap.md#24-september-compare-the-other-source-close-up-clips).

**Eye diffuse and surface controls:** the captured V now selects Kala’s eye-16 diffuse through its researched CCXL identity, with hash-verified local inputs, explicit fallback, source credit and exact reload preservation. Optical shading and generalized resolution remain open. Surface controls draw over the current transparent lashes/brows while retaining opaque head/eye occlusion. Browser checks cover gaze, missing-image fallback, lash-line dragging, closed lids, Undo and far-side rejection. [Eye evidence](../projects/xf-studio/authoring/evidence/saved-eye-preview-2026-09-23.json), [overlay evidence](../projects/xf-studio/authoring/evidence/surface-overlay-2026-09-23.json). 56 tests (including four research-prototype checks), typecheck and browser build pass.

**Parallel research checkpoint:** the [idle control design](../research/animation/idle-controls-design.md) confirms separate body/facial contributions and records pause/persistence/framing invariants. [Saved-eye resolution](../research/eye-artistry/modded-eye-resolution.md) traces the selected option through nutboy’s Unique Eyes to CCXL to Kala’s Eyes Standalone V2; the selected diffuse is now integrated as described above; the other maps remain local research inputs. [Camera findings](../research/authoring/camera-control-findings.md) verify existing right-drag pan and reload persistence; the viewport now advertises that gesture. The [camera framing pass](../research/authoring/camera-zoom-design.md) keeps a visible face plane in place as FOV changes, extends narrow-pane Front framing, adjusts depth clipping, and restores the longer orbit after reload. Isolated browser checks covered 10°/90° FOV, panned-eye close zoom and reload; the exact reported zoom restriction was not reproduced. Main integration passes 232 tests, typecheck and build. [The eye adapter audit](../research/eye-artistry/eye-preview-adapter-plan.md) verifies UV/colour-space compatibility for a first diffuse correction and identifies packed-normal/optical caveats. Further eye optics remain open.

**Renderer research:** Nathan supplied [wgpu](../research/backlog/wgpu-renderer-assessment.md) for assessment during the rendering-fidelity pass. A first source check is complete; any comparative prototype, target-hardware timing and renderer decision remain future work.

**Eye/lip optics source audit:** the installed 2.31 eye G-buffer shader samples roughness **red** times its material scale; the earlier green-channel guess came from Three's convention and is wrong for that game variant. Eye albedo/normal and roughness also use different UV paths, and the browser still uses a single diffuse-only eye material. The white lip line is absent from the source albedo, while its roughness/lighting, normals, geometry and missing mouth context remain competing causes. No speculative material change was made. [Compiled input evidence and fixed-camera diagnostic gates](../research/eye-artistry/eye-lip-optics-audit.md). [The first wgpu source check](../research/backlog/wgpu-renderer-assessment.md) finds native Vulkan-only experimental ray queries in documented 30.0.1; it supplies no reason to migrate this browser renderer before correcting material inputs.

**Opt-in saved-eye roughness study:** the exact local Kala roughness map is now hash-verified and can be previewed with source red copied into Three's green roughness channel at the inspected material scale. The diffuse-only material remains the default and failure fallback. Fixed-camera isolated comparison changed both iris reflections but did not establish a fix for waxiness; normals, refraction, UV semantics and game lighting remain open. Main verification toggled it without console errors; 236 tests, typecheck and build pass. [Source and visual limits](../research/eye-artistry/eye-roughness-preview.md).

**Path editing requests queued:** nearest-section insertion, single-eye UV view, continuous point weights, Bézier/corner handles, directional softness and surface controls above lashes are preserved in the [seven-item backlog](../research/backlog/path-and-falloff-controls.md). A numeric fixture confirms an internal zero-to-full-opacity discontinuity in the current nearest-edge weight evaluator. The [design note](../research/authoring/path-and-falloff-design.md) separates this defect from directional softness and covers compatible curve migration. A [measured comparison](../research/authoring/continuous-weight-study.md) selects regularized boundary integration for the next versioned strength prototype; uniform-mask parity, subdivision invariance, crossing limits and CPU costs are recorded. Surface ordering, nearest-section insertion and persistent single-eye UV editing are now implemented. Mirrored points and field controls share the recipe, support Undo/Escape and pass isolated browser checks. The sidebar introduction was removed as requested. 65 tests, typecheck and build pass; [UV evidence](../projects/xf-studio/authoring/evidence/uv-editor-2026-09-23.json). Continuous strength, Bézier/corner handles and directional softness remain pending.

**Preview workspace:** adjustable vertical FOV and full current control/selection/camera/lighting/panel state now persist in a validated browser workspace. Imported V restores on reload, fixing the reported regression; later eye-shape changes also survive. Library identity/revision, unsaved names and Undo survive without fetching a saved recipe over the draft. Actual browser checks covered import, reload, idle framing, revision continuation and Undo; 32 automated tests, typecheck and build pass. The user's live draft was not used for testing. The selected modded eye diffuse is locally resolved, with roughness in an opt-in study; broader mod resolution, SSS and eye/lip-seam shader diagnosis remain [queued](../research/backlog/preview-fidelity.md).

**Idle side quest:** the viewport's Character-creator idle toggle now plays the extracted female UI close-up body motion and facial motion baked with the community IO Suite solver. It includes mouth/eyelids/gaze; the eyes now have separate rig attachments. Browser verification found 387 mapped bones, no unmapped bones/errors, correct on/off controls and unchanged recipe. Offline checks cover the full facial loop, finite deformation, shared-bone agreement and exact reset. 27 tests, typecheck and browser build pass. Exact runtime graph selection/synchronization, wrinkle shading and scale-driven effects remain unverified. [Plain-language guide](idle-animation-guide.md), [reproduction](../research/animation/cc-idle.md), [credits](community-credits.md). Primary focus returns to eye-makeup preset compilation.

**Plate conversion:** [Experiment 004](../experiments/004-plate-import/README.md) rebuilds both mesh and morph resources from the owned neutral master. Source head correspondence is exact; original game shading is retained to avoid clipped Blender-derived morph normals/tangents. Numeric round-trip bounds pass: 105 facial shapes, both UV sets, all 122 weighted bones and eight influences retained. Neutral morph lighting deltas round-trip exactly; world/inverse-bind transforms agree within 1.2e-7. [Experiment 006](../experiments/006-plate-clearance/README.md) now tests six converted offset candidates through 107 static cases and 73 idle samples. All 3,010 plate triangles match the head. Offsets reduce overlaps greatly, but residual eyelid crease/corner contacts mean no release offset is approved. Actual game rendering remains unverified. Nathan's private [source repository](https://github.com/axefrog/xf-studio) holds source/research checkpoints; personal inventories and binary game-derived assets remain local.

**Plate deformation fidelity:** experiment 006 now preserves original packed skin weights in both the mesh and the morph file's separate base vertex buffer. Exported weights match the head exactly; paired tests retain identical geometry, UVs and all 105 morphs across 73 idle samples. This removes up to 0.00001788 units of conversion-induced motion difference but leaves exposed eyelid contacts. The visibility study and constrained-offset audit show why neither dismissing all folds as hidden nor using unbounded local extrusion is justified. [Evidence and next step](../experiments/006-plate-clearance/README.md).

**Fixed plate-correction result:** the bounded all-incident-plane construction fails at ten of 1,635 vertices across 73 sampled poses (seven over budget, three opposing constraints). An independent optimizer-free verifier checks accepted offsets and failure certificates. No mesh was changed or imported. Next inspect the marked crease regions and reconsider the constraint model; this does not rule out other surface constructions. [Evidence](../experiments/006-plate-clearance/fixed_feasibility.md).

**Preset compiler checkpoint:** the [first flat material adapter](../research/materials/preset-compiler-contract.md) composites matte/satin/metallic layers into diffuse, roughness and metalness maps using the inspected game's destination encoding. Arithmetic/raster tests pass; unsupported optics fail explicitly. It consumes exported collection JSON through the offline package command; the UI exports source/build plans, not installable packages. Mixed optical finishes, between-texel filtering and game rendering remain unfinished.

**Flat export mip checkpoint:** the four-preset offline fixture now supplies complete coverage-aware diffuse, roughness and metalness mip chains instead of letting import average encoded alpha. All twelve decoded base maps remain byte-identical to the prior import route; decoded 64² partial-edge coverage error falls from about .048–.065 to .022–.028 across the four looks. Two fresh main-checkout builds verify 16 identical resource payloads, all mip levels and 105 morphs; a stable component CRUID removed resource-level randomness. WolvenKit still writes current timestamps into the archive index, so the two package hashes differ despite identical payloads. Filtering between texel centres, actual game rendering, plate clearance and other finishes remain unproved. [Method and measurements](../research/materials/flat-preset-mip-filtering.md).

**One-selector package now built:** [Experiment 005](../experiments/005-preset-collection/README.md) compiles two complete authored looks plus Off into an 831,488-byte archive with one material template, six textures, one mesh/morph pair and one CCXL selector. Actual binary round-trip, compiled component bindings, texture metadata/base-mip error and all 10 unpacked resource hashes pass. Stable UUID names survive rename/reorder in tests; 29 tests and typecheck pass. No installation or game verification yet. Installable export UI, controlled plate clearance, other optical finishes and runtime selection/save behaviour remain unfinished.

**Earlier named-look checkpoint (superseded by collections above):** seven [finish families](../research/materials/makeup-finish-taxonomy.md), including separate Metallic, Glossy and Colour-shifting. SQLite named looks, immutable revisions, Save a copy, explicit Open look, stale-write protection and separate test storage now work. 21 tests and typecheck pass; browser save/revision/reload/open/undo/copy and new finish previews were verified without changing the main draft. DB autosave/history UI/backups, collection compilation, in-game rendering and desktop packaging remain pending. [Desktop assessment](../research/authoring/desktop-packaging.md) records Electron/Electrobun tradeoffs.

New research: [multilayered makeup assessment](../research/materials/multilayered-makeup-assessment.md) follows Nathan's wiki links and CDPR's primary explanation. Current standard multilayered template has blending disabled/depth writes enabled; it is not a drop-in transparent eye-plate decal. Offline compositing and relevant variants remain to investigate. [Claude UI/UX overhaul](../research/backlog/claude-ui-overhaul.md) is explicitly queued at lower priority for `claude` / Opus 5.5, only after UI and functionality are fully decoupled.

**Scope/name update:** Nathan renamed the product to **XF Studio** and requested the shorter `xfs_` prefix for new game resources. Quest design is recorded as a future direction; eye makeup remains first. The existing project directory, depot root, browser keys, SQLite data and serialized format IDs remain compatible; see the [naming contract](../projects/xf-studio/data/naming.md). Historical experiments retain their original names and evidence.

XFS verification: 32 tests (including existing collection input and workspace/library compatibility), typecheck and browser build pass. The running server serves the new title/header without touching the active browser draft. A fresh experiment 005 archive passes binary round-trip, XFS name/reference checks, all 10 unpacked payload hashes and texture checks; all 105 morphs and original model buffers remain intact. No installation or game verification was performed.

The earlier XF Appearance Studio / `xfas_` rename passed its 17-test and 15,680-case historical contract checks; those results describe the older namespace. [Glitter research](../research/materials/glitter-shader-investigation.md) identifies shipped compiled shader code; convincing game/browser glitter remains pending.

Material progress: [Experiment 002](../experiments/002-flake-material/README.md) adds stationary reflective-flake browser candidates, editable fineness/density/orientation, a movable key light and identical texture bakes for engine trials. All ten PNGs match their raw inputs; its original 19-test verification passed. The menu subsequently expanded to seven families above. Finish accuracy, overly dark speckles at some angles, normal blending in-game and subpixel sparkle filtering remain open. Nathan welcomes useful unsolicited insights as research advances.

[Experiment 003](../experiments/003-decal-material-import/README.md) now imports ten XBM textures and ten material instances for the game-side comparison. Actual decal DXIL confirms RG normal reconstruction, red-only scalar maps, independent blend weights and squared colour coverage. Texture metadata, material round-trips, local dependencies and decoded base-level compression error are checked; colour-mask compensation substantially reduces the measured edge-weight discrepancy. This is a resource fixture, not a working CCXL mod: mesh/morph/app assembly and runtime proof remain pending.

Foundation survey and initial ArchiveXL investigation completed. XF Studio (formerly Eye Artistry) is first; Photo Mode Tools is a separate second project. No legacy source implementation was edited, no game launch was requested, and no mod was deployed.

The first [XF Studio authoring prototype](../projects/xf-studio/authoring/README.md) is now running at [localhost](http://127.0.0.1:4317/). It edits procedural masks on the actual head/expanded plate, preserves all eight bone influences and 105 customization morphs, exports recipes/2K alpha masks, and offers exploratory blink motion. Materials/pose are still approximations.

Direct surface editing now has a working first implementation: curve and field controls drag on either side of the face, remain attached through blink/morph changes, share the UV recipe and support whole-gesture Undo/Escape cancellation. UV gap guards and head/eye occlusion are checked. 17 automated tests, typecheck and browser build pass; actual browser gestures and deformation were verified in the isolated page. Remaining authoring refinements and the explicit four-class/glitter material request are tracked in the [authoring queue](../research/backlog/eye-artistry-authoring.md).

Saved-V import is proven for the newest local 2.31 save: the whole appearance node decoded (zero trailing bytes), including CCXL/legacy Eye Artistry choices, and five facial morphs applied in the browser. Saved Arkhe brows 18 and Soft Natural lashes now render with independent toggles, full skin weights and their corresponding facial morphs; colours and blink remain approximations. Remaining appearance-resource assembly is pending; do not call this a complete reproduction of Nathan's current V. See [save import evidence](../research/eye-artistry/save-import.md).

## Completed

- Inventoried 43 external top-level `D:/Dev` folders plus the new HQ's role, all 6 RedModding top-level folders and 15 project folders, nested project/repository boundaries, 999 MO2 folders including 11 separators, profiles and both overwrite generations. 382,741 files/39,377 directories; exclusions and coverage are explicit. See [inventory](../inventory/README.md).
- Assessed old agent work: relevant `sx-cp2077` research is useful but was left at planning/prerequisite stage. Do not resume the old coordination toolbox.
- Refreshed 30 external source references; 22 advanced. ArchiveXL, TweakXL, Codeware, RED4ext/SDK, redscript and WolvenKit references are ready for research at their recorded revisions. Forks/branch caveats remain documented.
- Traced Eye Artistry generation: 15,681 material entries/local instances, 80 generated app files and 4 layer controls. Preserved latest art/backup chronology and hashes.
- Reconciled February 2026 plate lineage: `.011` is `.010` with saved facial shapes baked in, not a separate design. Nathan confirms he cut the plate from the head; all plate vertices and 105 shapes match the head exactly. Owned neutral master retains full deformation; no source save. [Plate audit and intake](../research/eye-artistry/lineage.md).
- Compared historical material templates: only `EMP_Normal` → `EMP_Front` differs; depth writes remain disabled. Geometric ordering is a hypothesis to test.
- Extracted/serialized three CCXL consumers, verified current ArchiveXL 1.27.3 composite material behavior from source, and wrote a concrete reduction strategy.
- Passed a fresh dynamic-material JSON/CR2W round-trip and all 15,680 baseline naming/path cases. This is offline contract evidence, not a working/render-tested mod.
- Created a reusable log capture helper and captured an existing-session baseline without launching the game.
- Saved/validated Nathan's Nexus API credential outside the repo; deleted supplied `temp.txt`.
- Queued the requested [CCXL capability investigation](../research/backlog/ccxl-character-creator-capabilities.md).

## User decisions that must carry forward

- Reimplement mods cleanly within HQ; mine original code/art as reference. No new all-in-one xf-omega toolbox.
- Old Eye Artistry designs, preset identities and IDs may all be replaced. Nathan prioritizes the layered structure and concept, and wants to author a fresh content collection with the new tools. Legacy catalogue parity is not an acceptance criterion.
- Build a Bun/TypeScript/Three.js local procedural authoring tool with shape controls, weights, falloffs, vector fields, actual meshes/material approximation and blink support. This also serves exploratory character-rendering work. Save import should eventually reconstruct the user's current V using their installed assets.
- Direct live editing on the model surface was explicitly requested; the first implementation now supports draggable curve/field controls that follow deformation. Remaining refinement and fidelity work must not be confused with complete authoring/mod delivery. The persistent [authoring queue](../research/backlog/eye-artistry-authoring.md) also covers configurable asset sources for MO2, Vortex and manual mod folders.
- User prioritizes more Eye Artistry colours. 49 is an old implementation compromise; do not freeze it into a product requirement. Avoid WolvenKit's prior full-material-matrix slowdown; benchmark residual UI/atlas/cache growth.
- Design for latest stable frameworks rather than constraining work to old installed versions. ArchiveXL 1.27.3, TweakXL 1.11.4 and Codeware 1.20.5 are now enabled only in the new diagnostic MO2 profile; actual game-loaded versions remain unverified.
- Blender can be upgraded if needed. Current headless 5.0 inspection succeeded; Blender MCP was not exposed in this session.
- Batch runtime tests to minimize Nathan's effort and launches. Use source/resource/geometry checks and diagnostics creatively.

## Next concrete work

1. Resolve local eyelid crease/corner contacts identified by [experiment 006](../experiments/006-plate-clearance/README.md). Four real converted offset candidates, 107 static cases and 73 idle samples now distinguish vertex-plane clearance from triangle contacts. All 3,010 plate faces match the source head; no blanket offset is approved. Classify visible/occluded contacts and test a local correction while preserving makeup coverage. Neutral master and [plate import](../experiments/004-plate-import/README.md) controls remain unchanged.
2. Extend the [one-selector authored-preset fixture](../experiments/005-preset-collection/README.md): controlled plate clearance, remaining optical finish adapters, installable export UI and runtime switching/save proof; editable SQLite collection/source export is complete. Two flat-finish looks plus Off are now packaged and verified offline. The old [ArchiveXL matrix contract](../research/archive-xl/eye-artistry-strategy.md) remains mechanism evidence, not the product target.
3. Verify morph import/export, paths, serialization and proposed offset candidates offline. Benchmark larger palette growth before selecting a full release palette.
4. Verify the diagnostic profile's effective framework/resource winners, preserve a recoverable test save, and execute the [single-session test card](validation.md) when ready. The package and side-by-side stable frameworks are staged without a game launch.
5. Research CCXL one-selector preset switching and identity persistence; the broader capability exploration remains queued. Extend SQLite with history/restore, autosave and export collections as the compiler takes shape.
6. Develop XF Studio from the validated first slice: exact game material mapping, authentic blink data, multiple shapes/fields and ergonomic authoring; resolve saved appearance hashes against installed winners and assemble a faithful V. Keep current limitations visible and work in an isolated verification page rather than changing Nathan's live draft.

## Open proof obligations

Runtime propagation of custom CCXL app names to these morph-skinned meshes; single-template mesh fallback in this resource shape; correct normal/morph transfer for the new topology; deterministic decal stacking; distinct finished material looks; actual large-palette editor/runtime performance; stable IDs/save behavior. None is disguised as a completed in-game check.

Entry points: [HQ README](../README.md), [XF Studio project](../projects/xf-studio/README.md), [lineage](../research/eye-artistry/lineage.md), [toolchain](toolchain.md), [validation](validation.md), [research queue](../research/backlog/README.md).
